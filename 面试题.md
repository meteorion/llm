# 大模型应用开发 · 面试题

> 持续更新，当前覆盖 Day 1–30（全部完成）+ Day 1–7 补充考点（Q156–Q174）

---

## 目录

**大模型基础**
- [Q1. 什么是大语言模型（LLM）？和传统 NLP 的核心区别？](#q1)
- [Q2. 大模型的训练分为哪三个阶段？](#q2)
- [Q3. Transformer 的核心机制是什么？为什么比 RNN 更适合大模型？](#q3)
- [Q4. Temperature 参数的作用？0、0.7、1.5 分别适合哪些场景？](#q4)
- [Q5. 大模型有哪些典型局限性？各自的解决方案？](#q5)
- [Q6. 闭源模型和开源模型如何选择？](#q6)
- [Q156. Top-P（核采样）和 Top-K 采样是什么？和 Temperature 有什么关系？](#q156)
- [Q157. Zero-shot、One-shot、Few-shot 分别是什么？In-context Learning 的本质是什么？](#q157)
- [Q158. Base Model 和 Chat Model（Instruct Model）有什么区别？应用开发中应该用哪种？](#q158)

**核心概念：Prompt / Token / 上下文窗口**
- [Q7. 什么是 Prompt？好的 Prompt 应该包含哪些要素？](#q7)
- [Q8. 什么是 Token？中英文的换算规则是怎样的？](#q8)
- [Q9. 上下文窗口是什么？超限后有哪些处理策略？](#q9)
- [Q159. Token 优化有哪些常见策略？从 Prompt 设计、历史管理、模型选择三个角度分析。](#q159)

**核心概念：Embedding / RAG / Tool Calling**
- [Q10. 什么是 Embedding？和关键词搜索的本质区别？](#q10)
- [Q11. RAG 是什么？解决了哪些问题？请描述完整流程。](#q11)
- [Q12. Tool Calling 是什么？模型是如何"调用"工具的？](#q12)
- [Q13. Tool Calling 和 Agent 有什么关系？](#q13)

**Python 基础**
- [Q14. 为什么大模型开发项目必须使用虚拟环境？](#q14)
- [Q15. 列表和字典在大模型应用中最常见的使用场景？](#q15)
- [Q16. 调用外部 API 时，异常处理应该捕获哪些错误？](#q16)
- [Q160. requirements.txt 的作用是什么？如何生成和使用它？](#q160)
- [Q161. pathlib.Path 和 os.path 有什么区别？为什么推荐在新项目中使用 pathlib？](#q161)
- [Q162. Python with 语句（上下文管理器）的原理是什么？在 LLM 项目中有哪些典型应用？](#q162)
- [Q163. *args 和 **kwargs 分别是什么？在封装 LLM 客户端时有哪些典型用途？](#q163)
- [Q164. Python 类型注解（Type Hints）的作用是什么？在 LLM 应用代码中为什么值得加？](#q164)

**API 调用**
- [Q17. API Key 为什么不能硬编码？正确的管理方式？](#q17)
- [Q18. HTTP 状态码 401、404、429 分别代表什么？如何处理？](#q18)
- [Q19. 用 OpenAI SDK 调用国内兼容接口需要注意什么？](#q19)
- [Q20. 流式输出（Streaming）的原理是什么？如何实现？](#q20)
- [Q165. HTTP 请求的基本结构是什么？请求头中哪些字段在 LLM API 调用中最重要？](#q165)
- [Q166. 什么是指数退避重试？为什么比固定间隔重试更好？如何实现？](#q166)
- [Q167. API 返回的 JSON 如何安全解析？json.loads() 和 response.json() 有什么区别？](#q167)

**多轮对话**
- [Q21. 为什么每次调用 API 都要传完整的消息历史？](#q21)
- [Q22. 多轮对话中消息历史的维护规则是什么？](#q22)
- [Q23. 如何实现消息历史截断，同时保证对话质量？](#q23)
- [Q24. system、user、assistant 三种角色各有什么典型用法？](#q24)
- [Q25. 从零设计一个命令行聊天程序，核心要考虑哪些模块？](#q25)
- [Q168. 流式输出时为什么必须等所有 chunk 接收完才能把 assistant 消息加入历史？](#q168)
- [Q169. 多轮对话的消息历史如何持久化保存？序列化到 JSON 再恢复时需要注意什么？](#q169)

**Prompt 工程与摘要任务**
- [Q26. 为什么说"总结这篇文章"是一个糟糕的 Prompt？](#q26)
- [Q27. Prompt 约束有哪四个维度？每个维度举一个摘要场景的例子。](#q27)
- [Q28. Prompt 层的长度约束和 max_tokens 参数有什么区别？应该如何配合使用？](#q28)
- [Q29. temperature 参数在摘要任务中应该设多少？为什么？](#q29)
- [Q30. 如何评估一个摘要 Prompt 写得好不好？](#q30)
- [Q170. Few-shot 示例在 Prompt 中的作用是什么？如何在摘要任务中设计有效的 Few-shot 示例？](#q170)
- [Q171. Prompt A/B 对比实验应该如何设计？开发集和留出集各起什么作用？](#q171)

**结构化信息抽取**
- [Q31. 信息抽取和文章摘要的核心区别是什么？](#q31)
- [Q32. 为什么要求模型只输出 JSON？有哪些方法可以强制 JSON 格式？](#q32)
- [Q33. 信息抽取 Prompt 的字段定义应该包含哪些要素？](#q33)
- [Q34. 抽取任务中常见的失败模式有哪些？各自如何解决？](#q34)
- [Q35. 如何设计一个可复用的通用信息抽取器？](#q35)
- [Q172. 如何用 Few-shot 示例提升信息抽取的稳定性？示例应该覆盖哪些情况？](#q172)
- [Q173. LLM 返回的 JSON 字符串有哪些常见格式问题？如何实现健壮的 parse_json_safe 函数？](#q173)

**第 1 周综合复盘**
- [Q36. 大模型 API 的计费逻辑是什么？哪些地方最容易产生不必要的 Token 消耗？](#q36)
- [Q37. Temperature 参数在数学上是如何影响模型输出的？](#q37)
- [Q38. 大模型 API 为什么设计成无状态的？这对应用开发有什么影响？](#q38)
- [Q39. 聊天 / 摘要 / 信息抽取三个 Demo 在设计上有哪些关键差异？](#q39)
- [Q40. 如果让你从零搭建一个"第 1 周成果汇总"工具包，你会如何设计它的模块结构？](#q40)
- [Q174. 不看账单，如何快速估算一次 LLM 调用的成本？估算公式和关键参数是什么？](#q174)

**结构化输出**
- [Q41. "能被 json.loads() 解析"和"结构化输出可靠"是一回事吗？三层约束分别解决什么问题？](#q41)
- [Q42. JSON Schema 中 required 和 additionalProperties 分别解决什么问题？](#q42)
- [Q43. 为什么推荐用 Pydantic 而不是手写 JSON Schema + 校验代码？](#q43)
- [Q44. 抽取任务中枚举字段的值容易跑偏，如何在 Prompt 和代码层面双重兜底？](#q44)
- [Q45. 如何从零设计一个支持"批量抽取 + 类型安全 + 失败重试"的结构化抽取器？](#q45)

**输出校验与异常处理**
- [Q46. 大模型调用链路中常见的异常有哪些？如何区分可重试与不可重试？](#q46)
- [Q47. 立即重试和指数退避有什么区别？为什么批量任务更适合指数退避？](#q47)
- [Q48. json.JSONDecodeError 和 pydantic.ValidationError 分别代表什么层面的失败？处理策略有何不同？](#q48)
- [Q49. 抽取结果字段缺失时，如何设计兜底策略而不是简单地"报错"或"忽略"？](#q49)
- [Q50. 如何从零设计一个不会因单条脏数据崩溃的健壮批量抽取器？](#q50)

**文本分类**
- [Q51. 分类任务和信息抽取任务在 Prompt 设计上有什么本质区别？](#q51)
- [Q52. 一个好的标签集合应该满足哪些设计原则？](#q52)
- [Q53. 为什么用 enum 而不是普通字符串字段约束分类标签？单标签和多标签分类的实现有什么不同？](#q53)
- [Q54. 为什么不能盲目相信 LLM 单次调用给出的分类结果？如何用 Self-Consistency 估计置信度？](#q54)
- [Q55. 如何从零设计一个可评估准确率、支持批量运行的文本分类器？](#q55)

**Prompt 优化**
- [Q56. Prompt 优化的四类手段分别解决什么问题？](#q56)
- [Q57. 为什么说 Prompt 优化的本质是"消除歧义"而不是"讨好模型"？](#q57)
- [Q58. 准确率和稳定性是一回事吗？如何分别量化这两个指标？](#q58)
- [Q59. 如何判断一次 Prompt "优化"是真的提升了效果，还是恰好在测试集上蒙对了？](#q59)
- [Q60. 如何从零设计一套可复现的 Prompt 版本对比实验？](#q60)

**建立成本意识**
- [Q61. 大模型 API 输入 Token 和输出 Token 为什么定价不同？这对 Prompt 设计有什么启发？](#q61)
- [Q62. 为什么说多轮对话的 Token 消耗是"平方级增长"而不是线性增长？](#q62)
- [Q63. 长输入短输出和短输入长输出两类任务，成本优化的着力点分别是什么？](#q63)
- [Q64. 如何从零设计一个用于统计和记录大模型调用成本的工具？](#q64)
- [Q65. 如何量化评估一次 Prompt 优化在"效果提升"和"成本增加"之间是否划算？](#q65)

**配置管理和日志**
- [Q66. 为什么 API Key 不能硬编码？`.env` + `.gitignore` 方案如何配合使用？](#q66)
- [Q67. 配置的三种来源（命令行参数、环境变量、.env 文件）优先级如何排序？为什么这样设计？](#q67)
- [Q68. Python `logging` 的五个级别分别适合哪些场景？为什么不用 `print` 替代？](#q68)
- [Q69. 给 LLM 项目的 API 调用加日志，应该记录哪些关键信息？](#q69)
- [Q70. 如何从零设计一个"配置 + 日志 + 重试"三合一的可维护 LLM 客户端？](#q70)

**第 2 周综合复盘**
- [Q71. 第 2 周（Day 8–13）六个主题之间是什么逻辑关系？为什么要按这个顺序学？](#q71)
- [Q72. 什么样的 Prompt 才算"稳定"？如何量化评估稳定性？](#q72)
- [Q73. 为什么说输出校验比"提示模型要小心"更可靠？这体现了什么工程原则？](#q73)
- [Q74. 为什么日志和配置管理要在 Demo 阶段就引入，而不是等项目变大再补？](#q74)
- [Q75. 如果让你从零搭建一个"第 2 周成果汇总"工程骨架，你会如何设计它的模块结构？](#q75)

**RAG 基本原理**
- [Q76. RAG 解决了大模型的哪些核心局限？为什么不能直接把整个知识库塞给模型？](#q76)
- [Q77. RAG 的离线构建和在线问答两个阶段分别做什么？为什么必须严格分开？](#q77)
- [Q78. 文档切分（Chunking）中 Chunk Size 和 Chunk Overlap 各自的作用是什么？如何选取合适的值？](#q78)
- [Q79. RAG 中对 Embedding 模型有什么约束？不同方案（API vs 本地模型）如何选择？](#q79)
- [Q80. 如何从零实现一个最小可用的 RAG 问答系统？请描述关键代码的设计思路。](#q80)

**读取本地文档**
- [Q81. RAG 系统中文档读取阶段的质量为什么至关重要？常见噪音有哪些？](#q81)
- [Q82. 读取中文文本文件时编码问题如何处理？UTF-8、GBK、UTF-8-BOM 有什么区别？](#q82)
- [Q83. 为什么 PDF 文本提取比读取 TXT 文件难得多？有哪些常见坑？](#q83)
- [Q84. 读取文档后为什么需要文本清洗？哪些清洗操作是安全的，哪些会破坏 RAG 效果？](#q84)
- [Q85. 如何设计一个支持多格式的统一文档加载器？请描述接口设计和关键实现思路。](#q85)

**文本切分**
- [Q86. 固定大小切分和按语义边界切分有什么本质区别？各自的适用场景？](#q86)
- [Q87. Chunk Size 和 Chunk Overlap 各自的作用是什么？选取不当会带来什么后果？](#q87)
- [Q88. 递归字符切分（Recursive Character Splitter）的核心思路是什么？为什么推荐作为默认策略？](#q88)
- [Q89. 切分策略对 RAG 检索质量的影响路径是什么？如何设计切分策略对比实验？](#q89)
- [Q90. 如何从零实现一个支持多策略、可对比的文本切分工具？请描述核心设计决策。](#q90)

**Embedding 与向量检索**
- [Q91. Embedding 模型的"一致性约束"是什么？违反后会出现什么问题？](#q91)
- [Q92. 余弦相似度、内积、L2 距离三种度量方式有什么区别？RAG 场景为什么通常选余弦相似度？](#q92)
- [Q93. API-based Embedding 和本地 Embedding 模型如何选择？各自的适用场景？](#q93)
- [Q94. 向量检索中的 Top-K 和相似度阈值分别解决什么问题？应该如何配合使用？](#q94)
- [Q95. 如何从零构建一个支持持久化、批量向量化、带阈值过滤的 RAG 检索组件？](#q95)

**RAG 问答生成**
- [Q96. RAG 场景下上下文拼接的"Lost in the Middle"效应是什么？如何缓解？](#q96)
- [Q97. 为什么 RAG 需要"只根据资料回答"约束？不加约束会出现什么问题？](#q97)
- [Q98. RAG 中拒答逻辑应该在检索层还是生成层实现？两者有何区别？](#q98)
- [Q99. RAG 的 temperature 应该设多少？和普通对话有何不同？](#q99)
- [Q100. 如何从零设计一个端到端的 RAG 问答系统？描述各模块的职责和接口。](#q100)

**RAG 引用与可解释性**
- [Q101. 为什么 RAG 系统需要引用机制？引用解决了什么问题？](#q101)
- [Q102. 自然语言引用和结构化 JSON 引用有什么本质区别？为什么推荐结构化输出？](#q102)
- [Q103. 什么是"虚构引用"？有哪些类型？如何检测？](#q103)
- [Q104. 引用校验的"三道防线"分别解决什么问题？为什么缺一不可？](#q104)
- [Q105. 如何从零设计一个带引用溯源和校验的 RAG 问答系统？描述核心设计决策。](#q105)

**整理作品与总结**
- [Q151. 一个"别人可以直接运行"的项目 README 需要包含哪些核心章节？](#q151)
- [Q152. 如何写"快速开始"章节，让陌生开发者 5 分钟内跑起来？](#q152)
- [Q153. 示例输入输出为什么比功能描述更有说服力？如何选择展示哪些示例？](#q153)
- [Q154. 30 天 LLM 应用开发学习中，最重要的五个认知转变是什么？](#q154)
- [Q155. 一个 LLM 项目从 Demo 到生产级别还需要补充哪些关键能力？](#q155)

**测试和优化**
- [Q146. LLM 应用测试和普通软件测试有哪些核心区别？](#q146)
- [Q147. 如何测试工具调用的"触发准确率"？精确率和召回率分别测什么？](#q147)
- [Q148. 什么是输出稳定性测试？temperature=0 时输出一定稳定吗？](#q148)
- [Q149. 如何分析一次完整对话的 Token 成本？有哪些典型的成本增长陷阱？](#q149)
- [Q150. 如何从零设计一份 LLM 项目的测试记录表？包含哪些维度和字段？](#q150)

**补工程细节**
- [Q141. LLM 项目中异常处理的"分层设计"是什么？各层应该如何处理各自的异常？](#q141)
- [Q142. 给 LLM 项目加日志时，应该记录哪三类关键信息？各自的内容是什么？](#q142)
- [Q143. 什么是"fail-fast"？配置验证为什么要在启动时而不是请求时做？](#q143)
- [Q144. 工具调用的输出校验（output validation）应该验证什么？如何实现？](#q144)
- [Q145. 如何从零给一个 LLM 项目补全"异常处理 + 日志 + 配置验证 + 输出校验"四件套？](#q145)

**整合成完整作品**
- [Q136. 三选一项目方向（文档问答 / 客服助手 / 工具助理）各自侧重什么技术？如何选择？](#q136)
- [Q137. 把多个 Day Demo 整合成一个项目时，目录结构应该怎么设计？各模块职责是什么？](#q137)
- [Q138. .env.example 和 .gitignore 应该怎么写？为什么不能提交 .env 文件？](#q138)
- [Q139. 一个可展示的 LLM 项目 README 需要包含哪些章节？快速启动指南有哪些常见疏漏？](#q139)
- [Q140. 如何从零搭建一个"可调用工具的个人助理"完整项目？描述模块划分和关键实现。](#q140)

**添加简单界面**
- [Q131. Streamlit 和 Gradio 分别适合什么场景？LLM Demo 应该选哪个？](#q131)
- [Q132. gr.Interface 和 gr.ChatInterface 有什么区别？各适用于什么场景？](#q132)
- [Q133. 如何在 Gradio 界面里实现流式输出？和非流式有什么体验差异？](#q133)
- [Q134. gr.State 是什么？为什么多轮对话需要它？不用会发生什么？](#q134)
- [Q135. 如何从零搭建一个展示工具调用链的 LLM Demo 界面？描述关键设计决策。](#q135)

**多步工作流**
- [Q126. 单步工具调用和多步工作流在结构上有什么本质区别？](#q126)
- [Q127. 多步工作流中的循环应该什么时候退出？有哪些终止条件？](#q127)
- [Q128. 为什么多步工作流必须设置 max_iterations 上限？不设置会有什么风险？](#q128)
- [Q129. 多步工作流的"可观测性"指什么？如何实现调用链日志？](#q129)
- [Q130. 如何从零实现一个支持多步推理、带调用链记录的工作流 Demo？](#q130)

**单工具调用 Demo**
- [Q121. tool_choice 参数的 auto / none / required 三种模式有什么区别？各适用于什么场景？](#q121)
- [Q122. 模型决定是否调用工具的依据是什么？哪些因素会影响触发稳定性？](#q122)
- [Q123. 工具调用的完整 Demo 应该包含哪些要素？如何添加日志和耗时统计？](#q123)
- [Q124. 多轮对话中为什么不能把 tool_calls 消息从历史里删掉？](#q124)
- [Q125. 如何从零实现一个支持多轮对话、带日志记录的单工具调用完整 Demo？](#q125)

**定义工具**
- [Q116. 工具参数设计有哪些核心原则？必填参数和可选参数如何决策？](#q116)
- [Q117. 工具函数的返回值应该如何设计才对 LLM"友好"？](#q117)
- [Q118. 工具出错时应该返回错误 dict 还是抛异常？两种方式有什么区别？](#q118)
- [Q119. 如何验证工具定义（Function Schema）的质量？有哪些常见的规范问题？](#q119)
- [Q120. 如何从零设计一个可被 LLM 调用的工具库？描述模块结构和关键设计决策。](#q120)

**Tool Calling 基础**
- [Q111. 什么是 Tool Calling？模型"调用"工具的过程中，实际发生了什么？](#q111)
- [Q112. "回答问题"和"调用工具做事"的本质区别是什么？](#q112)
- [Q113. Tool Calling 的完整交互流程是什么？每一步做了什么？](#q113)
- [Q114. 工具定义（Function Schema）中 name、description、parameters 各有什么作用？如何写好 description？](#q114)
- [Q115. 如何从零实现一个最小的 Tool Calling Demo？描述关键代码的设计思路。](#q115)

**第 3 周综合复盘**
- [Q106. 第 3 周（Day 15–20）六个主题之间是什么逻辑关系？RAG 各环节如何相互影响？](#q106)
- [Q107. RAG 准确率主要受哪些环节影响？每个环节出问题时典型表现是什么？](#q107)
- [Q108. Chunk 切分策略为什么会直接影响 RAG 最终回答的质量？影响路径是什么？](#q108)
- [Q109. 为什么引用来源在业务场景中比单纯的答案准确率更重要？](#q109)
- [Q110. 如果让你从零搭建一个"第 3 周成果汇总"完整 RAG Demo，你会如何设计它的架构？](#q110)

---

## 大模型基础

### Q1

**什么是大语言模型（LLM）？它和传统 NLP 模型的核心区别是什么？**

LLM 是基于 Transformer 架构、在海量文本上预训练的语言模型，通过预测下一个 Token 来学习语言规律和世界知识。核心区别在于规模（参数量达数十亿到万亿）带来的"涌现能力"——零样本/少样本即可完成多种任务，而传统 NLP 模型通常针对单一任务训练。

---

### Q2

**大模型的训练分为哪三个阶段？每个阶段解决什么问题？**

| 阶段 | 目标 | 数据 |
|------|------|------|
| 预训练（Pre-training） | 学习语言规律和世界知识 | 海量无标注文本（TB 级） |
| 监督微调（SFT） | 学会遵循指令、进行对话 | 高质量指令-回复对 |
| 人类反馈强化学习（RLHF） | 输出更安全、更符合人类偏好 | 人类对比排序数据 |

预训练产出 Base Model，只能续写文本；SFT 后变成 Chat Model，能对话；RLHF 进一步对齐人类价值观。

---

### Q3

**Transformer 的核心机制是什么？为什么它比 RNN 更适合大模型？**

核心是**自注意力机制（Self-Attention）**，让每个 Token 同时"关注"序列中所有其他 Token，从而捕捉长距离依赖关系。相比 RNN 的顺序计算，Transformer 可并行计算，训练效率更高；同时不存在 RNN 的梯度消失问题，更容易扩展到超大规模。

---

### Q4

**Temperature 参数的作用是什么？0、0.7、1.5 分别适合哪些场景？**

Temperature 控制输出的随机性，通过缩放 Softmax 前的 logits 来调整概率分布的"尖锐程度"。

- **Temperature = 0**：接近确定性输出，适合代码生成、事实问答、格式化抽取
- **Temperature = 0.7**：平衡创造性和准确性，适合日常对话、写作辅助
- **Temperature ≥ 1.5**：高度随机，适合创意写作、头脑风暴

---

### Q5

**大模型有哪些典型局限性？各自的解决方案是什么？**

| 局限 | 解决方案 |
|------|---------|
| 知识截止（不知训练后的事） | RAG + 联网搜索 |
| 幻觉（编造事实） | RAG、要求引用来源、事实核查 |
| 无实时信息 | Tool Calling 调用外部 API |
| 数学/复杂逻辑推理弱 | 推理模型（o1/DeepSeek-R1）、代码执行 |
| 上下文窗口有限 | 文档切分、滑动窗口、摘要压缩 |

---

### Q6

**闭源模型和开源模型如何选择？**

- **闭源**（GPT-4o、Claude）：效果最强，开箱即用，但成本高、数据不出境
- **开源**（Qwen、Llama、DeepSeek）：可本地部署、可微调、数据隐私有保障，但需要运维成本

中文应用优先考虑 Qwen / DeepSeek；对数据安全要求高的企业场景考虑本地部署开源模型；学习/Demo 阶段用 DeepSeek 或 GPT-4o-mini 成本最低。

---

## 核心概念：Prompt / Token / 上下文窗口

### Q7

**什么是 Prompt？一个好的 Prompt 应该包含哪些要素？**

Prompt 是发送给模型的文本指令，是人与模型交互的核心方式。好的 Prompt 通常包含：

1. **角色设定**：`你是一位资深 Python 工程师`
2. **任务描述**：明确告诉模型要做什么
3. **上下文/输入数据**：提供必要背景
4. **输出约束**：格式、长度、语言风格
5. **示例（Few-shot）**：复杂任务时给出输入-输出对

模糊的 Prompt（`帮我总结`）和清晰的 Prompt（`用 3 句话提炼以下文章的核心观点，保留关键数据`）效果差距显著。

---

### Q8

**什么是 Token？中英文的 Token 换算规则大概是怎样的？**

Token 是模型处理文本的最小单位，由 Tokenizer 将文本切分而来，不等同于字或词。换算规则：

- 英文：约 1 Token = 0.75 个单词（`Hello` = 1 Token）
- 中文：约 1 Token = 1–2 个汉字

Token 是**计费单位**（按输入/输出 Token 分别计价），也决定**上下文窗口是否超限**。

---

### Q9

**上下文窗口是什么？超出限制后会发生什么？有哪些处理策略？**

上下文窗口是模型单次请求能处理的最大 Token 数，包含 system prompt + 历史对话 + 当前输入 + 输出。超出限制后，早期消息会被截断，导致模型"忘记"早期信息。

处理策略：

| 策略 | 做法 | 适用场景 |
|------|------|---------|
| 滑动窗口 | 只保留最近 N 轮对话 | 简单对话 |
| 摘要压缩 | 对早期历史生成摘要，注入 system | 需要保留关键信息 |
| 向量检索 | 历史存向量库，按需检索 | 超长会话、知识库问答 |

截断时**必须保留 system 消息**（角色/规则设定）。

---

## 核心概念：Embedding / RAG / Tool Calling

### Q10

**什么是 Embedding？它和关键词搜索的本质区别是什么？**

Embedding 是将文本转换为高维向量的过程，使语义相近的文本在向量空间中距离更近。

- **关键词搜索**：只能匹配字面相同的词，搜索"苹果手机"找不到"iPhone"
- **语义搜索（Embedding）**：能匹配语义相近的内容，"苹果手机"可以匹配"iPhone 15 Pro Max"

相似度通常用余弦相似度计算，结果在 [-1, 1]，1 表示完全相同方向。

---

### Q11

**RAG 是什么？解决了大模型的哪些问题？请描述完整流程。**

RAG（Retrieval-Augmented Generation，检索增强生成）解决了：知识截止、不知企业私有知识、幻觉、上下文窗口有限。

**完整流程：**

```
【离线构建】原始文档 → 切分（Chunk）→ Embedding 向量化 → 存入向量数据库

【在线问答】用户问题 → Embedding → 向量相似度检索 → Top-K 片段
          → 构建 Prompt（问题 + 检索片段）→ 模型回答
```

文档切分参数：Chunk Size 推荐 200–500 Token，Chunk Overlap 约 10–20%，防止关键信息被截断。

---

### Q12

**Tool Calling 是什么？模型是如何"调用"工具的？**

Tool Calling 让模型能够请求调用外部工具（天气 API、数据库、代码执行等），从"只会说话"变成"能做事"。

模型本身**不直接执行**工具，而是返回一个调用意图：

```json
{"tool_name": "get_weather", "arguments": {"city": "北京"}}
```

应用层收到后执行真正的工具调用，再把结果返回给模型，模型据此生成最终回答。

---

### Q13

**Tool Calling 和 Agent 有什么关系？**

- **Tool Calling**：单次工具调用能力，模型决定调用哪个工具、传什么参数
- **Agent**：Tool Calling + 规划能力 + 多步推理，能自主拆解任务、循环调用多个工具直到完成目标

Agent 是以 Tool Calling 为基础、加入"思考-行动"循环后形成的更高级模式。

---

## Python 基础

### Q14

**为什么大模型开发项目必须使用虚拟环境？**

不同项目依赖的包版本可能冲突（如项目 A 需要 `openai==0.28`，项目 B 需要 `openai==1.x`）。虚拟环境为每个项目提供独立的 Python 解释器和包空间，互不影响，也便于用 `requirements.txt` 精确复现环境。

---

### Q15

**在大模型应用中，列表（List）和字典（Dict）最常见的使用场景是什么？**

- **列表**：存储消息历史，每轮追加新消息
- **字典**：单条消息本身、API 响应体（嵌套字典）、配置项

常用操作：

```python
messages.append({"role": "assistant", "content": reply})   # 追加消息
[m for m in messages if m["role"] != "system"]              # 过滤角色
response.get("usage", {}).get("total_tokens", 0)            # 安全访问嵌套字典
```

---

### Q16

**调用外部 API 时，异常处理应该捕获哪些错误？**

```python
try:
    response = requests.post(url, headers=headers, json=data, timeout=30)
    response.raise_for_status()           # 4xx/5xx 自动抛出
except requests.exceptions.Timeout:
    pass  # 超时：重试或提示用户
except requests.exceptions.ConnectionError:
    pass  # 网络不通：检查代理/网络
except requests.exceptions.HTTPError as e:
    pass  # HTTP 错误：根据 status_code 细分处理（401/429/500）
except Exception as e:
    pass  # 兜底，至少记录日志
```

不要裸 `except:` 或 `except Exception: pass`，至少记录日志。

---

## API 调用

### Q17

**API Key 为什么不能硬编码在代码中？正确的管理方式是什么？**

硬编码的 API Key 一旦代码提交到 Git（尤其是公开仓库），就会泄露，导致被盗用、产生高额费用。

正确做法：
1. 写入 `.env` 文件：`API_KEY=sk-xxxx`
2. 用 `python-dotenv` 读取：`os.getenv("API_KEY")`
3. `.gitignore` 中添加 `.env`，确保不被提交

---

### Q18

**HTTP 状态码 401、404、429 分别代表什么？各如何处理？**

| 状态码 | 含义 | 处理方式 |
|--------|------|---------|
| 401 | 未授权（API Key 无效/过期） | 检查 Key 是否正确、是否已过期 |
| 404 | 资源不存在 | 检查 URL 路径、**模型名称是否正确**（常见坑） |
| 429 | 请求频率超限 | 降低请求频率，实现指数退避重试 |

---

### Q19

**使用 OpenAI SDK 调用国内兼容接口（如 DashScope）时需要注意什么？**

需要设置 `base_url` 指向国内端点，模型名称必须使用该平台支持的名称：

```python
from openai import OpenAI
import os

client = OpenAI(
    api_key=os.getenv("API_KEY"),
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
)
response = client.chat.completions.create(
    model="qwen-turbo",   # 必须是平台支持的模型名，不能随便写
    messages=[{"role": "user", "content": "你好"}]
)
```

常见错误：模型名写错（如 `qwen-flash` 不存在）会返回 **404**。

---

### Q20

**流式输出（Streaming）的原理是什么？代码上如何实现？**

服务端使用 SSE（Server-Sent Events）逐块推送生成的 Token，客户端边接收边渲染，用户感知延迟更低。

```python
response = client.chat.completions.create(
    model=MODEL, messages=messages, stream=True
)

full_reply = ""
for chunk in response:
    content = chunk.choices[0].delta.content
    if content:
        full_reply += content
        print(content, end="", flush=True)

# 收完所有 chunk 后，才追加到消息历史
messages.append({"role": "assistant", "content": full_reply})
```

---

## 多轮对话

### Q21

**为什么每次调用 API 都要传完整的消息历史？**

大模型是**无状态**的，每次请求相互独立，模型不会记忆上一次的对话。上下文完全依赖请求体中的 `messages` 列表传递。如果不携带历史，模型无法回答"我叫什么名字"这类需要上文信息的问题。

---

### Q22

**多轮对话中消息历史的维护规则是什么？**

```
每轮对话：
  1. 将用户输入 append 到 messages（role: "user"）
  2. 携带完整 messages 调用 API
  3. 将模型回复 append 到 messages（role: "assistant"）← 不能遗漏
  4. 下一轮继续 1–3
```

system 消息只在初始化时添加一次，放在列表最前面，截断历史时不能丢弃。

---

### Q23

**如何实现消息历史的截断，同时保证对话质量？**

```python
def truncate_messages(messages: list, max_turns: int = 10) -> list:
    system = [m for m in messages if m["role"] == "system"]
    others = [m for m in messages if m["role"] != "system"]
    # 保留最近 max_turns 轮（每轮 = user + assistant = 2 条）
    return system + others[-(max_turns * 2):]
```

进阶策略：对早期消息调用 LLM 生成摘要，作为新的 system 消息注入，既节省 Token 又保留关键信息。

---

### Q24

**system、user、assistant 三种角色各有什么典型用法？**

```python
messages = [
    # system：设定角色/规则/输出格式，全程生效
    {"role": "system", "content": "你是一个专业的 Python 工程师，回答要附带代码示例。"},

    # user：用户输入，每轮一条
    {"role": "user", "content": "什么是装饰器？"},

    # assistant：模型上一轮的回复，必须保存以维持上下文
    {"role": "assistant", "content": "装饰器是一种语法糖，用于在不修改原函数的前提下..."},

    # 下一轮用户输入
    {"role": "user", "content": "能给个实际案例吗？"},
]
```

---

### Q25

**如果让你从零设计一个命令行聊天程序，核心要考虑哪些模块？**

1. **配置层**：从 `.env` 读取 API Key、模型名、base_url
2. **消息历史管理**：列表维护、system 消息初始化、截断策略
3. **API 调用层**：支持流式/非流式，统一错误处理和重试
4. **CLI 交互层**：输入循环、特殊命令（`/clear` 清空历史、`/quit` 退出、`/history` 查看）
5. **统计**：Token 用量、响应耗时

核心数据流：`用户输入 → append to messages → API 调用 → 回复 append to messages → 输出`

---

## Prompt 工程与摘要任务

### Q26

**为什么说"总结这篇文章"是一个糟糕的 Prompt？**

因为没有任何约束，模型输出完全不可预期：可能输出 50 字也可能输出 2000 字；可能用列表也可能用长段落；可能追加背景介绍和主观评价；不同运行结果差异很大，无法稳定集成进产品。

好的 Prompt 至少需要约束四件事：**输出多少（长度）、输出什么结构（格式）、来源哪里（内容范围）、用什么语气（风格）**。

---

### Q27

**Prompt 约束有哪四个维度？每个维度举一个摘要场景的例子。**

| 维度 | 作用 | 摘要场景示例 |
|------|------|------------|
| 内容范围 | 限定信息来源，防止幻觉 | "只提炼文章中明确陈述的内容，不添加背景或评价" |
| 输出格式 | 控制结构，便于解析和阅读 | "以编号列表输出，共 3 条" / "输出 JSON：{title, summary, keywords}" |
| 长度限制 | 控制篇幅，适配不同场景 | "控制在 100 字以内" / "3 句话" / "不超过原文的 10%" |
| 语气/风格 | 匹配受众和使用场景 | "使用简洁客观的书面语" / "通俗易懂，适合非专业读者" |

四个维度叠加约束，输出才会可预测、可重复。

---

### Q28

**Prompt 层的长度约束和 `max_tokens` 参数有什么区别？应该如何配合使用？**

| 对比项 | Prompt 约束（软约束） | max_tokens（硬截断） |
|--------|---------------------|-------------------|
| 实现方式 | 自然语言描述期望长度 | API 参数限制 Token 上限 |
| 执行方式 | 模型尽量遵守，但可能偏差 | 到达上限后强制停止 |
| 语义完整性 | 通常输出完整句子 | 可能截断在句子中间 |
| 适用场景 | 描述意图，灵活控制 | 防止意外超长，兜底保险 |

**最佳实践**：两者配合使用——Prompt 给方向（"请写一段 100 字左右的摘要"），`max_tokens` 做兜底（设为 300，防止模型意外输出过长内容）。不要只用其中一种。

---

### Q29

**temperature 参数在摘要任务中应该设多少？为什么？**

摘要任务推荐 `temperature = 0.0 ~ 0.3`，原因：

- 摘要要求**准确提炼原文**，不需要创造性发挥
- 低 temperature 输出更确定、更稳定，同一输入多次运行结果一致
- 高 temperature 会引入随机性，摘要可能包含原文没有的内容，或忽略关键信息

对比：创意写作适合 0.7–1.0；代码生成/事实问答/摘要/抽取适合 0.0–0.3。

---

### Q30

**如何评估一个摘要 Prompt 写得好不好？**

从以下四个角度判断：

1. **稳定性**：对同一篇文章多次运行，输出是否一致（结构、长度、侧重点）
2. **忠实度**：摘要内容是否都来自原文，有无编造或添加额外信息
3. **覆盖度**：是否涵盖了文章的核心观点，有无遗漏关键信息
4. **可用性**：输出格式是否符合预期，是否便于直接集成（比如可以直接解析的 JSON）

**实用做法**：准备 3–5 篇测试文章，用不同版本的 Prompt 各跑一遍，对比输出，看哪个版本在四个维度上更稳定。这就是 Day 5 的核心实践任务。

---

## 结构化信息抽取

### Q31

**信息抽取和文章摘要的核心区别是什么？**

| 对比项 | 文章摘要 | 信息抽取 |
|--------|---------|---------|
| 输出格式 | 自然语言文本 | 结构化 JSON |
| 下游消费 | 人直接阅读 | 程序解析 / 数据库入库 |
| 对原文忠实度 | 允许语义整合和改写 | 字段值必须来自原文，不得推断 |
| 评估标准 | 语义准确、覆盖度 | 字段完整、类型正确、无推断 |
| temperature | 0.1–0.3 | 0.0 |

摘要是"对内容的压缩"，抽取是"对指定字段的查找和复制"。字段找不到时应返回 null，而不是推测一个合理值。

---

### Q32

**为什么要求模型只输出 JSON？有哪些方法可以强制 JSON 格式？**

因为抽取结果要被程序解析（`json.loads()`），任何格式污染（说明文字、markdown 代码块、额外注释）都会导致解析失败。

强制 JSON 的两种方法：

**方法一：Prompt 约束**
```
直接输出 JSON 对象，从 { 开始，以 } 结尾，不要 markdown 代码块，不要任何说明文字。
```

**方法二：API 参数**
```python
response = client.chat.completions.create(
    model="deepseek-chat",
    messages=messages,
    response_format={"type": "json_object"},  # 强制 JSON 模式
    temperature=0.0
)
```

**注意**：使用 `response_format={"type": "json_object"}` 时，Prompt 中必须出现 "JSON" 字样，否则 API 报错。两种方法配合使用效果最佳。

---

### Q33

**信息抽取 Prompt 的字段定义应该包含哪些要素？**

一个完整的字段定义应包含：**字段名、数据类型、示例值、缺失时的处理**。

```
- name: 候选人姓名（字符串，例如：张伟）
- age: 年龄（整数，例如：25，找不到则 null）
- skills: 技能列表（字符串数组，即使只有一个也用数组，例如：["Python"]）
- price: 价格（整数，单位：元，只写数字不含货币符号，找不到则 null）
```

要素缺失的后果：

| 缺失要素 | 常见问题 |
|---------|---------|
| 类型说明 | `age` 返回 "26岁"（字符串）而不是 26（整数）|
| 示例值 | `skills` 返回逗号分隔字符串，而不是数组 |
| 空值规则 | 找不到时模型不输出该字段，程序 KeyError |

---

### Q34

**抽取任务中常见的失败模式有哪些？各自如何解决？**

| 失败类型 | 现象 | 解决方案 |
|---------|------|---------|
| 格式污染 | 输出包含 \`\`\`json ... \`\`\` 或说明文字 | 使用 `response_format={"type": "json_object"}` + Prompt 明确禁止 |
| 字段推断 | 原文无年龄，但模型从毕业年份推算出来 | Prompt 强调"只提取原文明确出现的信息" |
| 类型错误 | `price` 应为整数，模型返回 "7999元"（字符串）| 字段描述中加类型示例："整数，只写数字" |
| 字段遗漏 | 找不到的字段不输出 key，程序 KeyError | 提取后遍历预期字段，缺失的补 null |
| 列表/单值混淆 | `skills` 只有一个时返回字符串而非数组 | 描述中强调："即使只有一个也用数组" |

---

### Q35

**如何设计一个可复用的通用信息抽取器？从零描述你的思路。**

核心思路：将"字段定义"和"Prompt 构建"解耦，让字段可以按场景动态传入。

```python
class InfoExtractor:
    def __init__(self):
        self.client = OpenAI(
            api_key=os.getenv("DEEPSEEK_API_KEY"),
            base_url="https://api.deepseek.com/v1"
        )

    def extract(self, text: str, fields: dict) -> dict:
        # 1. 动态构建 Prompt
        field_lines = "\n".join(f"- {k}: {v}" for k, v in fields.items())
        prompt = f"从以下文本中抽取信息，以 JSON 格式输出：\n\n{field_lines}\n\n规则：只提取原文信息，找不到返回 null，只输出 JSON。\n\n文本：\n{text}"

        # 2. 调用 API，强制 JSON 模式，temperature=0
        response = self.client.chat.completions.create(
            model="deepseek-chat",
            messages=[{"role": "user", "content": prompt}],
            temperature=0.0,
            response_format={"type": "json_object"}
        )

        # 3. 解析 + 补全缺失字段
        data = json.loads(response.choices[0].message.content)
        for key in fields:
            if key not in data:
                data[key] = None
        return data
```

设计要点：
1. **字段参数化**：`fields: dict` 传入，调用方决定场景，提取器不感知业务
2. **强制 JSON**：API 参数 + Prompt 双重约束
3. **temperature=0**：抽取任务不需要随机性
4. **缺失字段补全**：确保所有预期 key 都存在，下游不会 KeyError

---

## 第 1 周综合复盘

### Q36

**大模型 API 的计费逻辑是什么？哪些地方最容易产生不必要的 Token 消耗？**

API 费用按 Token 计算，而非按请求次数：

```
费用 = 输入 Token 数 × 输入单价 + 输出 Token 数 × 输出单价
```

输出 Token 通常比输入贵（DeepSeek 约贵 2 倍），因为生成每个 Token 需要完整的前向推理。

**最容易浪费的四个来源：**

| 来源 | 典型问题 | 优化做法 |
|------|---------|---------|
| system prompt | 每次请求都重复计费，越长越贵 | 精简为关键规则，去掉冗余解释 |
| 消息历史 | 多轮对话后期历史积累，可能占输入 80% | 定期截断或摘要压缩 |
| 输入文档 | 塞入整篇文章不预处理 | 去空行/去无关段、先用 RAG 检索 |
| 输出长度 | 没设 max_tokens，模型"自由发挥" | 软约束（Prompt）+ 硬截断（max_tokens）配合 |

一次典型的摘要请求（500 Token 输入 + 200 Token 输出）成本约 < 0.001 元；但对话 100 轮后历史积累到 5000 Token，每轮成本增长 10 倍。

---

### Q37

**Temperature 参数在数学上是如何影响模型输出的？**

模型每步生成前，先为词表中每个 Token 打出原始分数（logits），再用 Softmax 转为概率：

```
P(token_i) = exp(logit_i / T) / Σ exp(logit_j / T)
```

Temperature T 通过**缩放 logits** 改变概率分布的"尖锐程度"：

| T 值 | 效果 | 数学直觉 |
|------|------|---------|
| T → 0 | 概率集中在最高分 Token | 分布变为 argmax（one-hot） |
| T = 1 | 使用原始 Softmax 概率 | 不做任何缩放 |
| T → ∞ | 所有 Token 概率趋于相等 | 分布变为均匀分布（完全随机） |

实际工程结论：
- `T=0.0`：同一输入多次运行结果相同（确定性），适合抽取/代码生成
- `T=0.7`：结果多样但仍合理，适合对话/写作辅助
- `T≥1.5`：高风险高多样，输出可能离题，适合创意头脑风暴且需人工筛选

注意：Temperature 控制的是**确定性**，不是**质量**。低 Temperature 不等于更聪明，只是更稳定。

---

### Q38

**大模型 API 为什么设计成无状态的？这对应用开发有什么影响？**

**为什么无状态：**

| 动机 | 说明 |
|------|------|
| 水平扩展 | 无状态服务可随意增减机器，任意实例处理任意请求 |
| 精准计费 | 按 Token 计费，服务方无需维护每个用户的会话状态 |
| 灵活控制 | 应用层可自由裁剪/摘要历史，不被 API 绑架 |
| 故障隔离 | 单次请求失败不影响其他请求，重试即可 |

**对应用开发的影响：**

1. **应用层必须维护对话历史**：每次请求都要传完整 `messages` 列表
2. **历史消息是成本的主要来源**：需要主动管理截断策略
3. **无法依赖"服务端记住了"**：换实例、重启服务都不影响，因为状态在客户端
4. **流式输出也是无状态的**：每个 chunk 是同一次请求的分片，不是独立请求

```python
# 错误做法：以为模型记得上一轮
client.chat("我叫张伟")
client.chat("我叫什么名字？")   # ← 第二次请求是全新的，模型不知道"张伟"

# 正确做法：每次传完整历史
messages = [
    {"role": "user", "content": "我叫张伟"},
    {"role": "assistant", "content": "你好，张伟！"},
    {"role": "user", "content": "我叫什么名字？"},
]
client.chat.completions.create(model=..., messages=messages)
```

---

### Q39

**聊天 / 摘要 / 信息抽取三个 Demo 在设计上有哪些关键差异？**

| 维度 | CLI 聊天 | 文章摘要器 | 信息抽取工具 |
|------|---------|-----------|------------|
| 消息历史 | **必须维护**（多轮依赖历史） | 不需要（单次任务） | 不需要（单次任务） |
| temperature | 0.7（平衡多样与准确） | 0.1（稳定但允许少量措辞变化） | 0.0（完全确定性） |
| 输出格式 | 自然语言（人读） | 自然语言（人读） | **结构化 JSON（程序消费）** |
| response_format | 不用 | 不用 | `json_object`（必须） |
| max_tokens | 不设（对话长短不定） | 300–500（兜底防超长） | 不设（字段数量固定） |
| 核心 Prompt 技术 | system 角色设定 | 四维度约束 | 字段类型定义 + 空值规则 |
| 主要风险 | Token 超限 | 软约束失效（模型不遵守长度） | 格式污染 / 字段推断 |

三者是递进关系：聊天 = 基础 API 调用；摘要 = 输出内容约束；抽取 = 输出格式约束。掌握这三种模式，基本覆盖了第一阶段 80% 的应用场景。

---

### Q40

**如果让你从零搭建一个"第 1 周成果汇总"工具包，你会如何设计它的模块结构？**

核心设计原则：**单一职责 + 共享 client + 输入参数化**。

```
week1_toolkit/
├── client.py          # 初始化 OpenAI client（读 .env，单例）
├── chat.py            # ChatSession 类（历史管理 + 截断 + /clear 命令）
├── summarizer.py      # summarize(text, style) 函数（多风格 Prompt 字典）
├── extractor.py       # extract(text, fields) 函数（参数化字段 + JSON 约束）
└── main.py            # 演示入口（调用上面三个模块）
```

```python
# client.py — 共享 client，避免在每个模块重复初始化
import os
from openai import OpenAI
from dotenv import load_dotenv

load_dotenv()

client = OpenAI(
    api_key=os.getenv("DEEPSEEK_API_KEY"),
    base_url="https://api.deepseek.com/v1"
)
```

```python
# chat.py — 只关心历史管理，不关心 client 创建
from client import client

class ChatSession:
    def __init__(self, system: str = "你是有帮助的助手。"):
        self.messages = [{"role": "system", "content": system}]

    def send(self, text: str) -> str:
        self.messages.append({"role": "user", "content": text})
        response = client.chat.completions.create(
            model="deepseek-chat", messages=self.messages, temperature=0.7
        )
        reply = response.choices[0].message.content
        self.messages.append({"role": "assistant", "content": reply})
        return reply
```

这种结构的好处：
1. `client.py` 改 API Key 或 base_url，其他模块零改动
2. 三个功能模块各自独立测试，互不干扰
3. `main.py` 只负责串联演示，不包含业务逻辑
4. 后续 Day 8–14 可以继续向这个工具包里添加新模块

---

## 结构化输出

### Q41

**"能被 json.loads() 解析"和"结构化输出可靠"是一回事吗？三层约束分别解决什么问题？**

不是一回事。`json.loads()` 只能保证这是一段**合法的 JSON 文本**，不能保证字段名对、类型对、必填字段没漏——例如 `{"nmae": "张伟"}` 拼错字段名、`{"age": "28"}` 类型是字符串而非整数，这些都能被 `json.loads()` 正常解析，但对下游代码来说都是"错的"。

| 层级 | 手段 | 解决的问题 |
|------|------|-----------|
| Prompt 层 | 文字描述 / 拼接 JSON Schema | 让模型"知道"应该输出什么结构 |
| API 参数层 | `response_format={"type": "json_object"}` | 保证输出是合法 JSON，减少多余文字 |
| 应用层 | JSON Schema / Pydantic 校验 | 保证字段类型、必填项、无多余字段 |

三层需要叠加使用：API 层只能兜底"合法 JSON"这一个维度，字段级别的正确性必须靠应用层校验才能保证。

---

### Q42

**JSON Schema 中 required 和 additionalProperties 分别解决什么问题？**

- **`required`**：声明哪些字段是必填的。模型如果漏输出这些字段，应用层校验（如 Pydantic）会直接报错，而不是让程序在后续使用 `data["field"]` 时抛 `KeyError`。
- **`additionalProperties: False`**：禁止模型输出 Schema 之外的多余字段。没有这个约束时，模型有时会"自作主张"多加字段（如把抽取理由也写进 JSON），污染下游数据结构。

```python
schema = {
    "type": "object",
    "properties": {"name": {"type": "string"}, "age": {"type": "integer"}},
    "required": ["name", "age"],       # 缺一个都算校验失败
    "additionalProperties": False,     # 多一个字段也算校验失败
}
```

两者共同保证"字段既不多也不少"，这是结构化输出能被下游系统安全消费的前提。

---

### Q43

**为什么推荐用 Pydantic 而不是手写 JSON Schema + 校验代码？**

手写方案需要单独维护一份 JSON Schema（给 Prompt 用）和一份校验逻辑（给代码用），两者容易"两处维护、逐渐失步"。Pydantic 用一个 `class` 同时承担三个职责：

| 职责 | Pydantic 实现方式 |
|------|-------------------|
| 数据结构定义 | class 字段本身就是 Schema |
| 类型校验与转换 | `Model(**data)`，失败自动抛 `ValidationError`，且能把 `"28"` 自动转成 `28` |
| Prompt 约束来源 | `Model.model_json_schema()` 一键导出，无需手写第二份描述 |

```python
from pydantic import BaseModel, Field
from typing import Optional

class ResumeInfo(BaseModel):
    name: str = Field(description="候选人姓名")
    age: Optional[int] = Field(default=None, description="年龄")

schema = ResumeInfo.model_json_schema()   # 直接喂给 Prompt
resume = ResumeInfo(**json.loads(raw))    # 一步完成解析+校验+转换
```

一份定义，三处复用，比"手写 Schema + 手写 if/else 校验"更不容易出现字段定义不一致的问题。

---

### Q44

**抽取任务中枚举字段的值容易跑偏，如何在 Prompt 和代码层面双重兜底？**

**Prompt 层**：在 JSON Schema 里用 `enum` 显式列出所有合法取值，并在提示词中强调"必须是以下枚举值之一，不要输出其他表述"：

```python
{"category": {"type": "string", "enum": ["咨询", "投诉", "建议", "故障"]}}
```

**代码层**：即使加了 `enum`，模型仍可能偶发输出近似但不完全匹配的值（如"投诉建议"），需要在校验失败时做一次兜底归一化，而不是直接判定整条记录失败：

```python
def normalize_enum(value: str, allowed: list[str]) -> Optional[str]:
    if value in allowed:
        return value
    for option in allowed:
        if option in value:      # 退化为包含关系匹配
            return option
    return None                  # 实在无法归一化，交给人工复核
```

两层兜底的关系：Prompt 层的 `enum` 降低跑偏概率，代码层的归一化处理"降低概率之后仍然存在的长尾情况"，两者配合才能把枚举命中率提升到生产可用水平。

---

### Q45

**如何从零设计一个支持"批量抽取 + 类型安全 + 失败重试"的结构化抽取器？**

核心设计原则：**Schema 与 Prompt 自动同步 + 校验失败可重试 + 失败记录不阻塞整体流程**。

```
structured_extractor/
├── client.py       # 共享 OpenAI client
├── schemas.py      # 各业务场景的 Pydantic 模型（ResumeInfo、FeedbackInfo…）
├── extractor.py    # build_prompt / extract_structured / batch_extract
└── main.py         # 演示入口
```

```python
# extractor.py 核心逻辑
def build_prompt(text: str, model_cls: type[BaseModel]) -> str:
    schema = json.dumps(model_cls.model_json_schema(), ensure_ascii=False)
    return f"严格按照该 JSON Schema 抽取信息：\n{schema}\n\n只输出 JSON。\n\n文本：\n{text}"

def extract_structured(text: str, model_cls: type[BaseModel], max_retries: int = 2) -> BaseModel:
    prompt = build_prompt(text, model_cls)
    last_error = None
    for _ in range(max_retries):
        raw = call_llm(prompt)  # temperature=0 + response_format=json_object
        try:
            return model_cls(**json.loads(raw))
        except (json.JSONDecodeError, ValidationError) as e:
            last_error = e
    raise RuntimeError(f"抽取失败：{last_error}")

def batch_extract(texts: list[str], model_cls: type[BaseModel]) -> dict:
    results, failures = [], []
    for i, text in enumerate(texts):
        try:
            results.append(extract_structured(text, model_cls))
        except RuntimeError as e:
            failures.append({"index": i, "error": str(e)})
    return {"success": results, "failed": failures}   # 失败记录单独落盘，不阻塞整批
```

这种设计的好处：
1. `schemas.py` 新增一个业务场景只需定义一个 Pydantic 模型，`extractor.py` 零改动即可复用
2. Prompt 约束始终和代码里的类型定义保持一致，不存在"文档描述和代码不符"的问题
3. `batch_extract` 把"部分记录失败"和"整体任务失败"解耦，失败记录可以离线人工复核或二次重试

---

## 输出校验与异常处理

### Q46

**大模型调用链路中常见的异常有哪些？如何区分可重试与不可重试？**

一次抽取请求要经过"网络 → HTTP 状态码 → JSON 解析 → 字段校验"多个环节，每个环节都有特有的失败方式：

| 异常/状态码 | 层面 | 是否可重试 |
|------------|------|----------|
| `ConnectionError` / `Timeout` | 网络层 | 是 |
| 429（限流） | HTTP 层 | 是（需退避） |
| 500/502/503（服务端错误） | HTTP 层 | 是 |
| 401/403（Key 无效/无权限） | HTTP 层（配置错误） | **否**，重试无法自愈，应立即终止并告警 |
| 404（模型名错误） | HTTP 层（配置错误） | **否**，属于配置问题 |
| `json.JSONDecodeError` | 解析层 | 是，换一次生成大概率能拿到合法 JSON |
| `pydantic.ValidationError` | 校验层 | 视情况重试 1-2 次，仍失败则转业务兜底 |

判断标准：**这次失败是偶发的（网络抖动、模型这次没输出好）还是确定性的（配置错了、权限不对）**。偶发性错误值得重试，确定性错误重试只会浪费时间和 Token，还会掩盖真正的问题。

---

### Q47

**立即重试和指数退避有什么区别？为什么批量任务更适合指数退避？**

- **立即重试**：失败后 0 秒重试。如果失败原因是限流（429），立刻重试等于继续触发限流，大批量任务并发时会形成"重试风暴"，让限流问题更严重。
- **指数退避**：等待时间随失败次数指数增长（`delay = base_delay * 2^attempt + 随机抖动`），给服务端恢复的时间，也降低多个客户端同一时刻集中重试的概率。

```python
delay = base_delay * (2 ** attempt) + random.uniform(0, 1)
# attempt=0 → 约 1s，attempt=1 → 约 2s，attempt=2 → 约 4s ...
```

批量任务同时处理成百上千条数据时，如果每条都在同一时刻失败重试，指数退避 + 随机抖动能有效分散重试请求，避免"重试风暴"把限流问题放大成雪崩。

---

### Q48

**json.JSONDecodeError 和 pydantic.ValidationError 分别代表什么层面的失败？处理策略有何不同？**

| 异常类型 | 失败层面 | 典型场景 | 处理策略 |
|---------|---------|---------|---------|
| `json.JSONDecodeError` | 格式层面 | 模型输出被截断、掺杂非 JSON 字符 | 值得重试，重新生成一次大概率能拿到完整合法的 JSON |
| `pydantic.ValidationError` | 语义层面 | 字段名拼错、类型不匹配、必填项缺失 | 视错误类型而定：`missing`（缺字段）和类型不匹配值得重试 1-2 次，仍失败则不再无脑重试，转入业务兜底（标记待复核或丢弃） |

关键区别：前者是"这段文本根本不是合法 JSON"，后者是"JSON 合法，但内容不符合预期结构"。两者都应该被统一包装成结构化的返回值（而不是让异常直接向上抛），调用方只需要判断 `ok` 字段，不需要关心具体抛了哪种异常。

---

### Q49

**抽取结果字段缺失时，如何设计兜底策略而不是简单地"报错"或"忽略"？**

核心原则：**不是所有缺失都一样严重，应该按字段重要性分级处理**，而不是用同一种策略应对所有缺失字段。

| 策略 | 做法 | 适用场景 |
|------|------|---------|
| 默认值填充 | 缺失字段填 `null`/空字符串/`0` | 该字段本来就允许为空 |
| 标记待人工复核 | 记录到 `needs_review`，附带缺失原因 | 缺失的是重要字段，需要人工确认 |
| 直接丢弃记录 | 该条数据完全跳过 | 缺失的是必填字段，记录不可用 |

按字段等级（`REQUIRED` / `IMPORTANT` / `OPTIONAL`）判断记录最终该归入 `success`、`needs_review` 还是 `failed` 三态之一：

```python
def assess_completeness(data: dict, field_levels: dict) -> str:
    missing_required = [k for k, lv in field_levels.items()
                         if lv == FieldLevel.REQUIRED and not data.get(k)]
    missing_important = [k for k, lv in field_levels.items()
                          if lv == FieldLevel.IMPORTANT and not data.get(k)]
    if missing_required:
        return "failed"
    if missing_important:
        return "needs_review"
    return "success"
```

这样批量任务的"成功率"不再是非黑即白，能清楚回答"有多少可以直接用、多少需要人工看一眼、多少彻底失败"。

---

### Q50

**如何从零设计一个不会因单条脏数据崩溃的健壮批量抽取器？**

核心设计原则：**分层捕获异常 + 按错误类型决定重试策略 + 结果三态化，让单条数据的失败不影响整批任务**。

```python
def call_llm_with_backoff(prompt, max_retries=3, base_delay=1.0):
    for attempt in range(max_retries):
        try:
            return client.chat.completions.create(...).choices[0].message.content
        except Exception as e:
            status_code = getattr(e, "status_code", None)
            if status_code in (401, 403, 404):
                raise NonRetryableError(f"不可恢复错误（{status_code}）：{e}")
            if attempt == max_retries - 1:
                raise
            time.sleep(base_delay * (2 ** attempt) + random.uniform(0, 1))

def parse_and_validate(raw, model_cls):
    try:
        data = json.loads(raw)
    except json.JSONDecodeError as e:
        return None, f"json_error: {e}"
    try:
        return model_cls(**data), None
    except ValidationError as e:
        return None, f"validation_error: {e.errors()}"

def extract_robust(text, model_cls, field_levels, build_prompt_fn, max_parse_retries=2):
    prompt = build_prompt_fn(text, model_cls)
    for attempt in range(max_parse_retries):
        try:
            raw = call_llm_with_backoff(prompt)
        except NonRetryableError as e:
            return ExtractionRecord(status="failed", reason=str(e))
        obj, error = parse_and_validate(raw, model_cls)
        if obj is not None:
            status, reason = assess_completeness(obj.model_dump(), field_levels)
            return ExtractionRecord(status=status, data=obj.model_dump(), reason=reason)
    return ExtractionRecord(status="failed", reason=f"重试耗尽：{error}")

def batch_extract_robust(texts, model_cls, field_levels, build_prompt_fn):
    buckets = {"success": [], "needs_review": [], "failed": []}
    for i, text in enumerate(texts):
        record = extract_robust(text, model_cls, field_levels, build_prompt_fn)
        buckets[record.status].append({"index": i, "text": text, "record": record})
    return buckets   # 单条失败只进入 failed 桶，不会中断循环
```

设计要点：
1. `call_llm_with_backoff` 只负责网络层重试，遇到 401/403/404 这类不可恢复错误立刻抛出，不浪费重试次数
2. `parse_and_validate` 把 JSON 解析和 Pydantic 校验两类异常收敛为统一的 `(obj, error)` 返回值，避免多层嵌套 `try/except`
3. `assess_completeness` 把"数据本身不完整"和"程序出错"分开处理，避免把正常的业务缺失当成异常上报
4. `batch_extract_robust` 用 `try/except` 包住每条数据的处理，配合三态分桶，保证 1000 条里有几条脏数据也不会拖垮整个批次

---

## 文本分类

### Q51

**分类任务和信息抽取任务在 Prompt 设计上有什么本质区别？**

核心区别在于**答案空间**：抽取任务的答案来自原文、半开放（找不到就是 null）；分类任务的答案空间**有限且预先确定**，模型必须从一个固定的标签集合里选一个，没有"部分正确"的中间状态。

| 对比维度 | 抽取 | 分类 |
|---------|------|------|
| 答案空间 | 半开放（原文中的值） | 有限且预定义 |
| Prompt 核心内容 | 描述"字段"（字段名、类型、示例） | 描述"标签集合"（每个标签的定义 + 边界示例） |
| 核心约束手段 | JSON Schema + Pydantic | `enum` |
| 评估方式 | 字段级别对错 | 准确率、混淆矩阵（客观可量化） |

因此分类任务的 Prompt 不需要像抽取那样描述字段类型，而要把精力放在**把每个标签的定义写清楚**——标签名对人类含义明确，但对模型来说只是一个字符串，必须靠清晰定义 + 边界案例示例才能让模型判断标准和人工标注对齐。

---

### Q52

**一个好的标签集合应该满足哪些设计原则？**

| 原则 | 说明 |
|------|------|
| 互斥（Mutually Exclusive） | 每条数据理想情况下只应明确属于一个类别，类别定义不应相互重叠 |
| 完备（Collectively Exhaustive） | 标签集合要覆盖绝大多数真实数据，预留"其他"兜底类别，不强迫模型硬选 |
| 定义清晰 | 每个标签配一句话定义 + 1-2 个边界示例，而不是只给一个词 |
| 数量适中 | 通常 3-15 类；类别太多时模型在长列表中选择的准确率会明显下降，可考虑先分大类再细分（两级分类） |

其中"完备性"最容易被忽视：如果标签集合里没有"其他"这样的兜底选项，模型遇到模糊或不相关的数据时会被迫"矬子里拔将军"，硬选一个不合适的标签，污染统计结果。

---

### Q53

**为什么用 enum 而不是普通字符串字段约束分类标签？单标签和多标签分类的实现有什么不同？**

用 Python 原生 `Enum` 类型（配合 Pydantic）而不是普通字符串 + 手写 `if value not in [...]` 校验，好处是**类型系统本身就是校验规则**：

```python
class FeedbackCategory(str, Enum):
    CONSULT = "咨询"
    COMPLAINT = "投诉"
    OTHER = "其他"

class ClassificationResult(BaseModel):
    category: FeedbackCategory   # 值不在枚举范围内，Pydantic 直接抛 ValidationError
```

`model_json_schema()` 会自动把 `Enum` 转成 JSON Schema 的 `enum` 约束，Schema 定义和校验逻辑共用同一份代码，不会出现"文档写了 5 个类别、代码只校验了 4 个"的不一致。

单标签和多标签的实现差异：

```python
# 单标签：一条数据只能属于一个类别
class SingleLabelResult(BaseModel):
    category: FeedbackCategory

# 多标签：一条数据可以同时属于多个类别
class MultiLabelResult(BaseModel):
    categories: list[FeedbackCategory]
```

Prompt 措辞也要相应调整（"选择最符合的一个类别" vs "选择所有符合的类别，可以是多个"），如果 Prompt 里没写清楚是单选还是多选，模型的输出形态（单个字符串 vs 列表）会不稳定。

---

### Q54

**为什么不能盲目相信 LLM 单次调用给出的分类结果？如何用 Self-Consistency 估计置信度？**

传统机器学习分类器的 `predict_proba()` 天然带置信度（如"投诉 0.55，故障 0.40"），但 LLM 单次调用只返回一个确定的字符串 `{"category": "投诉"}`——即使这条数据本身很模糊，模型也会给出一个看起来"言之凿凿"的答案，看不出模型内部其实"很纠结"。

**Self-Consistency** 思路：对同一条数据用较高的 `temperature`（引入随机性）多次调用，统计结果的一致程度作为置信度的近似：

```python
def classify_with_confidence(text: str, n_samples: int = 5) -> dict:
    votes = []
    for _ in range(n_samples):
        response = client.chat.completions.create(
            model="deepseek-chat",
            messages=[{"role": "user", "content": build_classify_prompt(text)}],
            temperature=0.7,   # 需要一定随机性，否则每次结果相同，失去统计意义
            response_format={"type": "json_object"},
        )
        votes.append(json.loads(response.choices[0].message.content).get("category"))

    counter = Counter(votes)
    top_category, top_count = counter.most_common(1)[0]
    return {"category": top_category, "confidence": top_count / n_samples}
```

置信度低（如 < 0.6）的记录应标记为"待人工复核"而不是直接采信。代价是调用次数乘以 `n_samples`，通常只对少量关键数据或抽样质检使用，不建议全量跑。

---

### Q55

**如何从零设计一个可评估准确率、支持批量运行的文本分类器？从零描述你的思路。**

核心设计原则：**enum 锁定标签集合 + 强类型校验兜底 + 分布统计 + 人工基准评估准确率**。

```python
class FeedbackCategory(str, Enum):
    CONSULT = "咨询"; COMPLAINT = "投诉"; SUGGESTION = "建议"; FAULT = "故障"; OTHER = "其他"

class ClassificationResult(BaseModel):
    category: FeedbackCategory

def classify_structured(text: str, max_retries: int = 2) -> ClassificationResult:
    prompt = build_classify_prompt(text)   # 含每个类别的一句话定义
    for _ in range(max_retries):
        raw = call_llm(prompt, temperature=0.0, response_format=json_object)
        try:
            return ClassificationResult(**json.loads(raw))
        except (json.JSONDecodeError, ValidationError):
            continue
    return ClassificationResult(category=FeedbackCategory.OTHER)   # 重试耗尽兜底为"其他"

def batch_classify(texts: list[str]) -> dict:
    results = [classify_structured(t) for t in texts]
    distribution = Counter(r.category.value for r in results)
    return {"results": list(zip(texts, results)), "distribution": dict(distribution)}

def evaluate_accuracy(texts: list[str], ground_truth: list[str]) -> dict:
    predictions = [classify_structured(t).category.value for t in texts]
    accuracy = sum(p == g for p, g in zip(predictions, ground_truth)) / len(texts)
    confusion = Counter(zip(ground_truth, predictions))   # 定位模型最容易混淆的类别对
    return {"accuracy": accuracy, "confusion": dict(confusion)}
```

设计要点：
1. `Enum` + Pydantic 让"标签值是否合法"这件事由类型系统保证，不需要额外写归一化逻辑
2. `classify_structured` 复用 Day 9 的重试思路，重试耗尽后兜底为"其他"而不是让程序崩溃
3. `batch_classify` 的类别分布统计是分类任务特有的产出，能直接回答"这批数据里投诉占比多少"这类业务问题
4. `evaluate_accuracy` 用小样本人工标注做基准，混淆矩阵能定位"哪两个类别定义不够清晰"，指导下一轮 Prompt 优化

---

## Prompt 优化

### Q56

**Prompt 优化的四类手段分别解决什么问题？**

| 手段 | 解决的歧义 | 适用场景 |
|------|-----------|---------|
| 角色设定（Role Setting） | 用什么标准/视角判断 | 主观性强、依赖领域经验的任务（如"资深客服主管"判断工单优先级） |
| 目标声明（Explicit Objective） | 要做什么 | 任务本身容易有多种理解方式，需要明确排除"顺便做别的" |
| Few-shot 示例 | 边界案例怎么判断 | 判断规则难以用文字穷举，靠类比而非规则更准确的场景 |
| 输出限制（Output Constraints） | 结果长什么样 | 所有需要程序解析输出的场景（格式、长度、取值范围） |

四者不互斥，一个成熟的生产级 Prompt 通常同时用到全部四种；先写清楚的文字规则覆盖大多数情况，再用 Few-shot 补充规则难以描述的边界案例，是效果和成本的较优平衡点。

---

### Q57

**为什么说 Prompt 优化的本质是"消除歧义"而不是"讨好模型"？**

很多人优化 Prompt 的第一反应是堆砌形容词（"你是全宇宙最专业的专家，请务必认真思考"），但这类"魔法词"效果有限——模型没有"偷懒"这个状态，堆砌形容词不会让它更努力思考。

真正影响输出质量的，是**任务本身有没有被讲清楚**：

```
模糊："请务必认真仔细地分析这条反馈"
清晰："紧急程度判断标准：涉及资金损失或安全问题 = 5，影响使用但有替代方案 = 3，纯咨询 = 1"
```

四类优化手段（角色设定、目标声明、Few-shot、输出限制）本质上都是把原本模糊、依赖常识和经验的判断标准，用文字显式表达出来，让模型有明确依据可以对齐——这是"消除歧义"，而不是"讨好"或"激励"模型。

---

### Q58

**准确率和稳定性是一回事吗？如何分别量化这两个指标？**

不是一回事，两者是独立的评估维度：

| 维度 | 定义 | 衡量方式 |
|------|------|---------|
| 准确率（Accuracy） | 模型输出和人工标注正确答案的一致程度 | 多数投票结果 == expected 的比例 |
| 稳定性（Stability） | 同一条输入多次调用，结果是否一致 | 多次运行结果完全相同的比例 |

一个 Prompt 可能"稳定地错"（每次调用结果一样，但都是错的，说明判断标准本身有偏差）；也可能"偶尔蒙对"（多次结果不一致，但多数投票恰好正确，说明单次调用不可靠）。量化方法：

```python
def evaluate_prompt(prompt_fn, test_set, n_runs=3):
    for item in test_set:
        runs = [call_llm(prompt_fn(item["text"])) for _ in range(n_runs)]
        most_common = max(set(runs), key=runs.count)
        is_stable = len(set(runs)) == 1        # 稳定性：多次结果是否完全一致
        is_correct = most_common == item["expected"]   # 准确率：多数投票是否正确
```

两者按"稳定性 × 准确率"划成四象限：高稳定+高准确是理想状态可直接上线；高稳定+低准确说明判断标准本身有偏差需要重新设计规则；低稳定则无论准确率高低都不可靠，需要提高约束强度。

---

### Q59

**如何判断一次 Prompt "优化"是真的提升了效果，还是恰好在测试集上蒙对了？**

陷阱场景：用几条固定的测试数据反复调整 Prompt，直到这几条全部命中就认为优化成功——但这些数据的措辞、边界情况很可能被"针对性"覆盖了，换一批新数据准确率可能骤降，这叫**对测试集过拟合**。

正确做法：

1. 测试集要有一定规模（至少 15-20 条）且覆盖不同难度
2. 把数据划分为**开发集**（迭代 Prompt 时反复使用）和**留出集**（从不参与调优，只在 Prompt 定稿后跑一次）
3. 对比开发集和留出集的表现：如果留出集准确率明显低于开发集（比如开发集 90%、留出集 60%），说明前面的"优化"很可能只是针对开发集几条数据的措辞做了过度适配，而不是抓住了任务的通用判断标准

```python
DEV_SET = TEST_SET[:5]      # 迭代阶段只看这部分
HOLDOUT_SET = TEST_SET[5:]  # 定稿后只跑一次，代表真实预期表现
```

---

### Q60

**如何从零设计一套可复现的 Prompt 版本对比实验？**

核心设计原则：**递进式版本设计（便于归因）+ 双维度量化（准确率、稳定性）+ 开发集/留出集分离（防止过拟合）**。

```python
def prompt_v1(text):   # 对照组：只有目标声明 + 输出限制
    return f"判断...紧急程度，只输出 JSON：{{\"urgency\": <整数>}}\n\n{text}"

def prompt_v2(text):   # 在 V1 基础上加角色设定 + 文字判断标准
    return "你是...客服主管。判断标准：5分=...\n\n" + prompt_v1(text)

def prompt_v3(text):   # 在 V2 基础上加 Few-shot 示例
    return prompt_v2(text).replace("只输出 JSON", "示例：...\n\n只输出 JSON")

def evaluate_prompt(prompt_fn, test_set, n_runs=3):
    results = []
    for item in test_set:
        runs = [run_prompt(prompt_fn, item["text"]) for _ in range(n_runs)]
        most_common = max(set(runs), key=runs.count)
        results.append({"stable": len(set(runs)) == 1, "correct": most_common == item["expected"]})
    accuracy = sum(r["correct"] for r in results) / len(results)
    stability = sum(r["stable"] for r in results) / len(results)
    return {"accuracy": accuracy, "stability": stability}

def compare_prompts(prompts: dict, test_set, n_runs=3):
    for name, fn in prompts.items():
        result = evaluate_prompt(fn, test_set, n_runs)
        print(f"{name}：准确率 {result['accuracy']:.0%}，稳定性 {result['stability']:.0%}")

# 迭代阶段：只在开发集上对比三版
compare_prompts({"V1": prompt_v1, "V2": prompt_v2, "V3": prompt_v3}, DEV_SET)
# 定稿阶段：留出集只跑一次，验证真实表现
final = evaluate_prompt(prompt_v3, HOLDOUT_SET, n_runs=3)
```

设计要点：
1. V1→V2→V3 递进式设计，每次只新增一类优化手段，能精确归因"这一提升是哪类手段带来的"
2. 每条数据跑 `n_runs` 次，同时计算准确率（vs 人工标注）和稳定性（结果一致性），而不是只看"看起来对不对"
3. 开发集和留出集严格分离，留出集只在版本定稿后跑一次，避免"优化"实际上是过拟合了测试数据
4. 实验结论可复现——换一批新的测试数据、换一个人跑这套代码，得到的对比结论应该是一致的

---

## 建立成本意识

### Q61

**大模型 API 输入 Token 和输出 Token 为什么定价不同？这对 Prompt 设计有什么启发？**

输入 Token 只需要一次性编码（模型对已知的完整文本做一次前向计算），而输出 Token 是逐个自回归生成的——每生成一个新 Token，都要基于"之前已生成的全部内容"重新做一次完整的前向推理。生成 100 个输出 Token 实际上做了远多于"处理 100 个输入 Token"的计算量，因此多数平台的输出单价是输入单价的 2-4 倍。

启发：**控制输出长度是性价比最高的成本优化手段之一**。同样节省 100 个 Token，压缩 system prompt（输入）省的钱通常只有让模型少输出 100 Token（省的钱）的 1/2 到 1/4。这也是 Day 5 讲的"Prompt 长度约束 + `max_tokens` 兜底"在成本层面的意义——不仅是为了格式可控，也是最直接的降本手段。

---

### Q62

**为什么说多轮对话的 Token 消耗是"平方级增长"而不是线性增长？**

因为大模型 API 无状态（Day 4/Day 9 已讲），每一轮请求都要携带**完整的历史消息**。设每轮新增的消息本身占用 `L` 个 Token，第 N 轮请求时，输入里包含了前 N-1 轮的历史 + 本轮内容，输入 Token 约为 `N × L`：

```
第 1 轮输入 ≈ L，第 2 轮输入 ≈ 2L，...，第 N 轮输入 ≈ NL

N 轮对话的总输入 Token 消耗 = L + 2L + ... + NL = L × N(N+1)/2  ← 平方级
```

对比"线性增长"的直觉误区：很多人以为"20 轮对话成本 = 20 × 单轮成本"，但实际上每一轮的输入本身也在变长，总消耗是 `N²` 量级而不是 `N` 量级。这正是滑动窗口截断、历史摘要压缩这些手段在成本层面的意义——不做处理的话，对话越长，单轮成本增长得越快，而不是保持恒定。

---

### Q63

**长输入短输出和短输入长输出两类任务，成本优化的着力点分别是什么？**

判断依据是拆开看 `usage.prompt_tokens` 和 `usage.completion_tokens` 谁占大头，而不是只看 `total_tokens`：

| 场景 | 典型任务 | 成本主要由谁决定 | 优化方向 |
|------|---------|-----------------|---------|
| 长输入 + 短输出 | 文档问答、长文摘要 | 输入 Token | 减少输入：更精细的文档切分、只喂检索到的相关片段、精简历史 |
| 短输入 + 长输出 | 代码生成、长文写作 | 输出 Token（单价通常更高） | 约束输出长度、拆分任务分步生成、避免模型"自由发挥" |
| 长输入 + 长输出 | 带大量历史的多轮技术讨论 | 两者都重要 | 两头同时控制，是成本最容易失控的场景 |

两种场景优化方向完全不同：给"长输入短输出"任务加 `max_tokens` 限制收效有限（成本大头在输入），给"短输入长输出"任务优化历史截断也收效有限（本来就没有多轮历史）——先定位大头，再对症优化。

---

### Q64

**如何从零设计一个用于统计和记录大模型调用成本的工具？**

核心思路：**每次调用后立即从 `response.usage` 取真实计费数字，用结构化对象记录，而不是事后凭印象估算**。

```python
from dataclasses import dataclass, field

PRICING = {
    "deepseek-chat": {"input_per_million": 1.0, "output_per_million": 2.0},
}

@dataclass
class CostRecord:
    label: str
    model: str
    input_tokens: int
    output_tokens: int
    cost: float = field(init=False)

    def __post_init__(self):
        price = PRICING[self.model]
        self.cost = (
            self.input_tokens * price["input_per_million"] / 1_000_000
            + self.output_tokens * price["output_per_million"] / 1_000_000
        )


def call_and_record(label, messages, model="deepseek-chat"):
    response = client.chat.completions.create(model=model, messages=messages)
    usage = response.usage
    return CostRecord(label, model, usage.prompt_tokens, usage.completion_tokens)
```

设计要点：
1. **永远以 `usage` 字段为准**，不用字符数或 `tiktoken` 本地估算替代真实计费（本地估算只适合"下单前"的粗略预判）
2. 用 `dataclass` 把"一次调用的成本"变成结构化记录，而不是散落的变量，方便后续批量汇总、写入 CSV 或接入监控看板
3. 价格表（`PRICING`）单独维护，模型换代或平台调价时只改一处
4. 汇总时按"请求类型"分组统计，能直接回答"哪类功能最烧钱"这种业务问题，而不是只有一个笼统的总花费数字

---

### Q65

**如何量化评估一次 Prompt 优化在"效果提升"和"成本增加"之间是否划算？**

核心思路：把"额外付出的 Token"和"换来的效果提升"做比值，而不是只看效果单方面变好了就认为优化成功。

```python
def cost_per_accuracy_point(extra_tokens: int, accuracy_gain_pp: float) -> float:
    """每提升 1 个百分点准确率需要额外付出多少 Token，数值越小性价比越高"""
    return extra_tokens / accuracy_gain_pp if accuracy_gain_pp > 0 else float("inf")
```

以 Day 11 的 V1→V2→V3 实验为例：V1→V2 增加 80 Token 换来 40 个百分点准确率提升（2.0 Token/pp），V2→V3 增加 100 Token 只换来 20 个百分点提升（5.0 Token/pp）——V1→V2 这一步性价比明显更高。

**关键判断标准不是"哪个版本准确率最高"，而是"这个任务出错的业务代价有多大"**：

- 如果是成本敏感、容错率较高的场景（如粗筛工单优先级），可能停在性价比更高的 V2 就够了
- 如果是错误代价很高的场景（如涉及资金损失的紧急程度判断），V3 那部分性价比更低的额外提升仍然值得付出

这也是"效果优化"和"成本优化"两条主线在 Day 11、Day 12 交汇的地方：Day 11 只回答"哪个 Prompt 更准"，Day 12 补上"为了更准，多花的钱划不划算"，两者合起来才是完整的工程决策。

---

## 配置管理和日志

### Q66

**为什么 API Key 不能硬编码在代码中？`.env` + `.gitignore` 方案是如何配合使用的？**

硬编码密钥的风险链条是：代码提交到 Git → 仓库成员可见（哪怕是私有仓库）→ 一旦 push 到公开仓库，GitHub 等平台秒级扫描发现密钥 → Key 被吊销或盗用。这不是"只要小心就能避免"的问题——依赖人的自律不如用机制来保障。

`.env` + `.gitignore` 方案：

| 文件 | 内容 | 是否提交 Git |
|------|------|------------|
| `.env` | 真实的 Key 和本地配置值 | **不提交**（在 `.gitignore` 中排除） |
| `.env.example` | 变量名 + 占位符说明 | **提交**（供团队参考应该设哪些变量） |

```dotenv
# .env（不提交）
DEEPSEEK_API_KEY=sk-your-actual-key

# .env.example（提交）
DEEPSEEK_API_KEY=sk-your-key-here
```

```python
from dotenv import load_dotenv
import os

load_dotenv()
api_key = os.getenv("DEEPSEEK_API_KEY")
if not api_key:
    raise ValueError("DEEPSEEK_API_KEY 未设置，请在 .env 文件中配置")
```

`load_dotenv()` 的关键行为：环境变量已存在时默认不覆盖，CI/CD 注入的密钥优先级自然高于 `.env` 文件——这正是"命令行/环境变量 > .env 文件"优先级的机制实现。

---

### Q67

**配置的三种来源（命令行参数、环境变量、.env 文件）优先级如何排序？为什么这样设计？**

优先级从高到低：**命令行参数 > 环境变量 > .env 文件 > 代码默认值**。

| 来源 | 优先级 | 典型用途 |
|------|--------|---------|
| 命令行参数 | 最高 | 临时覆盖，调试时一次性切换模型 |
| 环境变量 | 高 | CI/CD 注入密钥，不同机器/环境设不同值 |
| `.env` 文件 | 中 | 本地开发默认配置，每人一份不互干扰 |
| 代码默认值 | 最低 | 非必填项的合理兜底，方便快速启动 |

**设计原因**：越"临时"的来源优先级越高——命令行参数是单次运行时的临时覆盖，不应该被持久化的 `.env` 文件覆盖；环境变量是运维/CI 在部署时注入的"正式值"，同样不应被开发者的本地 `.env` 覆盖；代码默认值只是"写了比没写好"的最低保障，任何外部配置都应该覆盖它。这种设计让同一份代码在开发、测试、生产三个环境中，只需改配置来源而无需改代码。

---

### Q68

**Python `logging` 的五个级别分别适合哪些场景？为什么建议用 `logging` 而不是 `print`？**

| 级别 | 数值 | LLM 项目典型用途 |
|------|------|----------------|
| DEBUG | 10 | 打印完整的 messages 列表、原始 API 响应（开发时用，生产关闭） |
| INFO | 20 | 程序启动信息、每次 API 调用完成（耗时、Token 数） |
| WARNING | 30 | 触发重试、输出被 max_tokens 截断、请求临近限速 |
| ERROR | 40 | JSON 解析失败、API 调用失败但程序可继续运行 |
| CRITICAL | 50 | API Key 无效等程序无法继续的灾难性错误 |

**为什么不用 `print`**：

| 能力 | `print` | `logging` |
|------|---------|-----------|
| 区分严重程度 | 无 | 有（五个级别） |
| 运行时过滤 | 只能注释掉代码 | `setLevel()` 一行切换 |
| 自动时间戳 | 无 | 有 |
| 写入文件 | 需要重定向 | 加一个 `FileHandler` |
| 性能（被过滤时） | `f-string` 总是先计算 | `%s` 格式化被过滤时不执行 |

实践细节：`logger.debug("msg: %s", obj)` 而不是 `logger.debug(f"msg: {obj}")`——后者无论日志级别是否过滤都会先执行字符串拼接，前者在 DEBUG 被过滤时完全不计算，大对象场景下性能差异明显。

---

### Q69

**给 LLM 项目的 API 调用加日志，应该记录哪些关键信息？**

最小有效日志集合：**请求标识 + 耗时 + Token 消耗**——这三个数字回答了排查时最常见的三个问题（这是哪个请求？为什么慢？钱花在哪了？）：

```python
import time, logging

logger = logging.getLogger(__name__)

def call_with_logging(client, messages, model, label=""):
    start = time.monotonic()
    try:
        response = client.chat.completions.create(model=model, messages=messages)
        elapsed = time.monotonic() - start
        usage = response.usage
        logger.info(
            "[%s] 完成 | 耗时 %.2fs | 输入 %d Token | 输出 %d Token",
            label or model, elapsed, usage.prompt_tokens, usage.completion_tokens,
        )
        return response
    except Exception as e:
        elapsed = time.monotonic() - start
        logger.error("[%s] 失败 | 耗时 %.2fs | %s", label or model, elapsed, e)
        raise
```

**启动日志单独处理**：程序启动时打印一次当前生效的配置（模型名、API Key 前 8 位、超时等），让运维在日志文件里就能确认"这次跑的是哪个配置"，而不需要登录机器查看 `.env` 文件：

```python
logger.info("模型：%s | Key：%s... | 超时：%ds", model, api_key[:8], timeout)
```

**打印 API Key 前 8 位而非完整值**：能区分"用的是哪个 Key"但不泄露完整密钥，是"可调试性"和"安全性"之间的标准折中。

---

### Q70

**如何从零设计一个"配置 + 日志 + 重试"三合一的可维护 LLM 客户端？**

核心设计原则：**配置对象化（可测试） + 日志结构化（可追溯） + 重试分级（可重试 vs 不可重试）**。

```python
import os, time, logging, sys
from dataclasses import dataclass
from openai import OpenAI, APIStatusError, APIConnectionError, RateLimitError
from dotenv import load_dotenv

load_dotenv()

@dataclass
class AppConfig:
    api_key: str
    model_name: str
    base_url: str
    max_retries: int
    request_timeout: int

    @classmethod
    def from_env(cls) -> "AppConfig":
        key = os.getenv("DEEPSEEK_API_KEY", "")
        if not key:
            raise ValueError("DEEPSEEK_API_KEY 未设置")
        return cls(
            api_key=key,
            model_name=os.getenv("MODEL_NAME", "deepseek-chat"),
            base_url=os.getenv("BASE_URL", "https://api.deepseek.com/v1"),
            max_retries=int(os.getenv("MAX_RETRIES", "3")),
            request_timeout=int(os.getenv("REQUEST_TIMEOUT", "30")),
        )

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s  %(levelname)-8s  %(name)s  %(message)s",
    handlers=[logging.StreamHandler(sys.stdout)],
)
logger = logging.getLogger(__name__)

class LLMClient:
    def __init__(self, config: AppConfig):
        self.config = config
        self.client = OpenAI(api_key=config.api_key, base_url=config.base_url)
        logger.info("初始化 | 模型：%s | Key：%s...", config.model_name, config.api_key[:8])

    def chat(self, messages: list, label: str = "") -> str:
        for attempt in range(1, self.config.max_retries + 1):
            start = time.monotonic()
            try:
                resp = self.client.chat.completions.create(
                    model=self.config.model_name, messages=messages,
                    timeout=self.config.request_timeout,
                )
                elapsed = time.monotonic() - start
                usage = resp.usage
                logger.info("[%s] 完成 | %.2fs | 输入 %d | 输出 %d Token",
                            label, elapsed, usage.prompt_tokens, usage.completion_tokens)
                return resp.choices[0].message.content
            except RateLimitError:
                wait = 2 ** attempt
                logger.warning("限速，%ds 后重试 (%d/%d)", wait, attempt, self.config.max_retries)
                time.sleep(wait)
            except APIConnectionError as e:
                logger.warning("连接失败，重试 (%d/%d)：%s", attempt, self.config.max_retries, e)
                time.sleep(1)
            except APIStatusError as e:
                if e.status_code in (400, 401, 403):
                    logger.error("不可重试错误 %d：%s", e.status_code, e.message)
                    raise
                logger.warning("服务器错误 %d，重试 (%d/%d)", e.status_code, attempt, self.config.max_retries)
                time.sleep(2)
        raise RuntimeError(f"达到最大重试次数 {self.config.max_retries}")
```

三层设计要点：
1. **配置层**：`AppConfig.from_env()` 集中读取和校验，测试时可直接 `AppConfig(api_key="test", ...)` 构造，不依赖真实环境变量
2. **日志层**：INFO 记录正常里程碑（耗时/Token），WARNING 记录临时性问题（重试），ERROR 记录不可恢复错误——级别清晰，生产环境用 `LOG_LEVEL=WARNING` 屏蔽噪音
3. **重试层**：区分"可重试"（限速/网络/5xx）和"不可重试"（401/400）——不区分地无脑重试会浪费时间，直接 raise 让调用方知道是不可恢复的错误

---

## 第 2 周综合复盘

### Q71

**第 2 周（Day 8–13）六个主题之间是什么逻辑关系？为什么要按这个顺序学？**

第 2 周的六个主题是同一个命题的六个层面——**如何让 LLM 应用从"能跑"变成"可信赖、可迭代、可量化、可维护"**，是一条递进依赖的学习链：

| 顺序 | Day | 主题 | 解决什么 | 依赖什么 |
|------|-----|------|---------|---------|
| 1 | 8 | 结构化输出 | 输出格式不可控 | Day 6 的 JSON 约束 |
| 2 | 9 | 输出校验与异常处理 | 校验失败时程序崩溃 | Day 8 的 Pydantic 模型 |
| 3 | 10 | 文本分类器 | 新任务类型（封闭集输出） | Day 8/9 的结构化 + 校验 |
| 4 | 11 | Prompt 优化 | 效果评估没有量化框架 | Day 10 的多次采样实验 |
| 5 | 12 | 成本意识 | Prompt 优化只看效果不看代价 | Day 11 的版本对比数据 |
| 6 | 13 | 配置与日志 | 成本/性能指标没有留存机制 | Day 12 的 Token 记录 |

**不能乱序的原因**：Day 9 的重试依赖 Day 8 定义的 Pydantic 模型（校验失败才触发重试）；Day 11 的稳定性评估依赖 Day 10 的多次采样结构；Day 12 的成本权衡依赖 Day 11 的准确率数据；Day 13 的 logging 把 Day 12 的成本数字持久化。每一步都在上一步产出的基础上加一层工程保障。

---

### Q72

**什么样的 Prompt 才算"稳定"？如何量化评估稳定性？**

技术定义：同一条输入多次调用（通常 3 次），结果完全一致的比例高。稳定性 < 70% 的 Prompt 不应上生产。

不稳定的四类根因及修复：

| 根因 | 表现 | 修复方式 |
|------|------|---------|
| 任务目标模糊 | 每次返回不同格式/维度 | 明确说"只输出 JSON，字段为 X" |
| 值域未锁定 | 标签值随机变化 | enum 类型 + Pydantic 强制校验 |
| 边界案例未覆盖 | 特殊输入结果飘忽 | Few-shot 展示边界案例处理方式 |
| Temperature 过高 | 结构化任务仍有随机性 | 结构化任务一律 temperature=0 |

```python
def stability_score(prompt_fn, test_set: list[str], n_runs: int = 3) -> float:
    stable = 0
    for text in test_set:
        runs = [call_llm(prompt_fn(text)) for _ in range(n_runs)]
        if len(set(runs)) == 1:
            stable += 1
    return stable / len(test_set)
```

**稳定性和准确率的关系**：两者独立，必须分开评估。一个 Prompt 可以"稳定地错"（每次输出相同的错误结果），也可以"偶尔蒙对"（结果不一致但多数投票正确）——两者都不是好 Prompt。理想状态是稳定性 ≥ 80% 且准确率达到业务要求。

---

### Q73

**为什么说输出校验比"提示模型要小心"更可靠？这体现了什么工程原则？**

"提示模型小心"是依赖 LLM 的输出自律，"校验"是在代码层建立检查点——两种方式的本质差距类似于"提醒开发者别提交密钥"和"用 `.gitignore` 阻止提交"：前者依赖人的记忆，后者用机制保障。

| 对比维度 | 依赖提示 | 代码层校验 |
|---------|---------|----------|
| 失败时行为 | 随机、不可预测 | 确定性抛出异常 |
| 失败可发现性 | 可能悄悄带着错误数据继续运行 | 立即捕获，不流入下游 |
| 补救手段 | 无（已经继续运行了） | 重试 → 兜底 → 三态结果 |
| 随时间稳定性 | 随模型版本更新波动 | 代码逻辑不变则行为稳定 |

**体现的工程原则**："能用机制保证的事情，不依赖人（或模型）的自律"。校验层的意义不是"LLM 会出错所以不信任它"，而是"任何外部输入（包括 LLM 输出）都应该在进入业务逻辑前经过验证"——这和验证用户输入、验证第三方 API 响应是完全相同的工程理念。

---

### Q74

**为什么日志和配置管理要在 Demo 阶段就引入，而不是等项目变大再补？**

**配置管理的债务是复利**：从第 1 个硬编码密钥到第 10 个，改造成本是线性增长的；但一旦发生泄露，代价是非线性的。更重要的是，改造时机越晚，"顺便用原来的方式就好了"的惯性越强，技术债越难偿还。

```
最佳引入时机：第一次写 client = OpenAI(api_key="...") 时
此时成本：创建 .env，写 AppConfig.from_env() — 10 分钟
此后收益：所有脚本共享配置，改一处生效全部，且永远不会意外提交密钥
```

**日志的价值在出问题时才体现**：用 `print()` 的项目出了问题必须在本地复现，而 `logging` 的项目看日志文件就能定位。问题总是在最不方便的时候（线上、压力下）发生，此时才意识到没有日志为时已晚。

```
print() 的调试困境：
  "上周五线上那次报错，输入是什么？输出了什么？耗时多少？"
  → 不知道，需要复现 → 复现不稳定 → 浪费大量时间

logging 的排查流程：
  "上周五线上那次报错，查日志" → 有时间戳、输入摘要、错误类型、Token 数
  → 5 分钟定位问题
```

**最佳实践**：把 `setup_logging()` 和 `AppConfig.from_env()` 写进项目模板，新建任何 LLM 脚本时第一件事就是复制这两行——让好习惯的执行成本趋近于零。

---

### Q75

**如果让你从零搭建一个"第 2 周成果汇总"工程骨架，你会如何设计它的模块结构？**

核心设计原则：**单一职责 + 配置集中 + 功能正交（各模块可独立调用）**。

```
week2_toolkit/
├── config.py          # AppConfig dataclass + from_env() + setup_logging()
├── client.py          # LLMClient（重试 + 日志 + 成本记录），依赖 config.py
├── prompt_templates.py# 抽取/分类/摘要三类 Prompt 构建函数，纯函数，无副作用
├── schemas.py         # Pydantic 模型定义（ResumeInfo, ClassificationResult 等）
└── main.py            # 演示入口，串联以上四个模块
```

各模块职责边界：

| 模块 | 职责 | 不包含 |
|------|------|--------|
| `config.py` | 读取/校验配置，初始化 logging | 不包含 API 调用逻辑 |
| `client.py` | 封装 API 调用（重试/日志/成本） | 不包含 Prompt 模板 |
| `prompt_templates.py` | 构建 messages 列表 | 不包含 API 调用，不含 Pydantic 校验 |
| `schemas.py` | Pydantic 模型定义 | 不含业务逻辑 |
| `main.py` | 演示串联 | 不含可复用逻辑 |

```python
# main.py — 串联示例
from config import AppConfig, setup_logging
from client import LLMClient
from prompt_templates import build_extraction_prompt, build_classification_prompt
from schemas import ResumeInfo, ClassificationResult

config = AppConfig.from_env()
setup_logging(config.log_level)
client = LLMClient(config)

resume = client.call_structured(
    build_extraction_prompt("张伟，Python 开发，5年经验", {"name": "姓名", "skills": "技能"}),
    schema=ResumeInfo, label="简历抽取",
)

category = client.call_structured(
    build_classification_prompt("我的订单延迟了", role="客服主管",
                                labels={"投诉": "有损失", "咨询": "信息查询"}),
    schema=ClassificationResult, label="工单分类",
)

client.print_cost_summary()
```

这种结构的好处：
1. 换 API 供应商只改 `config.py` 和 `client.py`，`prompt_templates.py` 和 `schemas.py` 零改动
2. 单元测试 `prompt_templates.py` 不需要 mock API——纯函数，输入什么出什么
3. `schemas.py` 的 Pydantic 模型可以直接生成 JSON Schema 用于 Prompt，两处定义合一
4. `main.py` 只有 15 行，每行都是业务逻辑，工程细节全部隐藏在其他模块里

---

## RAG 基本原理

### Q76

**RAG 解决了大模型的哪些核心局限？为什么不能直接把整个知识库塞给模型？**

RAG 主要解决三大局限：

| 局限 | 表现 | RAG 的解法 |
|------|------|-----------|
| 知识截止 | 训练数据有截止日期，之后的事不知道 | 检索实时/最新文档喂给模型 |
| 私有/领域知识 | 公司内部文档、产品手册从未出现在训练数据 | 把私有文档构建成向量索引 |
| 幻觉 | 模型用"听起来合理"的话填充不确定细节 | 强制模型只基于给定文档回答 |

**为什么不能直接塞整个知识库：**

1. **窗口有限**：百万级文档远超任何模型的 context 上限
2. **"Lost in the Middle" 效应**：context 太长时，模型对中间内容关注度显著下降
3. **成本爆炸**：10 份文档 × 1000 Token = 10,000 Token 输入；RAG 只喂 Top-3 片段 ≈ 900 Token，成本差 11 倍
4. **无关内容引入噪音**：不相关内容越多，模型越容易被"带偏"产生幻觉

核心结论：**RAG 不是"给模型更多信息"，而是"给模型更精准、更少的信息"**——这才是提升准确率的关键。

---

### Q77

**RAG 的离线构建和在线问答两个阶段分别做什么？为什么必须严格分开？**

| 阶段 | 时机 | 做什么 | 成本特点 |
|------|------|--------|---------|
| 离线构建 | 知识库更新时（一次性） | 文档加载 → 切分 → Embedding 向量化 → 存入向量数据库 | 一次性成本，换来后续查询的快速响应 |
| 在线问答 | 每次用户提问时 | 问题 Embedding → 向量检索 → 构建增强 Prompt → LLM 生成 | 每次查询只做检索 + 生成，不重复向量化 |

**为什么必须严格分开**：

```
错误做法（每次提问时都重新向量化所有文档）：
  用户提问 → 向量化全部文档（N × Embedding API 调用）→ 检索 → 生成
  成本 = 每次查询 × 全库文档数，不可接受

正确做法（离线预计算）：
  离线：向量化所有文档，存入向量库（一次性）
  在线：只向量化用户问题（1 次 Embedding）→ 从向量库检索（毫秒级）→ 生成
  成本 = 1 次 Embedding + 1 次生成，完全可接受
```

**一个实际感受**：1000 份文档如果每次查询都重新向量化，每天 1000 次查询的 Embedding 成本会是"离线预计算 + 在线查询"方案的 1000 倍。

---

### Q78

**文档切分（Chunking）中 Chunk Size 和 Chunk Overlap 各自的作用是什么？如何选取合适的值？**

**Chunk Size 的作用**：决定每块的语义粒度。太小则语义不完整，太大则检索精度下降且输入成本高。

**Chunk Overlap 的作用**：防止关键信息被切断到相邻块的边界处。

```
无 Overlap 的切断问题：
  Chunk 1: "...退款需在购买后 30 天内"
  Chunk 2: "提交申请，3-5 个工作日退回..."
  → "购买后 30 天内提交申请"这条关键信息跨越了两个 Chunk
  
有 Overlap（50 Token）：
  Chunk 1: "...退款需在购买后 30 天内"
  Chunk 2: "退款需在购买后 30 天内 提交申请，3-5 个工作日退回..."
  → Chunk 2 包含了完整信息
```

参数选取经验：

| 场景 | Chunk Size | Overlap |
|------|-----------|---------|
| 通用文档问答（默认） | 300 Token | 50 Token（约 15%） |
| 法律/合同（长句、严格逻辑） | 500 Token | 100 Token（约 20%） |
| FAQ 条目（每条本来就短） | 100–150 Token | 20 Token |

**不要过度调参**：先用默认值（300/50）跑通，根据实际回答质量再调整——问答质量低通常是 Embedding 或检索的问题，不一定是切分参数的问题。

---

### Q79

**RAG 中对 Embedding 模型有什么约束？不同方案（API vs 本地模型）如何选择？**

**核心约束**：文档的 Chunk 和用户的问题**必须使用同一个 Embedding 模型**。不同模型的向量空间不兼容——即使维度相同，语义相同的文本在不同模型下的向量也不在同一个空间里，余弦相似度比较没有意义。

**实践含义**：一旦选定 Embedding 模型并构建了向量索引，换模型就必须**重新向量化全部文档**，这是有成本的运维操作。模型选型要谨慎。

**方案对比**：

| 方案 | 代表模型 | 优点 | 缺点 | 适用场景 |
|------|---------|------|------|---------|
| 云端 API | OpenAI text-embedding-3-small | 效果好、接入简单 | 按量付费，数据出境 | 公开内容，对成本不敏感 |
| 国内 API | 各平台 Embedding 接口 | 中文优化，合规 | 需评估效果和稳定性 | 国内企业合规要求 |
| 本地模型 | sentence-transformers | 零 API 成本，数据不出境 | 需要推理硬件，维护成本 | 数据敏感、大规模批量向量化 |

入门阶段推荐 `sentence-transformers`（`paraphrase-multilingual-MiniLM-L12-v2`）：免费、支持中文、本地运行，跑通流程后再评估是否需要换 API。

---

### Q80

**如何从零实现一个最小可用的 RAG 问答系统？请描述关键代码的设计思路。**

核心思路：**离线构建向量索引 + 在线"检索-生成"两步流程**，最小实现只需 `sentence-transformers`、`numpy`、`openai` 三个库。

```python
from sentence_transformers import SentenceTransformer
import numpy as np

model = SentenceTransformer("paraphrase-multilingual-MiniLM-L12-v2")

# ─── 离线构建 ──────────────────────────────────────────────────────────────

def chunk_text(text: str, size: int = 300, overlap: int = 50) -> list[str]:
    words = text.split()
    chunks, i = [], 0
    while i < len(words):
        chunks.append(" ".join(words[i:i+size]))
        i += size - overlap
    return [c for c in chunks if c.strip()]

def build_index(docs: list[str]) -> dict:
    chunks = [c for doc in docs for c in chunk_text(doc)]
    return {"chunks": chunks, "vectors": model.encode(chunks)}

# ─── 在线检索 ──────────────────────────────────────────────────────────────

def retrieve(query: str, index: dict, top_k: int = 3) -> list[str]:
    q = model.encode([query])[0]
    scores = [float(np.dot(q, v) / (np.linalg.norm(q) * np.linalg.norm(v)))
              for v in index["vectors"]]
    top = sorted(range(len(scores)), key=lambda i: scores[i], reverse=True)[:top_k]
    return [index["chunks"][i] for i in top]

# ─── 在线生成 ──────────────────────────────────────────────────────────────

def answer(question: str, chunks: list[str], client) -> str:
    context = "\n\n---\n\n".join(f"[片段 {i+1}]\n{c}" for i, c in enumerate(chunks))
    messages = [
        {"role": "system", "content": "只根据参考资料回答，资料中没有的内容请明确说无法回答。"},
        {"role": "user", "content": f"参考资料：\n\n{context}\n\n问题：{question}"},
    ]
    return client.chat.completions.create(
        model="deepseek-chat", messages=messages, temperature=0.1
    ).choices[0].message.content
```

关键设计决策：
1. **`build_index` 与 `answer` 分离**：索引只建一次，问答每次调用；不要在问答函数里重建索引
2. **同一个 `model` 实例**：`build_index` 和 `retrieve` 用同一个 `SentenceTransformer` 对象，保证向量空间一致
3. **`temperature=0.1`**：事实问答用低温度减少随机性，但不用 0 是因为自然语言回答需要一点流畅度
4. **"只根据资料回答"约束**：这条 system 指令让模型在找不到答案时拒答，而不是编造——这是 RAG 减少幻觉的关键机制之一

---

## 读取本地文档

### Q81

**RAG 系统中文档读取阶段的质量为什么至关重要？常见的文档噪音有哪些？**

文档读取是 RAG 流程的入口，"垃圾进，垃圾出"——读取阶段引入的噪音会污染切分、向量化和最终回答的每一个环节。

```
噪音对下游的影响链：

  页眉"第 3 章" 混入正文
    → 这块 Chunk 向量化后代表"关于第 3 章的内容"
    → 用户问无关问题时，Chunk 可能被错误召回
    → 模型看到这段噪音内容，生成不准确的回答
```

**常见噪音类型**：

| 噪音类型 | 来源 | 影响 |
|---------|------|------|
| 连续多余空行 | PDF 排版还原 / 段落分隔 | Chunk 大量浪费在空白上 |
| 行内多余空格 | PDF 坐标还原误差 | 词被拆成多个 Token |
| 页眉页脚 | PDF 固定区域 | 每页都有相同文字混入多个 Chunk |
| 页码（- 1 - 等格式） | PDF 页脚 | 无语义，污染向量表示 |
| 图注（图 3-1：...） | 图片下方说明 | 没有图片内容，图注单独出现语义完整度低 |
| 编码乱码字符 | 字体映射不完整 | 切分时产生"词"实际是乱码，向量化后语义偏差 |

---

### Q82

**读取中文文本文件时编码问题如何处理？UTF-8、GBK、UTF-8-BOM 有什么区别？**

| 编码 | 全称 | 特点 | 常见来源 |
|------|------|------|---------|
| UTF-8 | Unicode Transform Format 8-bit | 国际通用，中文 3 字节/字 | macOS/Linux 创建的文件 |
| GBK | 国家标准汉字编码 | 中文 2 字节/字，向下兼容 GB2312 | Windows 下创建的中文文件 |
| UTF-8-BOM | UTF-8 + Byte Order Mark | UTF-8 文件开头有 3 字节 BOM（`\xEF\xBB\xBF`） | Windows 记事本"另存为 UTF-8" |

处理策略（按优先级）：

```python
# 1. 来源已知 → 直接指定
open(path, encoding="utf-8")

# 2. 中文文件，来源不确定 → 尝试列表
for enc in ("utf-8", "utf-8-sig", "gbk"):
    try:
        return open(path, encoding=enc).read()
    except UnicodeDecodeError:
        continue

# 3. 完全未知 → chardet 自动检测
import chardet
raw = open(path, "rb").read(10_000)
encoding = chardet.detect(raw)["encoding"] or "utf-8"
```

**`utf-8-sig` 的作用**：自动去除 UTF-8-BOM 开头的 3 字节标记——如果用 `utf-8` 读取含 BOM 的文件，文本开头会有一个看不见的 `﻿` 字符，混入文档内容影响后续处理。

---

### Q83

**为什么 PDF 文本提取比读取 TXT 文件难得多？有哪些常见坑？**

**根本原因**：PDF 是"页面描述语言"，存储的是"在坐标 (x, y) 处绘制字形 X"的指令，而不是有序的文本流。文本提取是逆向工程——把视觉坐标系上的字形还原成有逻辑顺序的文字。

```
TXT：字节序列直接对应字符顺序，`open()` 即读即用
PDF：坐标 (100, 700) 处的字 + 坐标 (500, 700) 处的字 → 它们在同一行，但顺序需要推断
```

**四大常见坑**：

| 坑 | 表现 | 解决方案 |
|----|------|---------|
| 多栏布局乱序 | 左右两栏文字交错出现（左1行→右1行→左2行→右2行） | 用 `pdfplumber.crop()` 分区域提取再拼接 |
| 页眉页脚混入 | 每页都有相同内容重复出现在多个 Chunk | `crop()` 裁剪掉顶底固定高度 |
| 乱码字符 | 中文显示为方块或奇怪符号 | 换用 pymupdf（字体处理更强）|
| 扫描件无文本 | `extract_text()` 返回 `None` 或空字符串 | OCR（pytesseract 等），Day 16 先跳过 |

推荐处理顺序：先用 `pdfplumber` 试跑，质量差再考虑 pymupdf；扫描件问题放到有 OCR 需求时再解决，不要一开始就引入复杂依赖。

---

### Q84

**读取文档后为什么需要文本清洗？哪些清洗操作是安全的，哪些会破坏 RAG 效果？**

**为什么需要清洗**：不同来源的文档读取后格式差异大，噪音直接影响切分质量和向量语义准确性。清洗的目标是"去除视觉格式噪音，保留语义信息"。

**安全的清洗操作**（不影响语义）：

```python
text = text.replace("\r\n", "\n")         # 统一换行符
text = re.sub(r"\n{3,}", "\n\n", text)    # 压缩连续空行
text = re.sub(r"[ \t]{2,}", " ", text)    # 压缩多余空格
text = text.strip()                       # 去首尾空白
```

**危险的清洗操作**（会破坏 RAG 效果）：

| 操作 | 破坏原因 |
|------|---------|
| 去停用词（"的""了""是"） | Embedding 模型依赖完整句子的上下文，去停用词破坏语法结构 |
| 词干化 / 词形还原 | 中文分词本身不适用，英文文档也会破坏 Embedding 语义 |
| 去标点符号 | 标点影响句子边界，去掉后切分和 Embedding 都会出问题 |
| 强制截断到 N 字符 | 应该在切分阶段处理，不是在加载阶段截断 |

**判断原则**：如果一个清洗操作会改变文本的语义或信息完整性，就不应该在文档加载阶段做——这类操作要么不做，要么放到切分策略里按需处理。

---

### Q85

**如何设计一个支持多格式的统一文档加载器？请描述接口设计和关键实现思路。**

核心设计原则：**统一输出格式（`Document` dataclass）+ 格式路由表 + 各格式独立加载函数**。

```python
from dataclasses import dataclass, field
from pathlib import Path

@dataclass
class Document:
    text: str          # 清洗后的纯文本
    source: str        # 来源文件路径（Day 20 引用来源时使用）
    file_type: str     # txt / md / pdf
    page_count: int = 1
    metadata: dict = field(default_factory=dict)

def load_document(path: str) -> Document:
    ext = Path(path).suffix.lower()
    loader = {".txt": _load_txt, ".md": _load_md, ".pdf": _load_pdf}.get(ext)
    if loader is None:
        raise ValueError(f"不支持的格式：{ext}")
    return loader(path)

def load_directory(dir_path: str, extensions=None, recursive=False) -> list[Document]:
    exts = extensions or [".txt", ".md", ".pdf"]
    pattern = "**/*" if recursive else "*"
    docs = []
    for ext in exts:
        for f in Path(dir_path).glob(f"{pattern}{ext}"):
            try:
                docs.append(load_document(str(f)))
            except Exception as e:
                print(f"[跳过] {f}：{e}")
    return docs
```

**接口设计的三个关键决策**：

1. **为什么用 `Document` 而不是直接返回字符串**：`source` 字段是 Day 20 引用来源的基础；`page_count` 和 `metadata` 为后续扩展预留空间；统一格式让调用方代码稳定，换格式只改加载器内部实现

2. **为什么用路由表 `{".txt": _load_txt, ...}`**：比 `if/elif` 更易扩展——新增格式只在路由表加一行，不改 `load_document` 主体逻辑

3. **`load_directory` 为什么用 try/except 跳过失败文件**：知识库可能包含部分损坏或不支持的文件，单个失败不应中断整批加载——记录日志后跳过，保证能加载其他正常文件

---

## 文本切分

### Q86

**固定大小切分和按语义边界切分有什么本质区别？各自的适用场景？**

| 维度 | 固定大小切分 | 按语义边界切分 |
|------|-------------|--------------|
| 切分依据 | 字符数 / Token 数（与内容无关） | 段落、句子等自然语言边界 |
| Chunk 大小 | 均匀可控 | 差异较大（受原文结构影响） |
| 语义完整性 | 可能在句子中间截断 | 保证每块是完整的语义单元 |
| 实现复杂度 | 极简 | 需要语言检测（段落符、句号等） |
| 适用场景 | 快速原型；结构无规律的纯流文本 | 有清晰段落结构的正式文档、法律合同、FAQ |

**核心区别**：固定大小切分以"字符均匀"为目标，与文本语义完全解耦；语义边界切分以"语义完整"为目标，接受大小不均换来的内容完整性。

**实际影响**：固定切分可能把一句"退款需在 30 天内 | 提交申请"从中截断，导致两个 Chunk 各只包含半句话，向量化后语义都不完整，检索时容易召回错误内容。

---

### Q87

**Chunk Size 和 Chunk Overlap 各自的作用是什么？选取不当会带来什么后果？**

**Chunk Size（块大小）**：

- 决定每块向量代表的"语义粒度"
- 过小：每块只有几个词，语义不完整，无法独立回答问题；向量化后语义混乱
- 过大：一块里混入多个话题，向量是多话题的平均，检索时既不精准又带入大量无关内容
- 经验值：中文场景 300–500 字符（约 200–400 字），英文场景 300–600 Token

**Chunk Overlap（块重叠）**：

- 解决语义跨块边界的问题——一个事实可能恰好跨在两个 Chunk 的切断处
- 通过让相邻两块共享一段文本（前块尾部 = 后块头部），确保边界处的语义在至少一块中是完整的
- 过小（< 5%）：边界信息丢失，检索时跨块关键信息无法被召回
- 过大（> 40%）：大量冗余存储，索引体积膨胀，相似块互相干扰检索结果
- 经验值：Chunk Size 的 15–25%（如 Chunk Size=400，Overlap=60–100）

---

### Q88

**递归字符切分（Recursive Character Splitter）的核心思路是什么？为什么推荐作为默认策略？**

**核心思路**：给定一组按优先级排列的分隔符（如 `["\n\n", "\n", "。", ".", " ", ""]`），先尝试最高优先级的分隔符切分；如果某块仍然超过 `chunk_size`，则用下一个分隔符继续切分这块，直到满足大小约束或用尽所有分隔符（最后用字符强制切分兜底）。

```
分隔符优先级：段落 > 行 > 句号 > 词 > 字符
结果：尽可能在"最自然的边界"处切，只在迫不得已时才截断更小的单元
```

**为什么推荐作为默认**：

1. **语义完整性好**：优先段落边界，退而求其次用句子，最差情况才截字符——比固定字符切分语义损失小得多
2. **大小可控**：通过 `chunk_size` 硬性约束，不会出现纯段落切分时 Chunk 大小差异几十倍的情况
3. **通用性强**：同一组分隔符对中英文混合文档都适用，无需针对语言调整
4. **LangChain 默认实现验证**：社区大规模使用证明它是最实用的折中方案

---

### Q89

**切分策略对 RAG 检索质量的影响路径是什么？如何设计切分策略对比实验？**

**影响路径**：

```
切分策略
  → Chunk 语义纯度（一块是否只包含一个话题）
    → 向量化质量（向量能否精确代表这块的语义）
      → 检索召回质量（问题向量和答案向量的余弦相似度）
        → 最终回答质量（模型拿到的上下文是否包含答案）
```

**对比实验设计**：

```python
# Step 1：准备同一份文档，用不同策略切分
strategies = {
    "固定字符": chunk_by_chars(text, 400, 80),
    "递归切分": RecursiveCharacterSplitter(400, 80).split(text),
    "句子切分": chunk_by_sentences(text, 300, 1),
}

# Step 2：对每种策略统计 Chunk 分布（块数、大小均值/标准差/最值）
for name, chunks in strategies.items():
    print(analyze_chunks(chunks, name))

# Step 3：构建向量索引，用一组测试问题检索，记录最高相似度分数
for query in test_questions:
    evaluate_retrieval(query, text, strategies)

# Step 4：分析哪种策略的最高相似度最高（说明检索到的最相关块更纯粹）
```

**关键观察点**：相同 `chunk_size` 下，语义边界切分通常检索相似度更高；过粗的切分（大段落）相似度反而低，因为向量是多话题平均，与问题向量距离更远。

---

### Q90

**如何从零实现一个支持多策略、可对比的文本切分工具？请描述核心设计决策。**

**设计目标**：支持四种策略，统一输出格式，能输出统计报告，易于扩展新策略。

```python
from abc import ABC, abstractmethod
from dataclasses import dataclass

@dataclass
class ChunkStats:
    strategy: str
    count: int
    mean_size: float
    std_size: float
    min_size: int
    max_size: int


class BaseSplitter(ABC):
    @abstractmethod
    def split(self, text: str) -> list[str]:
        ...

    def analyze(self, text: str) -> ChunkStats:
        from statistics import mean, stdev
        chunks = self.split(text)
        sizes = [len(c) for c in chunks]
        return ChunkStats(
            strategy=self.__class__.__name__,
            count=len(chunks),
            mean_size=round(mean(sizes), 1) if sizes else 0,
            std_size=round(stdev(sizes), 1) if len(sizes) > 1 else 0,
            min_size=min(sizes) if sizes else 0,
            max_size=max(sizes) if sizes else 0,
        )


class FixedCharSplitter(BaseSplitter):
    def __init__(self, chunk_size=400, overlap=80):
        self.chunk_size = chunk_size
        self.overlap = overlap

    def split(self, text: str) -> list[str]:
        chunks, start = [], 0
        while start < len(text):
            chunks.append(text[start:start + self.chunk_size].strip())
            start += self.chunk_size - self.overlap
        return [c for c in chunks if c]


# RecursiveCharacterSplitter、SentenceSplitter 继承 BaseSplitter，各自实现 split()

def compare_all(text: str, splitters: list[BaseSplitter]) -> None:
    print(f"{'策略':<30}{'块数':>6}{'均值':>8}{'标准差':>8}{'最小':>8}{'最大':>8}")
    print("-" * 70)
    for sp in splitters:
        s = sp.analyze(text)
        print(f"{s.strategy:<30}{s.count:>6}{s.mean_size:>8}{s.std_size:>8}"
              f"{s.min_size:>8}{s.max_size:>8}")
```

**三个核心设计决策**：

1. **抽象基类 `BaseSplitter`**：统一接口，`compare_all` 不关心策略内部实现；新增策略只加一个子类，不改比较框架
2. **`analyze()` 放在基类**：统计逻辑复用，各策略天然支持统计报告，无需重复实现
3. **`ChunkStats` dataclass**：结构化输出，比返回 dict 更类型安全，方便后续扩展（如画分布直方图）

---

## Embedding 与向量检索

### Q91

**Embedding 模型的"一致性约束"是什么？违反后会出现什么问题？**

一致性约束是指：RAG 流程中，对文档 Chunk 进行向量化（离线构建）和对用户问题进行向量化（在线检索）**必须使用同一个 Embedding 模型**。

**违反后的后果**：不同 Embedding 模型产生的向量处于不同的语义空间——即使两个向量的维度相同，它们的坐标系也完全不同，余弦相似度计算结果毫无意义。

```
类比：用厘米尺量文档（建立索引），用英寸尺量问题（在线查询）
      结果：12 厘米和 12 英寸看起来都是 12，但实际对比的是两个不同单位，毫无意义

Embedding 层面：
  模型 A 认为"退款"→ [0.8, 0.1, ...]
  模型 B 认为"退款"→ [0.3, 0.7, ...]
  用 A 建库，用 B 查询 → 两个向量在各自空间里都合理，但放在一起比余弦相似度是随机的
```

**实践影响**：一旦上线后更换 Embedding 模型，必须**重新向量化全部文档**并重建索引——这是一个有运维成本的操作，因此模型选型要尽早确定。

---

### Q92

**余弦相似度、内积、L2 距离三种度量方式有什么区别？RAG 场景为什么通常选余弦相似度？**

| 度量 | 公式 | 范围 | 受向量长度影响 | RAG 适用性 |
|------|------|------|--------------|-----------|
| 余弦相似度 | `(A·B)/(‖A‖·‖B‖)` | [-1, 1] | 否（只看方向） | **首选** |
| 内积（点积） | `A·B` | 无限制 | 是（长度影响结果） | 仅在向量归一化后等价于余弦 |
| L2 距离 | `√Σ(aᵢ-bᵢ)²` | [0, ∞) | 是 | 图像/数值特征，文本场景少用 |

**为什么 RAG 选余弦相似度**：

文本的 Embedding 向量长度会随文本长度变化——短问题（5 个字）的向量模通常比长段落（200 个字）小。如果用内积，"语义完全相同但文字更多"的段落会因为向量模更大而得到更高分，这不是我们想要的排序逻辑。余弦相似度只看两个向量的**方向夹角**，消除了长度带来的偏差，让语义相近程度直接决定排序结果。

**实践速查**：

```
score ≥ 0.85 → 语义非常相近，高度相关
0.7–0.85    → 相关，通常可以作为参考资料
0.5–0.7     → 弱相关，需要结合其他条件判断
< 0.5       → 关系很小，通常过滤掉
```

---

### Q93

**API-based Embedding 和本地 Embedding 模型如何选择？各自的适用场景？**

| 维度 | API-based | 本地模型 |
|------|----------|---------|
| 代表方案 | DeepSeek Embedding、OpenAI text-embedding-3-small | sentence-transformers（MiniLM / BGE） |
| 向量维度 | 1024–1536 | 384–1024 |
| 效果 | 较强，商用级优化 | 略低，但中文 BGE 系列效果优秀 |
| 成本 | 按 Token 计费 | 零 API 成本（有硬件成本） |
| 数据隐私 | 数据需发送到云端 | 数据留在本地 |
| 延迟 | 100–300ms（网络 I/O） | 5–50ms（CPU 推理）|
| 适用阶段 | 生产环境，对效果要求高 | 学习/原型/数据敏感场景 |

**决策树**：

```
数据能出境且预算充足？
  → 是 → API-based（DeepSeek 或 OpenAI）
  → 否 → 本地模型

本地模型选型：
  纯学习/原型             → MiniLM-L12-v2（轻量，中英文双语）
  生产中文知识库           → BAAI/bge-large-zh-v1.5（效果最强）
  需要中文+英文均衡         → BAAI/bge-m3（多语言版本）
```

入门阶段统一推荐 `paraphrase-multilingual-MiniLM-L12-v2`：约 120MB，免费，支持中英文，CPU 可运行，足以验证 RAG 完整流程。

---

### Q94

**向量检索中的 Top-K 和相似度阈值分别解决什么问题？应该如何配合使用？**

**Top-K** 解决"返回多少结果"的问题：

- K 过小（K=1）：上下文信息量不足，模型可能因为缺少关键细节而回答不完整
- K 过大（K=10）：引入太多低相关内容，占用过多 Context Token 且容易干扰模型判断
- 经验值：`top_k=3` 是大多数场景的合理起点

**相似度阈值（min_score）** 解决"什么都没检索到时如何拒绝"的问题：

```
场景：用户问"明天天气怎么样"，知识库全是产品文档

无阈值：top-1 结果 score=0.38，强行返回一段完全不相关的文档
        → 模型收到无关资料，可能生成幻觉或胡乱联系

有阈值（min_score=0.6）：score=0.38 < 0.6，过滤掉，返回空列表
                         → RAG 层报告"无相关文档"→ 模型可以诚实说不知道
```

**配合使用策略**：

```python
results = index.search(query_vec, top_k=3)
results = [r for r in results if r["score"] >= 0.6]

if not results:
    return "根据当前知识库，未找到与您问题相关的信息。"
```

阈值不是越高越好——过高会导致"宁愿拒答也不回答"的体验；过低则相当于没有阈值。建议先用 0.5–0.6 起步，根据实际拒答率调整。

---

### Q95

**如何从零构建一个支持持久化、批量向量化、带阈值过滤的 RAG 检索组件？请描述核心设计决策。**

核心设计原则：**Embedding 模型可替换 + 向量存储与检索逻辑分离 + 批量 API 调用降成本**。

```python
class EmbeddingModel:
    """统一封装 API 和本地两种 Embedding 来源，外部调用方无感知"""
    def __init__(self, mode="local"):  # mode: "local" | "api"
        ...
    def embed(self, texts: list[str]) -> list[list[float]]:
        ...  # 内部分发：local 用 sentence-transformers，api 用 OpenAI SDK

@dataclass
class VectorStore:
    """纯 numpy 实现的向量索引，支持 JSON 持久化"""
    texts: list[str]
    embeddings: list[list[float]]
    metadatas: list[dict]

    def search(self, query_vec, top_k=3, min_score=0.0) -> list[dict]:
        # 矩阵归一化 + 矩阵乘法，一次计算所有余弦相似度
        scores = (matrix_normed) @ q_norm
        return [r for r in top_k_results if r["score"] >= min_score]

    def save(self, path): json.dump(...)
    def load(cls, path): ...

class RAGRetriever:
    """上层门面，对外暴露 build + retrieve 两个接口"""
    def build_from_texts(self, chunks, metadatas=None):
        vecs = self.embedder.embed(chunks)  # 批量向量化
        self.store.add(chunks, vecs, metadatas)

    def retrieve(self, query, top_k=3, min_score=0.5) -> list[dict]:
        q_vec = self.embedder.embed([query])[0]
        return self.store.search(q_vec, top_k, min_score)
```

**三个核心设计决策**：

1. **`EmbeddingModel` 隔离 API/本地差异**：调用方（`RAGRetriever`）只调用 `embedder.embed(texts)`，切换 API/本地只改构造函数参数，不改检索逻辑
2. **矩阵运算代替循环**：`matrix_normed @ q_norm` 一次算出全部相似度，比 for 循环逐一计算快 10–100 倍
3. **`min_score` 过滤在 `VectorStore.search` 内完成**：不在上层判断，保证"空结果"和"有结果"两个分支都被统一处理，调用方不需要额外过滤逻辑

---

## RAG 问答生成

### Q96

**RAG 场景下上下文拼接的"Lost in the Middle"效应是什么？如何缓解？**

"Lost in the Middle" 是斯坦福 2023 年研究提出的现象：当 Prompt 中有多段检索文本时，模型对开头和结尾部分的注意力显著高于中间部分，中间的内容容易被忽视。

```
模型注意力分布示意（Context 中的位置 vs 关注程度）：

  开头 ████████████████████ 高
  中间 ████░░░░░░░░░░░░░░░░ 低
  末尾 ████████████████     较高

实验结论：同样的关键信息，放在开头或末尾，模型引用率比放中间高约 20%
```

**缓解策略**：

| 策略 | 做法 | 适用场景 |
|------|------|---------|
| 相关度降序（默认） | 最相关 Chunk 排第一（开头优势） | Top-K ≤ 3 时简单有效 |
| "三明治"排列 | 最相关排末尾，其余降序在前 | 有明确最佳命中 Chunk 时 |
| 控制 Chunk 数量 | Top-K=3 而非 10，减少中间层 | 通用推荐 |
| 精准切分 | Chunk 语义纯粹，不混入多话题 | 治本，而非治标 |

---

### Q97

**为什么 RAG 需要"只根据资料回答"约束？不加约束会出现什么问题？**

大模型有"不愿说不知道"的倾向——当知识不确定时，宁愿生成"听起来合理"的内容，而不是承认局限。RAG 场景中如果没有约束，模型会把检索到的资料和训练数据中的"已有知识"混合使用，产生**RAG 幻觉**：

```
场景：知识库只有产品 A 的退款政策（30天）

用户问：产品 B 能退款吗？
检索结果：[产品 A 退款条款]（最相关的内容，但与 B 无关）

无约束回答：
  "根据通用退款政策，产品 B 可以在 30 天内退款。"
  → 把 A 的规则"推断"成了 B 的规则，看起来权威但完全是捏造的

有约束回答：
  "参考资料中仅包含产品 A 的退款政策，未涉及产品 B。
   建议直接联系客服确认产品 B 的退款条款。"
  → 诚实地反映了资料的边界，用户知道应该去哪里找答案
```

约束的核心价值：**让系统的可信度可预测**——用户知道这个系统只说它"知道"的事，不会冒充专家随意发挥。

---

### Q98

**RAG 中拒答逻辑应该在检索层还是生成层实现？两者有何区别？**

两层拒答解决的是不同的问题，应该同时实现：

| 维度 | 检索层拒答（min_score 阈值） | 生成层拒答（模型自判断） |
|------|--------------------------|----------------------|
| 触发条件 | 向量相似度低于阈值，没有足够相关的 Chunk | Chunk 存在但模型认为与问题实际无关 |
| 实现方式 | `if not results: return "拒答文案"` | System Prompt 中的约束指令 |
| 优点 | 确定性强、节省 LLM 调用成本 | 能处理"检索到但内容无关"的情况 |
| 缺点 | 阈值敏感，过高误拒、过低漏放 | 依赖模型遵守指令，不如代码逻辑稳定 |
| 适用场景 | 完全无关的问题（节省 API 费用） | 边界情况（Chunk 相关但内容不够） |

```python
# 双层拒答的正确写法
results = store.search(q_vec, top_k=3, min_score=0.6)  # 检索层

if not results:   # 检索层拒答：直接返回，不调用 LLM
    return "根据现有知识库，未找到相关信息。"

answer = llm_generate(query, results)   # 生成层可能自行判断"资料不够"
return answer
```

两层协同：检索层是"硬拒绝"（快速、节省成本），生成层是"软拒绝"（处理检索层漏网的边界情况）。

---

### Q99

**RAG 的 temperature 应该设多少？和普通对话有何不同？**

RAG 场景的核心目标是**忠实地从资料中提取和整理信息**，这需要确定性而非创造性，因此 temperature 通常比普通对话低得多：

| 场景 | 推荐 temperature | 原因 |
|------|----------------|------|
| RAG 问答（精确） | 0.0–0.1 | 同一问题多次调用结果一致，方便测试和验证 |
| 普通对话 | 0.7 | 需要一定多样性和自然感 |
| 创意写作 | 1.0–1.5 | 需要高随机性 |
| 代码生成 | 0.0 | 语法和逻辑不能随机 |

**为什么 RAG 不用 0.0 而用 0.1**：temperature=0.0 会导致完全确定性的输出，但不同版本的模型对同一个 `temperature=0.0` 的处理方式可能有细微差异；0.1 保留了极少量随机性，让回答在语言流畅性上稍微自然一些，同时对事实准确性几乎没有影响。

**关键判断**：如果 RAG 系统用于生产，回答需要可测试（同一问题每次答案要基本一致），就使用低 temperature。如果只是演示，可以适当提高到 0.3 让语言更自然。

---

### Q100

**如何从零设计一个端到端的 RAG 问答系统？描述各模块的职责和接口。**

端到端 RAG 由五个阶段组成，每个阶段有明确的输入输出契约：

```
【离线构建阶段】
DocumentLoader.load(path) → Document(text, source, ...)
       ↓
TextSplitter.split(text) → list[str]（Chunks）
       ↓
EmbeddingModel.embed(chunks) → list[list[float]]
       ↓
VectorStore.add(chunks, vecs, metadatas) → None
VectorStore.save(path) → 持久化到磁盘

【在线问答阶段】
用户输入 query
       ↓
EmbeddingModel.embed([query]) → list[float]
       ↓
VectorStore.search(q_vec, top_k=3, min_score=0.6) → list[dict]
       ↓
空列表 → 直接返回拒答文案（不调用 LLM）
       ↓
build_prompt(query, results) → messages（system + user）
       ↓
LLM.generate(messages, temperature=0.1) → str
       ↓
返回最终回答
```

**各模块接口设计原则**：

```python
# 统一文档格式，使加载器可替换
@dataclass
class Document:
    text: str; source: str; metadata: dict

# 切分器可替换（固定字符 / 递归 / 句子）
class BaseSplitter:
    def split(self, text: str) -> list[str]: ...

# Embedding 可替换（API / 本地）
class EmbeddingModel:
    def embed(self, texts: list[str]) -> list[list[float]]: ...

# 向量库可替换（numpy / FAISS / Chroma）
class VectorStore:
    def add(self, texts, vecs, metadatas=None): ...
    def search(self, q_vec, top_k=3, min_score=0.5) -> list[dict]: ...
    def save(self, path) / load(cls, path): ...

# 上层 Pipeline：组合所有模块
class RAGPipeline:
    def build(self, docs_dir: str): ...
    def ask(self, query: str) -> str: ...
```

**三个工程关键点**：
1. **每层只暴露最小接口**：`ask(query)` 返回字符串，调用方不需要感知检索/生成的内部细节
2. **拒答必须在 LLM 之前**：`if not results: return` 放在生成调用之前，节省 API 费用
3. **模块替换不影响上层**：换 Embedding 模型或向量库只改 `RAGPipeline.__init__`，`ask()` 的逻辑完全不动

---

## RAG 引用与可解释性

### Q101

**为什么 RAG 系统需要引用机制？引用解决了什么问题？**

引用机制让用户能追溯答案的来源，是 RAG 系统可解释性（Explainability）的核心保障。它解决了 Day 19 "只根据资料回答"约束遗留的一个关键问题——用户无法区分答案来自知识库还是模型"脑补"。

```
没有引用的回答：
  "购买后 30 天内可申请全额退款。"
  → 用户疑问：这 30 天的规定从哪来的？是模型编的吗？

有引用的回答：
  "购买后 30 天内可申请全额退款 [1]。"
  [1] 退款政策：购买后 30 天内可申请全额退款...
  → 用户可以核实，对系统产生信任
```

引用的三个层次：

| 层次 | 做法 | 价值 |
|------|------|------|
| L1 标记引用 | 在回答中标注来源编号 | 最低成本，让用户知道"有出处" |
| L2 展示来源 | 回答后附上引用原文片段 | 用户可直接核实，无需另外查找 |
| L3 可验证 | 程序校验引用编号是否真实、内容是否匹配 | 防止模型"虚构引用"，保证引用可信 |

引用与 Day 19 拒答的关系：拒答保证"不知道时不说"，引用保证"知道时给出依据"，两者共同构成 RAG 的可解释性。

---

### Q102

**自然语言引用和结构化 JSON 引用有什么本质区别？为什么推荐结构化输出？**

自然语言引用是让模型在回答文本中写"根据[资料1]"，结构化引用是让模型输出 JSON `{"answer": "...", "sources": [1]}`，两者有三个本质区别：

| 维度 | 自然语言引用 | 结构化 JSON 引用 |
|------|------------|-----------------|
| 格式稳定性 | 模型可能写"[1]"、"资料1"、"根据资料1"等不一致格式 | JSON 字段固定，格式稳定 |
| 可解析性 | 需要正则或再调一次模型提取编号 | `json.loads()` 直接取 `sources` |
| 可校验性 | 无法可靠映射编号到资料做验证 | 整数列表可直接和检索结果索引匹配 |

自然语言引用的核心问题：

```
模型可能输出："根据[资料1]和[资料3]，退款时间为 30 天"
→ 要从自然语言中提取 "1" 和 "3" 需要额外解析，且不可靠

结构化输出：
{"answer": "退款时间为 30 天", "sources": [1, 3]}
→ 直接取 sources=[1,3]，可以和检索结果做索引匹配
```

结构化输出的设计要点：`answer` 字段保持纯文本不含引用标记（展示时由程序决定标注方式），`sources` 字段用整数列表（可直接做索引匹配），`sources` 只放实际存在的编号（防止越界）。

---

### Q103

**什么是"虚构引用"？有哪些类型？如何检测？**

虚构引用是指模型在结构化输出中声称引用了某条资料，但实际上该资料中并没有相关内容——这比不引用更危险，因为用户以为有出处，实际是幻觉。

三种虚构引用类型：

| 类型 | 表现 | 检测难度 | 检测方法 |
|------|------|---------|---------|
| 编号越界 | sources=[5]，但只有 3 条资料 | 简单 | 检查编号是否在 1–N 范围内 |
| 编号有效但内容不匹配 | sources=[3]，但 [3] 中没有 answer 的内容 | 困难 | 语义匹配：检查 answer 与引用资料的共享关键词 |
| 遗漏引用 | answer 非空但 sources 为空 | 中等 | 检查 answer 非空时 sources 是否也非空 |

```python
def validate_citation(rag_answer, contexts):
    issues = []
    valid_sources = []
    # 防线 1：编号有效性
    for src in rag_answer.sources:
        if 1 <= src <= len(contexts):
            valid_sources.append(src)
        else:
            issues.append(f"引用编号 {src} 越界")
    # 防线 2：拒答一致性
    is_refusal = "无法回答" in rag_answer.answer
    if is_refusal and valid_sources:
        issues.append("拒答但仍有引用，存在矛盾")
    # 防线 3：内容匹配性（粗粒度关键词检查）
    for src in valid_sources:
        ctx_text = contexts[src - 1]["text"]
        if not extract_shared_keywords(rag_answer.answer, ctx_text):
            issues.append(f"引用 [{src}] 与回答内容不匹配，可能为虚构引用")
    return {"is_valid": len(issues) == 0, "valid_sources": valid_sources, "issues": issues}
```

类型 2（内容不匹配）最难检测——粗粒度关键词匹配可以拦截明显的不匹配，但更精确的校验需要语义相似度或额外模型调用，成本较高。生产环境中先用关键词匹配做粗筛，高价值场景再引入语义校验。

---

### Q104

**引用校验的"三道防线"分别解决什么问题？为什么缺一不可？**

三道防线分别解决不同类型的引用问题，覆盖从"格式合法性"到"语义合理性"的完整校验链路：

| 防线 | 检查内容 | 拦截的虚构类型 | 为什么缺一不可 |
|------|---------|---------------|---------------|
| 1. 编号有效性 | sources 中的编号是否在 1–N 范围内 | 越界引用 | 拦截"不存在的资料编号"，防止展示时索引越界报错 |
| 2. 拒答一致性 | answer="无法回答" 时 sources 是否为空 | 逻辑矛盾 | 拦截"说了不知道却又引用了资料"的自相矛盾输出 |
| 3. 内容匹配性 | 引用资料与 answer 是否有共享关键词 | 内容虚构 | 拦截"编号合法但内容无关"最危险的幻觉引用 |

三道防线的协同关系：

```
防线 1（格式层）→ 过滤越界编号，保证 sources 指向真实存在的资料
    ↓
防线 2（逻辑层）→ 检查拒答与引用的一致性，保证输出不自相矛盾
    ↓
防线 3（语义层）→ 检查引用内容与回答的匹配度，拦截"有编号但内容对不上"的虚构

只有防线 1：越界问题解决了，但"编号合法、内容虚构"的幻觉漏网
只有防线 1+2：逻辑矛盾解决了，但"编号和逻辑都对、内容却无关"的问题漏网
三道齐全：从格式 → 逻辑 → 语义，层层递进，覆盖最广
```

防线 3 是粗粒度关键词匹配而非精确语义校验，原因：关键词匹配成本极低（纯 Python 字符串操作），能拦截最明显的内容不匹配；精确语义校验需要额外 Embedding 或模型调用，成本高，留到高价值场景按需引入。

---

### Q105

**如何从零设计一个带引用溯源和校验的 RAG 问答系统？描述核心设计决策。**

核心设计原则：**结构化输出保证可解析 + 三道防线保证可信 + 引用展示保证可追溯**。

```python
from pydantic import BaseModel, Field

class RAGAnswer(BaseModel):
    """带引用的回答结构"""
    answer: str = Field(description="基于参考资料生成的回答")
    sources: list[int] = Field(description="引用的资料编号列表")

class CitedRAGPipeline:
    def ask(self, query: str) -> str:
        # 1. 检索（Day 18）
        results = self.store.search(embed([query])[0], top_k=3, min_score=0.55)
        if not results:
            return "根据当前知识库，未找到相关信息。"  # 检索层拒答

        # 2. 生成带引用的回答（response_format 强制 JSON）
        prompt = build_citation_prompt(query, results)
        raw = self.llm_call(prompt, response_format={"type": "json_object"})
        rag_answer = RAGAnswer(**json.loads(raw))

        # 3. 引用校验（三道防线）
        validation = validate_citation(rag_answer, results)

        # 4. 格式化展示（回答 + 引用区块 + 警告）
        return format_cited_answer(rag_answer, results, validation)
```

三个核心设计决策：

1. **answer 与 sources 分离**：`answer` 保持纯文本不含 `[1]` 标记，引用信息独立放在 `sources` 列表。展示时由程序决定标注方式（方括号、上标、超链接），而非让模型在自然语言中混入标记。好处是格式稳定、可程序化处理、展示灵活。

2. **三道防线分层校验**：编号有效性（格式层）→ 拒答一致性（逻辑层）→ 内容匹配性（语义层），从简单到复杂递进。每层解决不同类型的虚构引用，防线 1 和 2 成本极低（纯逻辑判断），防线 3 用粗粒度关键词匹配保证零额外 API 成本。校验结果通过 `issues` 列表反馈给展示层，用户可以看到"⚠ 引用 [3] 与回答内容不匹配"的警告。

3. **校验结果不影响主流程**：即使引用校验发现问题，系统仍然返回回答（附带警告），而不是直接拦截。原因：虚构引用的检测是"概率性"的（尤其防线 3 的关键词匹配可能有误报），直接拦截会导致误拒；让用户看到回答 + 警告，由用户自行判断更合理。生产环境可以根据 `issues` 数量设置分级策略（如 1 个 issue 只警告，3 个以上 issue 标记"低可信度"）。

---

## 第 3 周综合复盘

### Q106

**第 3 周（Day 15–20）六个主题之间是什么逻辑关系？RAG 各环节如何相互影响？**

第 3 周的六个主题是同一条数据流的六个连续环节——**把"私有文档"转化为"可信回答"**，是一条串联依赖链，每个环节的输出是下一个环节的输入：

| 顺序 | Day | 主题 | 输入 | 输出 | 关键影响 |
|------|-----|------|------|------|---------|
| 1 | 16 | 读取文档 | 磁盘文件 | 干净文本 | 噪音决定后续信噪比 |
| 2 | 17 | 文本切分 | 干净文本 | Chunks | 切分质量决定检索颗粒度 |
| 3 | 18 | Embedding | Chunks | 向量索引 | 向量质量决定检索相似度 |
| 4 | 18 | 向量检索 | 问题+索引 | 相关 Chunks | 召回率决定答案上界 |
| 5 | 19 | 问答生成 | 问题+Chunks | 回答文本 | Prompt 约束决定忠实度 |
| 6 | 20 | 引用校验 | 回答+Chunks | 可信回答 | 校验决定用户信任度 |

**串联依赖的关键结论**：RAG 的质量上限由最弱的环节决定。再好的 LLM 也救不了"文档噪音太多"；再好的 Embedding 模型也救不了"关键句子被切断到两个 Chunk 里"。不能只调 Prompt，而忽略上游数据质量。

---

### Q107

**RAG 准确率主要受哪些环节影响？每个环节出问题时典型表现是什么？**

RAG 准确率有两层：检索准确率（找到答案）和生成准确率（忠实表达答案），两者是串联关系——检索准确率是生成准确率的上界。

| 环节 | 典型问题表现 | 根本原因 | 诊断方法 |
|------|------------|---------|---------|
| 文档读取 | 正确答案在文档里，但未被召回 | 噪音稀释了 Chunk 向量 | 打印原始读取文本，看有无页眉页脚乱码 |
| 文本切分 | 模型说"资料不足"，但内容跨两个相邻 Chunk | 语义边界被截断 | 打印 Top-K Chunks，看答案是否被切断 |
| Embedding | 中文问题匹配不到中文文档 | 使用了纯英文 Embedding 模型 | 换多语言模型（如 paraphrase-multilingual-MiniLM） |
| 相似度阈值 | 问有答案的问题却拒答 | min_score 设置过高 | 临时降到 0.0 看是否召回，定位阈值问题 |
| Prompt 约束 | 模型混入训练知识回答，来源不可追溯 | 约束指令不够明确 | 在 System Prompt 明确"未在资料中的内容必须拒答" |

**诊断优先级**：先打印检索结果（`top_k=5, min_score=0.0`），确认答案在不在检索结果里；在的话改 Prompt，不在的话往上游（切分/读取）查。大多数"答案不准"问题出在检索层，不是生成层。

---

### Q108

**Chunk 切分策略为什么会直接影响 RAG 最终回答的质量？影响路径是什么？**

切分影响质量有三条独立的传导路径：

**路径一：语义截断 → 向量失真 → 检索遗漏**

```
"退款需在 30 天内提交。提交后 3-5 天退回。"
     ↓  在"提交"后切断
Chunk A："退款需在 30 天内提交"（不含处理时长）
Chunk B："提交后 3-5 天退回"（不含前置条件）
     ↓
问"退款处理多久" → Chunk A、B 分别独立检索均不完整 → 回答残缺
```

**路径二：Chunk 过小 → 上下文碎片化 → 生成质量差**

Chunk 过小时，一个完整意思散落在 5 个 Chunk 里，拼接后语义跳跃，LLM 无法整合出完整答案。

**路径三：Chunk 过大 → 向量被稀释 → 相似度虚低**

单个 Chunk 包含退款+发货+会员多话题时，与"退款问题"的相似度被非退款内容拉低，可能排名靠后而错过召回。

**实用参数选择**：通用文档用 300 Token / 50 Token Overlap，先跑通再根据召回效果调整。Overlap 的作用是保证"关键句子必然完整出现在至少一个 Chunk 里"，不是越大越好（浪费存储和 API 成本）。

---

### Q109

**为什么引用来源在业务场景中比单纯的答案准确率更重要？**

引用解决的不是"答案对不对"，而是"用户能否验证答案"——这是两个不同层次的问题。

**信任度不等于准确率**：一个系统准确率 95% 意味着每 20 个问题有 1 个错误。如果没有引用，用户不知道哪 1 个是错的，只能选择全信或全不信。有了引用，用户可以在"重要决策"时验证，在"日常查询"时直接接受，建立有边界的信任。

**错误信息的传播成本**：

```
无引用系统的错误答案：
  "退款需要 3 天" → 用户直接相信 → 告诉朋友 → 10 个人产生错误预期
  → 企业直到大量投诉才发现问题

有引用系统的错误答案：
  "退款需要 3 天 [1]" + [1] 实际写的是"5 天"
  → 用户查看原文，发现矛盾 → 反馈给企业 → 企业快速修复知识库
```

**业务必要性按场景**：企业客服和法律/医疗场景中，错误答案可能导致法律纠纷（如错误承诺退款时间），引用使企业能追溯答案来源，证明"我们的答案来自这份政策文档"；内部工作助手中，员工向上级说明依据时需要引用原始政策文件，而不是"AI 说的"。

---

### Q110

**如果让你从零搭建一个"第 3 周成果汇总"完整 RAG Demo，你会如何设计它的架构？**

核心设计原则：**模块接口清晰 + 组件可替换 + 每步可观测**。

```
week3_rag/
├── document_loader.py  # Document dataclass + load_txt/load_md/load_pdf
├── text_splitter.py    # RecursiveCharacterSplitter（可配置 size/overlap）
├── embedding.py        # EmbeddingModel（API 版/本地版可切换）
├── vector_store.py     # VectorStore（numpy → FAISS → Chroma 可升级）
├── rag_generator.py    # build_prompt + 约束生成 + 双层拒答
├── citation.py         # RAGAnswer + validate_citation（三道防线）
└── rag_pipeline.py     # RAGPipeline.build() + RAGPipeline.ask()
```

```python
# rag_pipeline.py — 核心组合逻辑
class RAGPipeline:
    def build(self, docs: list[Document]) -> None:
        chunks = [c for doc in docs for c in self.splitter.split(doc.text)]
        sources = [doc.source for doc in docs for _ in self.splitter.split(doc.text)]
        vecs = self.embedder.embed(chunks)
        self.store.add(chunks, vecs, sources)

    def ask(self, query: str) -> str:
        results = self.store.search(self.embedder.embed([query])[0], top_k=3, min_score=0.55)
        if not results:
            return "根据当前知识库，未找到相关信息。"
        raw = self.llm_call(build_citation_prompt(query, results))
        rag_answer = RAGAnswer(**json.loads(raw))
        validation = validate_citation(rag_answer, results)
        return format_cited_answer(rag_answer, results, validation)
```

**三个关键工程决策**：

1. **`build` 与 `ask` 严格分离**：索引只建一次，`ask` 每次只做"问题向量化 + 检索 + 生成"，不重复向量化文档。这是 RAG 性能的根本保证。

2. **每层输入输出用数据类约束**：`Document`、`RAGAnswer` 用 dataclass 定义，不用裸字典。换组件时只改实现，接口不变，上层代码零修改。

3. **校验结果展示，不拦截**：`validate_citation` 的问题通过 `issues` 列表附加到回答末尾（⚠ 警告），不直接丢弃回答。理由：关键词匹配是粗粒度的，误报率不低；让用户看到警告自行判断，比"悄悄丢弃"更透明。

---

## Tool Calling 基础

### Q111

**什么是 Tool Calling？模型"调用"工具的过程中，实际发生了什么？**

Tool Calling（工具调用）是一种让 LLM 主动触发外部函数执行的机制。开发者提前注册工具，模型根据用户意图判断是否调用，以及调用哪个工具、传什么参数。

**关键认知**：模型本身不执行工具，它只负责**决策和参数填充**，输出一个结构化的"调用请求"；实际执行发生在你的代码里。

完整过程：

```
用户输入 → 模型判断需要工具 → 输出 tool_calls（JSON 格式）
  → 你的代码解析 tool_calls → 执行真实函数 → 把结果传回模型
  → 模型基于结果生成自然语言回答
```

这意味着一次"Tool Calling"包含**两次 LLM API 调用**（第一次决策，第二次生成最终回答），中间的工具执行在应用侧完成。

---

### Q112

**"回答问题"和"调用工具做事"的本质区别是什么？**

| 维度 | 普通对话（回答问题） | Tool Calling（调用工具） |
|------|------------------|----------------------|
| 数据来源 | 训练时冻结的知识 | 运行时从外部实时获取 |
| 是否有副作用 | 无（只生成文字） | 有（可写数据库、发邮件、调 API） |
| 回答依据 | 模型"记忆" | 外部系统的确定性返回值 |
| 数据新鲜度 | 知识截止日期限制 | 每次调用都是最新数据 |

**直觉类比**：普通对话的模型像一本百科全书（只能告诉你书里写的内容）；Tool Calling 的模型像一个带手机的顾问（可以实时搜索、打电话，告诉你现在的真实情况）。

问"今天北京天气"：普通对话只能给出泛泛的季节性描述；Tool Calling 可以调用天气 API 返回今天的实时温度。

---

### Q113

**Tool Calling 的完整交互流程是什么？每一步做了什么？**

完整流程分 5 步，横跨两次 API 调用：

| 步骤 | 谁做 | 做什么 |
|------|------|--------|
| 1 | 你的代码 | 把工具定义（`tools` 参数）+ 对话历史发给模型 |
| 2 | 模型 | 判断是否需要工具，若需要则返回 `tool_calls`（而不是普通文字回答） |
| 3 | 你的代码 | 解析 `tool_calls`，执行对应的 Python 函数，得到结果 |
| 4 | 你的代码 | 把工具结果以 `role: "tool"` 消息追加进历史，再次发给模型 |
| 5 | 模型 | 基于工具结果生成最终自然语言回答 |

```python
# Step 2 的关键检查
msg = response.choices[0].message
if msg.tool_calls:        # 需要调用工具
    for tc in msg.tool_calls:
        result = dispatch(tc.function.name, json.loads(tc.function.arguments))
        messages.append({"role": "tool", "tool_call_id": tc.id,
                         "content": json.dumps(result)})
    # Step 5：第二次 create()
    final = client.chat.completions.create(model=..., messages=messages, tools=TOOLS)
else:                     # 直接回答，不需要工具
    answer = msg.content
```

`tool_call_id` 的作用：当模型一次返回多个 `tool_calls` 时，每个工具结果通过 `id` 对应到具体请求，避免混淆。

---

### Q114

**工具定义（Function Schema）中 name、description、parameters 各有什么作用？如何写好 description？**

| 字段 | 作用 | 写不好的后果 |
|------|------|------------|
| `name` | 模型用这个名字在 `tool_calls` 里标记要调用哪个函数 | 命名不清晰，调试时难以识别 |
| `description` | 模型靠这段文字判断"是否该调用这个工具" | 描述模糊 → 模型调错工具或不调工具 |
| `parameters` | 告诉模型该传什么参数，是 JSON Schema 格式 | 约束不清 → 模型传错类型或遗漏必填项 |

**写好 description 的四个要素**：

```python
# 好的 description
"获取指定城市的当前实时天气，返回温度（摄氏度）、天气状况和风速。"
"仅适用于查询当前天气，不支持历史数据和未来预报。"

# 要素分解：
# 1. 工具能做什么："获取指定城市的当前实时天气"
# 2. 返回什么格式："返回温度（摄氏度）、天气状况和风速"
# 3. 适用边界："仅适用于当前天气查询"
# 4. 不适用场景："不支持历史数据和未来预报"（防止滥用）
```

**参数 description 同样重要**：每个参数的 `description` 字段告诉模型如何填写该参数，如 `"城市名称，例如'北京'、'上海'"` 比 `"城市"` 对模型更有指导意义。

---

### Q115

**如何从零实现一个最小的 Tool Calling Demo？描述关键代码的设计思路。**

最小实现的三个核心设计：**工具注册分离、统一分发器、两次 API 调用**。

```python
# 1. 工具定义（JSON Schema）与实现函数分开管理
TOOLS = [{"type": "function", "function": {
    "name": "get_weather",
    "description": "获取城市实时天气",
    "parameters": {"type": "object",
                   "properties": {"city": {"type": "string", "description": "城市名"}},
                   "required": ["city"]}
}}]

def get_weather(city: str) -> dict:
    return {"city": city, "temp": 25, "condition": "晴"}  # 真实场景调外部 API

# 2. 统一分发器：将模型返回的函数名映射到实际函数
TOOL_FUNCTIONS = {"get_weather": get_weather}
def dispatch_tool(name, args): return TOOL_FUNCTIONS[name](**args)

# 3. 两次 API 调用的核心逻辑
def chat_with_tools(user_input: str) -> str:
    messages = [{"role": "user", "content": user_input}]
    # 第一次：模型决策
    resp = client.chat.completions.create(model="deepseek-chat",
                                          messages=messages, tools=TOOLS, tool_choice="auto")
    msg = resp.choices[0].message
    if not msg.tool_calls:
        return msg.content   # 不需要工具，直接返回
    # 执行工具
    messages.append(msg)
    for tc in msg.tool_calls:
        result = dispatch_tool(tc.function.name, json.loads(tc.function.arguments))
        messages.append({"role": "tool", "tool_call_id": tc.id,
                         "content": json.dumps(result, ensure_ascii=False)})
    # 第二次：生成最终回答
    final = client.chat.completions.create(model="deepseek-chat", messages=messages, tools=TOOLS)
    return final.choices[0].message.content
```

**三个设计决策的理由**：
1. **工具注册与实现分离**：`TOOLS`（Schema）和 `TOOL_FUNCTIONS`（函数）解耦，新增工具只加一行 Schema + 一个函数，主流程零改动
2. **统一分发器**：把 `if name == "get_weather": ...` 的条件分支改成字典映射，可扩展性强，且工具名错误时能统一报错
3. **`tool_choice="auto"`**：让模型自己判断是否调用工具；`"none"` 禁用工具（测试用），`"required"` 强制调用（特殊场景）

---

## 定义工具

### Q116

**工具参数设计有哪些核心原则？必填参数和可选参数如何决策？**

工具参数不是给人用的，是给 **LLM 填写**的——设计目标是让模型在不需要额外解释的情况下正确填写每个参数。

**四个核心原则**：

| 原则 | 说明 | 反例 |
|------|------|------|
| 参数名自解释 | `city` 比 `c` 好，`user_id` 比 `uid` 好 | 参数名 `data`，描述"数据" |
| 每个参数只传一类信息 | 城市和国家用两个参数，不要用一个"地址"参数 | `location: "中国北京"` |
| 参数数量控制 | 5 个以内；超过就考虑拆分工具 | 15 个参数的单工具 |
| 可选参数给合理默认值 | `unit="celsius"` 比让模型每次都填好 | 没有默认值的可选参数 |

**必填 vs 可选决策**：工具在没有这个参数时能正常运行 → 可选；不能 → 必填。如果有默认行为，必须在 description 里说明缺省时的行为。

**enum 的作用**：选项型参数用 enum 锁定所有合法值，比只写 description 的限制效果强得多：

```python
# 弱约束：模型可能填 "celsius"、"c"、"摄氏度" 等不一致的值
"unit": {"type": "string", "description": "温度单位"}

# 强约束：模型只能填这两个值
"unit": {"type": "string", "enum": ["celsius", "fahrenheit"]}
```

---

### Q117

**工具函数的返回值应该如何设计才对 LLM"友好"？**

LLM 会把工具返回值序列化成字符串传入对话，设计原则是**让模型容易理解和引用**。

**三个设计原则**：

1. **平铺优于嵌套**：模型处理 `result["rate"]` 比 `result["data"]["exchange"]["rate"]` 更可靠
2. **包含语义上下文**：数值旁边附上单位和含义
3. **提供 summary 字段**：预先整合的自然语言描述，模型可以直接引用

```python
# 不友好（缩写字段名 + 纯数字 + 深嵌套）
{"t": 25, "c": "sun", "data": {"w": {"speed": 3}}}

# 友好（自解释 + 有单位 + 有 summary）
{
    "city": "北京",
    "temperature_celsius": 25,
    "condition": "晴天",
    "wind_level": "3 级",
    "summary": "北京今天晴天，25°C，3 级风，适合户外活动"
}
```

**summary 的价值**：为复杂数据提供预先整合好的自然语言描述，模型无需自己拼接多个字段，减少幻觉风险。特别是在数据有多个维度时（天气 = 温度 + 天气状况 + 风速 + 湿度），summary 可以帮助模型生成更准确、更流畅的回答。

---

### Q118

**工具出错时应该返回错误 dict 还是抛异常？两种方式有什么区别？**

推荐**返回错误 dict**，而不是抛异常。

| 对比维度 | 抛异常 | 返回错误 dict |
|---------|--------|-------------|
| 对话流程 | 中断，需要在 dispatch 层捕获 | 不中断，错误信息传给模型 |
| 模型感知 | 看不到错误信息 | 能看到 message，可生成友好提示 |
| 用户体验 | 程序可能崩溃或返回技术栈信息 | 模型解释"城市名不对，您是否想查..." |
| 适用场景 | 程序级不可恢复错误（配置错误） | 业务级可解释错误（输入无效、API 超时） |

```python
# 标准错误返回格式
def get_weather(city: str) -> dict:
    if not city.strip():
        return {"error": True, "message": "城市名不能为空"}   # 模型能读懂并向用户解释
    result = call_api(city)
    if result is None:
        return {"error": True, "message": f"未找到城市 '{city}' 的天气数据，请检查名称"}
    return result
```

当模型接收到 `{"error": True, "message": "未找到城市 'Abc' 的天气数据"}` 时，它可以生成"抱歉，我没有找到 Abc 的天气数据，请确认城市名称是否正确，或者尝试英文名称"，而不是让程序崩溃。

---

### Q119

**如何验证工具定义（Function Schema）的质量？有哪些常见的规范问题？**

工具 Schema 的质量直接影响模型是否能正确判断"何时调用"和"如何填参数"，上线前应该做系统性检查。

**常见规范问题**：

| 问题类型 | 表现 | 修复方式 |
|---------|------|---------|
| description 过短 | 少于 20 字，缺少关键信息 | 补充能做什么 + 适用边界 |
| 参数缺少 description | 模型只能靠参数名猜含义 | 每个参数都加 description + 示例 |
| 选项型参数未用 enum | 模型可能填不一致的值 | 加 `"enum": [...]` 锁定取值 |
| 必填参数设置过多 | 把可选参数也设成必填 | 能有默认值的改为可选 |
| 工具描述互相重叠 | 两个工具功能相似，模型不知选哪个 | 在 description 里明确各自的使用边界 |

```python
def validate_tool_schema(schema: dict) -> list[str]:
    issues = []
    fn = schema.get("function", {})
    if not fn.get("description") or len(fn["description"]) < 20:
        issues.append("description 过短，建议 ≥ 20 字")
    for name, param in fn.get("parameters", {}).get("properties", {}).items():
        if not param.get("description"):
            issues.append(f"参数 '{name}' 缺少 description")
    return issues
```

**实际验证方法**：schema 静态检查之外，还要**实测模型行为**——用 5–10 个不同措辞的问题测试工具能否被正确触发，以及参数填写是否准确。

---

### Q120

**如何从零设计一个可被 LLM 调用的工具库？描述模块结构和关键设计决策。**

核心设计原则：**工具实现与 Schema 定义同文件、统一分发器、工具可独立测试**。

```
tools/
├── __init__.py          # 汇总所有工具的 SCHEMA + 分发器
├── weather.py           # get_weather 函数 + WEATHER_TOOL_SCHEMA
├── exchange_rate.py     # get_exchange_rate 函数 + EXCHANGE_RATE_TOOL_SCHEMA
├── knowledge_base.py    # search_knowledge_base 函数 + KB_TOOL_SCHEMA
└── tests/
    └── test_tools.py    # 每个工具的独立测试（正常路径 + 错误路径）
```

```python
# tools/__init__.py — 统一入口
from .weather import get_weather, WEATHER_TOOL_SCHEMA
from .exchange_rate import get_exchange_rate, EXCHANGE_RATE_TOOL_SCHEMA
from .knowledge_base import search_knowledge_base, KNOWLEDGE_BASE_TOOL_SCHEMA

ALL_TOOLS = [WEATHER_TOOL_SCHEMA, EXCHANGE_RATE_TOOL_SCHEMA, KNOWLEDGE_BASE_TOOL_SCHEMA]

TOOL_REGISTRY = {
    "get_weather": get_weather,
    "get_exchange_rate": get_exchange_rate,
    "search_knowledge_base": search_knowledge_base,
}

def dispatch(name: str, args: dict) -> dict:
    if name not in TOOL_REGISTRY:
        return {"error": True, "message": f"未知工具：{name}"}
    return TOOL_REGISTRY[name](**args)
```

**三个关键设计决策**：

1. **Schema 与实现同文件**：`WEATHER_TOOL_SCHEMA` 和 `get_weather` 在同一个文件里，避免修改函数参数时忘记同步 Schema，减少"Schema 和实现不一致"的 bug

2. **统一分发器 + 注册表**：`TOOL_REGISTRY` 字典替代 `if/elif` 条件分支，新增工具只加一行注册，不改主流程；工具名不存在时统一报错

3. **工具必须能独立运行**：每个工具文件的 `if __name__ == "__main__"` 块可以直接运行测试，不依赖 LLM 环境——这是发现参数设计问题的最快方式，在接入模型之前就能排除大部分问题

---

## 多步工作流

### Q126

**单步工具调用和多步工作流在结构上有什么本质区别？**

核心区别是**工具调用次数是否在运行时确定**：

| 维度 | 单步（Day 24） | 多步（Day 25） |
|------|--------------|--------------|
| API 调用次数 | 固定 2 次（决策 + 生成） | N+1 次（N 由模型自主决定） |
| 实现结构 | `if msg.tool_calls: 执行; 第二次调用` | `while iteration < max_iter: 执行; if not tool_calls: break` |
| 调用链长度 | 最多 1 个工具 | 0 到 max_iter 个工具 |
| 适用问题 | 单一数据源查询（"北京天气"） | 多数据源综合（"上海出差，带500美元够吗，天气怎样"） |

单步 = 固定两次 API 调用；多步 = 循环 N+1 次，N 由模型在每一轮的 `tool_calls` 是否为空决定。

---

### Q127

**多步工作流中的循环应该什么时候退出？有哪些终止条件？**

有三种退出条件，**前两种是必须实现的**：

**条件 1（正常）：模型不再请求工具**
```python
if not msg.tool_calls:
    return msg.content   # 模型认为信息已够，直接回答
```
这是最理想的退出方式，模型自主判断"我已经有足够信息了"。

**条件 2（安全）：达到最大迭代次数 `max_iterations`**
```python
for iteration in range(max_iter):
    ...
else:
    # for 循环正常结束（未被 break 打断）= 达到上限
    return force_generate_answer(messages)
```
防止模型在工具返回错误或 Bug 时无限调用，产生大量 API 费用。

**条件 3（可选）：连续 N 次工具调用均返回错误**
```python
consecutive_errors += 1
if consecutive_errors >= 3:
    return "工具暂时不可用，请稍后再试。"
```
适合对可靠性要求高的生产场景。

---

### Q128

**为什么多步工作流必须设置 max_iterations 上限？不设置会有什么风险？**

`max_iterations` 是多步工作流的**安全阀**，防止两类问题：

**风险 1：模型 Bug 导致无限循环**
```
异常场景：工具返回了空结果 {}，模型认为"还需要数据"继续调用 → 工具还是返回空 → ...
无上限：无限循环直到 API 超时或账单耗尽
有上限：第 6 次迭代强制退出，生成基于已有信息的回答
```

**风险 2：Token 成本累积超预期**
```
第 N 次 API 调用的输入 Token = Σ(前 N-1 次的所有工具调用结果) + 系统提示 + 用户输入
→ 随迭代次数增加，每次调用的 Token 消耗线性增长
→ 10 次迭代的总 Token ≈ 1 次的 4–5 倍

有上限：明确限制最大成本区间，避免单次对话成本暴增
```

**推荐值**：通用助手 `max_iter=5`；复杂 Agent `max_iter=10`；生产环境根据 `预算 / 单次平均成本` 计算。

---

### Q129

**多步工作流的"可观测性"指什么？如何实现调用链日志？**

可观测性指能从外部看清楚工作流内部每一步发生了什么，包含三个层次：

- **层次 1（基础）**：调用了几次工具、分别是哪些
- **层次 2（调试）**：每次调用的入参、出参（能定位"哪一步出了问题"）
- **层次 3（审计）**：每次调用的耗时、Token 消耗、决策理由

**实现要点**：

```python
for iteration in range(max_iter):
    # 记录决策
    logger.info("[迭代 %d] 工具：%s", iteration, [tc.function.name for tc in msg.tool_calls])
    
    for tc in msg.tool_calls:
        t = time.monotonic()
        result = registry[tc.function.name](**args)
        
        # 记录工具结果（用 summary 字段，避免日志过长）
        logger.info("  %s(%s) → %s（%.3fs）", tc.function.name, args,
                    result.get("summary", str(result)[:60]), time.monotonic() - t)
```

同时可以把每步记录到 `WorkflowResult.steps`（dataclass），用于：
- 事后分析哪类问题会触发几步工具调用
- 将调用链展示在 UI 界面上（Day 26 的需求）
- 计算平均工具调用次数，优化 max_iter 设置

---

### Q130

**如何从零实现一个支持多步推理、带调用链记录的工作流 Demo？描述关键设计决策。**

核心设计：**while 循环 + max_iter 安全上限 + for-else 强制生成 + WorkflowResult 记录**。

```python
@dataclass
class WorkflowResult:
    user_input: str
    final_answer: str
    steps: list = field(default_factory=list)  # 每步 ToolStep 记录
    terminated_by: str = "model_done"          # "model_done" | "max_iterations"

def run_workflow(user_input, max_iter=6):
    wf = WorkflowResult(user_input=user_input)
    messages = [system_msg, {"role": "user", "content": user_input}]
    
    for iteration in range(max_iter):
        msg = client.chat.completions.create(
            model="deepseek-chat", messages=messages,
            tools=TOOLS, tool_choice="auto", temperature=0,
        ).choices[0].message
        
        if not msg.tool_calls:      # ← 正常退出
            wf.final_answer = msg.content
            wf.terminated_by = "model_done"
            break
        
        messages.append(msg)
        for tc in msg.tool_calls:
            result = TOOL_REGISTRY.get(tc.function.name, bad_tool)(**json.loads(tc.function.arguments))
            wf.steps.append(ToolStep(tc.function.name, ...))
            messages.append({"role": "tool", "tool_call_id": tc.id,
                             "content": json.dumps(result, ensure_ascii=False)})
    
    else:   # for-else：循环未被 break 打断，说明达到 max_iter   ← 安全退出
        wf.terminated_by = "max_iterations"
        wf.final_answer = client.chat.completions.create(model="deepseek-chat",
            messages=messages, temperature=0).choices[0].message.content
    
    return wf
```

**五个关键设计决策**：
1. **for-else 语义**：`for` 循环被 `break` 打断 → 正常退出（不执行 `else`）；循环跑完 → 达上限（执行 `else`），天然区分两种退出路径
2. **WorkflowResult dataclass**：记录每步 ToolStep，支持事后分析和 UI 展示
3. **强制生成兜底**：达到上限后不直接报错，而是基于已有历史消息调一次 API 生成尽量有用的回答
4. **`temperature=0`**：每次决策调用都用 0，保证同一问题的调用顺序可预测、可复现
5. **工具错误 dict 而非 raise**：工具返回 `{"error": True, ...}` 让模型感知并处理，而不是程序崩溃

---

## 单工具调用 Demo

### Q121

**tool_choice 参数的 auto / none / required 三种模式有什么区别？各适用于什么场景？**

`tool_choice` 控制模型在收到工具列表后的决策行为：

| 模式 | 行为 | 典型场景 |
|------|------|---------|
| `"auto"`（默认） | 模型自主判断是否调用工具；需要时返回 `tool_calls`，不需要时直接回答 | 通用对话助手，用户可能问任何问题 |
| `"none"` | 禁用所有工具，即使注册了工具也不调用，只用训练知识回答 | 基线测试（对比有无工具时的回答质量） |
| `"required"` | 强制模型必须调用至少一个工具，不允许直接文字回答 | 信息抽取流水线，需要保证每条输入都走工具路径 |

还可以指定具体工具：
```python
tool_choice={"type": "function", "function": {"name": "get_weather"}}
# → 强制调用 get_weather，参数由模型填写
```
适用于批量抽取（强制每条数据都调用 `extract_info`）或测试特定工具的参数填写是否正确。

**生产环境默认用 `"auto"`**，它最符合用户期望：该触发时触发，不该触发时直接回答。

---

### Q122

**模型决定是否调用工具的依据是什么？哪些因素会影响触发稳定性？**

模型的触发决策基于三个维度：

1. **用户意图与工具能力的匹配度**：用户问"今天北京天气" + `get_weather` → 高匹配 → 触发；问"Python 是什么" + `get_weather` → 无匹配 → 不触发

2. **工具 description 的清晰程度**：
   ```
   模糊："获取数据"
   → 模型不知道什么时候用，触发边界不稳定
   
   清晰："获取指定城市的当前实时天气，仅适用于当前天气查询，不支持历史天气"
   → 模型准确知道触发边界，"北京历史最冷温度"不触发，"今天北京天气"触发
   ```

3. **模型对自身知识的自信度**：基础知识问题（"GIL 是什么"）模型直接回答；实时状态问题（"今天天气"）倾向调用工具

**提高触发稳定性的方法**：
- 优化 description：在 description 中明确"适用于..."和"不适用于..."的边界
- 添加 System Prompt 指导：`"当用户询问任何当前天气时，必须先调用 get_weather 工具"`
- 设置 `temperature=0`：降低随机性，让触发行为更一致

---

### Q123

**工具调用的完整 Demo 应该包含哪些要素？如何添加日志和耗时统计？**

一个生产级 Demo 需要覆盖完整的"决策 → 执行 → 回答"闭环，并且可观测：

**核心要素**：
1. 工具定义（Schema）和执行函数分离，通过注册表（`TOOL_REGISTRY`）关联
2. 两次 API 调用结构：第一次决策（是否调用工具），第二次生成（基于工具结果回答）
3. 分发器处理工具执行，统一处理未知工具和参数错误
4. 工具错误时返回错误 dict 而不是抛异常，让模型生成友好提示

**日志和耗时统计**：

```python
import logging, time

logger = logging.getLogger(__name__)

def chat(user_input, history):
    # 第一次调用：决策
    t0 = time.monotonic()
    resp1 = client.chat.completions.create(...)
    elapsed1 = time.monotonic() - t0
    
    if not msg.tool_calls:
        logger.info("直接回答（%.2fs，无工具调用）", elapsed1)
        return msg.content, history
    
    logger.info("模型决定调用 %d 个工具（%.2fs）", len(msg.tool_calls), elapsed1)
    
    for tc in msg.tool_calls:
        t_tool = time.monotonic()
        result = TOOL_REGISTRY[tc.function.name](**args)
        logger.info("  ← 工具返回（%.3fs）：%s", time.monotonic() - t_tool, result.get("summary"))
    
    # 第二次调用：生成
    t2 = time.monotonic()
    ...
    logger.info("最终回答（%.2fs）", time.monotonic() - t2)
```

日志的核心价值：在调试时能清楚看到"模型有没有调工具、调了哪个工具、工具返回了什么、最终回答是什么"，不需要逐行 print。

---

### Q124

**多轮对话中为什么不能把 tool_calls 消息从历史里删掉？**

因为 `tool_calls` 消息（`role: assistant`）和 `tool` 消息（`role: tool`）是一对强绑定的记录——`tool` 消息里的 `tool_call_id` 必须和对应 `assistant` 消息里的 `tc.id` 一致，模型用这个 ID 配对"哪个工具结果对应哪次调用"。

```
完整消息历史（含工具调用轮次）：

[system]    "你是智能助手..."
[user]      "北京天气怎么样？"
[assistant] {tool_calls: [{id: "call_abc", name: "get_weather", args: ...}]}  ← 不能删
[tool]      {tool_call_id: "call_abc", content: "北京晴，22°C"}               ← 不能删
[assistant] "北京今天晴天，22°C，非常适合出行。"
[user]      "上海呢？"
```

**删掉会怎样**：
- 如果只保留最终 `assistant` 回答（删掉 `tool_calls` 和 `tool` 消息），下一轮模型不知道第 1 轮是通过工具获取了实时天气，可能会认为"北京晴，22°C"是模型自己的判断，而非真实数据，导致第 2 轮误用
- API 层面：如果删掉 `tool_calls` 但保留 `tool` 消息（`tool_call_id` 找不到对应的 `assistant` 消息），API 会直接报错

---

### Q125

**如何从零实现一个支持多轮对话、带日志记录的单工具调用完整 Demo？描述关键设计决策。**

核心设计：**共享消息历史 + 日志记录每步 + 错误返回 dict + 工具注册表解耦**。

```python
SYSTEM_PROMPT = "你是智能助手，可查询天气和汇率，询问实时信息时优先使用工具。"
TOOL_REGISTRY = {"get_weather": get_weather, "get_exchange_rate": get_exchange_rate}

def chat(user_input: str, history: list = None) -> tuple[str, list]:
    if history is None:
        history = [{"role": "system", "content": SYSTEM_PROMPT}]
    history.append({"role": "user", "content": user_input})
    
    # 第一次调用：让模型决策
    msg = client.chat.completions.create(
        model="deepseek-chat", messages=history,
        tools=TOOLS, tool_choice="auto", temperature=0,
    ).choices[0].message
    
    if not msg.tool_calls:
        history.append({"role": "assistant", "content": msg.content})
        return msg.content, history
    
    history.append(msg)  # 必须完整保留 tool_calls 消息
    for tc in msg.tool_calls:
        result = TOOL_REGISTRY.get(tc.function.name,
                     lambda **_: {"error": True, "message": "未知工具"}
                 )(**json.loads(tc.function.arguments))
        history.append({"role": "tool", "tool_call_id": tc.id,
                        "content": json.dumps(result, ensure_ascii=False)})
    
    # 第二次调用：生成最终回答
    answer = client.chat.completions.create(
        model="deepseek-chat", messages=history, tools=TOOLS, temperature=0,
    ).choices[0].message.content
    history.append({"role": "assistant", "content": answer})
    return answer, history

# 使用方式（多轮）
history = None
for q in ["北京今天天气？", "上海呢？两个城市哪个更热？", "1000人民币换多少美元？"]:
    answer, history = chat(q, history)
    print(f"Q: {q}\nA: {answer}\n")
```

**五个关键设计决策**：
1. **`history` 跨轮共享**：函数返回更新后的 `history`，由调用方持有并传入下一轮，实现多轮记忆
2. **`tool_calls` 完整入历史**：直接 `history.append(msg)`，保留 `tool_calls` 原始对象，防止 `tool_call_id` 对不上
3. **`TOOL_REGISTRY` 字典分发**：比 `if/elif` 可扩展，新增工具只加一行注册
4. **工具错误返回 dict**：未知工具返回 `{"error": True, ...}`，让模型生成用户友好的提示而不是程序崩溃
5. **`temperature=0`**：两次调用都设 0，保证触发行为和最终回答的一致性，方便测试

---

## 添加简单界面

### Q131

**Streamlit 和 Gradio 分别适合什么场景？LLM Demo 应该选哪个？**

两者都是把 Python 函数变成 Web 应用的框架，但侧重点不同：

| 对比维度 | Streamlit | Gradio |
|---------|-----------|--------|
| 核心定位 | 数据应用（图表、报表、Dashboard） | ML/AI 模型 Demo（输入 → 推理 → 输出） |
| 聊天界面 | 需手动用 `st.chat_message` + `st.chat_input` 搭建 | 内置 `gr.ChatInterface`，5 行代码完成 |
| 组件丰富度 | 更丰富（数据表格、图表、地图） | AI 场景专用（Image/Audio/Chatbot） |
| 学习曲线 | 略高（需理解响应式重渲染机制） | 更低（函数签名即界面） |
| 流式输出 | `st.write_stream()` | 函数用 `yield` 即可 |

**结论**：
- LLM 聊天/工具 Demo → **Gradio**（内置 ChatInterface，更少代码，更快展示）
- 数据分析/监控面板 → **Streamlit**（图表和布局更灵活）
- 两者差距不大，选你更熟悉的即可；Gradio 在 LLM Demo 场景的"入门成本"确实更低

---

### Q132

**gr.Interface 和 gr.ChatInterface 有什么区别？各适用于什么场景？**

| 维度 | gr.Interface | gr.ChatInterface |
|------|-------------|-----------------|
| 适用交互模式 | 一次性：单次输入 → 单次输出 | 多轮：保持对话历史的聊天 |
| 历史管理 | 不自动管理，每次独立 | 自动维护 `history` 列表 |
| 函数签名 | `fn(input1, input2) → output` | `fn(message, history) → reply` |
| 内置 UI | 输入/输出并排展示 | 气泡式聊天窗口 + 输入框 |
| 典型场景 | 文本摘要、情感分析、翻译 | 聊天机器人、对话式助手 |

```python
# gr.Interface：一次性处理
def summarize(text):
    return call_llm(f"总结：{text}")
gr.Interface(fn=summarize, inputs="textbox", outputs="textbox").launch()

# gr.ChatInterface：多轮对话（Gradio 自动传入 history）
def chat(message, history):
    messages = [{"role": "system", "content": "你是助手"}]
    messages += history
    messages.append({"role": "user", "content": message})
    return call_llm(messages)
gr.ChatInterface(fn=chat).launch()
```

需要**自定义布局**（比如左侧聊天 + 右侧展示工具链）时，用 `gr.Blocks`，可以自由组合 `gr.Chatbot`、`gr.Textbox`、`gr.Markdown` 等组件。

---

### Q133

**如何在 Gradio 界面里实现流式输出？和非流式有什么体验差异？**

Gradio 检测到函数是生成器（包含 `yield`）时自动开启流式模式：

```python
# 非流式：等待完整结果，文字一次性出现
def chat(message, history):
    response = client.chat.completions.create(model="deepseek-chat", messages=...)
    return response.choices[0].message.content   # return

# 流式：文字逐渐出现（打字机效果）
def chat_stream(message, history):
    stream = client.chat.completions.create(
        model="deepseek-chat", messages=...,
        stream=True,    # 关键：API 层开启流式
    )
    partial = ""
    for chunk in stream:
        delta = chunk.choices[0].delta.content
        if delta:
            partial += delta
            yield partial   # 每个 chunk 都更新显示
```

**体验差异**：
```
非流式：[等待 2 秒]……→ 完整文字突然出现
流式：  文字逐字出现……体验更流畅，用户感知的等待时间更短
```

流式输出的实际价值：长回答（>200 字）时，用户不需要盯着空白屏幕等 2–3 秒，看到第一个字就知道在响应，心理等待感显著降低。

---

### Q134

**gr.State 是什么？为什么多轮对话需要它？不用会发生什么？**

`gr.State` 是 Gradio 的**会话级持久化存储**——每个用户 Tab 独立拥有一份，跨请求保持（HTTP 请求本身是无状态的，Gradio 在 WebSocket 层为每个 Session 维护 State）。

**不用 gr.State 时**：每次用户发消息，函数收到的 `history` 都是空的——模型不记得之前的对话。

```python
# ❌ 不用 State：每次对话历史丢失
def chat(message, history):   # history 每次都是 []
    ...

# ✅ 用 gr.State：历史跨请求保持
with gr.Blocks() as demo:
    messages_state = gr.State(value=None)   # 每个用户 Session 独立的 State
    
    def respond(user_msg, state):
        if state is None:
            state = [{"role": "system", "content": "你是助手"}]
        state.append({"role": "user", "content": user_msg})
        reply = call_llm(state)
        state.append({"role": "assistant", "content": reply})
        return reply, state   # 必须 return 更新后的 state
    
    # outputs 里包含 messages_state，才能把更新后的值存回去
    btn.click(respond, [msg_input, messages_state], [output, messages_state])
```

关键规则：`gr.State` 的值通过函数的 **return** 传回，不能在函数内直接修改外部变量。

---

### Q135

**如何从零搭建一个展示工具调用链的 LLM Demo 界面？描述关键设计决策。**

核心设计：**gr.Blocks 自定义布局 + gr.State 保持历史 + 调用链格式化为 Markdown**。

```python
with gr.Blocks() as demo:
    messages_state = gr.State(value=None)   # 工作流消息历史
    chain_state = gr.State(value="")        # 累积的调用链文本
    
    with gr.Row():
        with gr.Column(scale=3):
            chatbot = gr.Chatbot(type="messages")  # 左侧：对话
            msg_input = gr.Textbox()
        with gr.Column(scale=2):
            chain_display = gr.Markdown()          # 右侧：工具调用链
    
    def respond(user_msg, chat_history, msgs, chain):
        if not msgs:
            msgs = [{"role": "system", "content": SYSTEM_PROMPT}]
        
        # 执行多步工作流（Day 25 逻辑）
        result, msgs = run_workflow(user_msg, msgs)
        
        chat_history.append({"role": "user", "content": user_msg})
        chat_history.append({"role": "assistant", "content": result.final_answer})
        
        # 把调用链格式化为 Markdown，追加到历史
        chain_md = format_chain(result.steps)   # 每步 ToolStep → Markdown 文本
        chain = (chain or "") + f"\n\n---\n\n{chain_md}"
        
        return "", chat_history, msgs, chain
    
    msg_input.submit(respond, [msg_input, chatbot, messages_state, chain_state],
                               [msg_input, chatbot, messages_state, chain_state])
    chain_state.change(lambda x: x, chain_state, chain_display)  # 同步显示
```

**四个关键设计决策**：
1. **gr.Blocks 而非 ChatInterface**：需要左右分栏（聊天 + 工具链）必须用 Blocks，ChatInterface 不支持自定义布局
2. **两个 State**：`messages_state` 保存工作流历史（给模型用），`chain_state` 保存调用链文本（给 UI 展示用），职责分离
3. **chain_state.change 绑定**：修改 State 不会自动刷新 UI，需要显式绑定 `chain_state → chain_display`
4. **format_chain 函数**：把 `WorkflowResult.steps` 格式化成可读 Markdown，包含工具名、参数、结果摘要，支持直接渲染

---

## 整合成完整作品

### Q136

**三选一项目方向（文档问答 / 客服助手 / 工具助理）各自侧重什么技术？如何选择？**

| 方向 | 核心能力侧重 | 技术复杂度 | 推荐人群 |
|------|------------|---------|---------|
| 本地文档问答助手 | RAG 全流程（Day 15–20）+ 界面 | 中 | RAG 学得比较扎实的同学 |
| 企业知识库客服助手 | RAG + Tool Calling 融合 | 高 | 有充裕时间、想展示更完整的工程能力 |
| 可调用工具的个人助理 | Tool Calling（Day 22–25）+ 界面 | 低–中 | 时间有限、或 Day 22–26 学得更扎实 |

**选择原则**：
- 选你最近几天学得最扎实的方向，不要选"看起来更酷"但没认真学的方向
- 整合日的重点是**项目结构规范化**，而非技术难度，方向不是核心
- 如果时间不够 → 方向 C（技术栈最集中，Day 22–26 的直接成果）

---

### Q137

**把多个 Day Demo 整合成一个项目时，目录结构应该怎么设计？各模块职责是什么？**

```
tool-assistant/
├── app.py              ← 入口：只做 UI 绑定，不含业务逻辑
├── core/
│   ├── config.py       ← 统一配置（读 .env，暴露 CFG 对象）
│   ├── client.py       ← LLM 客户端单例
│   └── workflow.py     ← 多步工作流主逻辑
├── tools/
│   ├── __init__.py     ← ALL_TOOLS、TOOL_REGISTRY、dispatch()
│   ├── weather.py      ← get_weather + SCHEMA
│   └── exchange_rate.py
└── tests/
    └── test_tools.py
```

**各模块职责边界**（关键是"谁知道什么"）：

| 模块 | 知道 | 不知道 |
|------|------|-------|
| `tools/` | 工具如何执行 | LLM 是谁、UI 是什么 |
| `core/workflow.py` | 如何调用 LLM 和工具 | UI 怎么展示 |
| `app.py` | UI 组件如何绑定 | 工具实现细节 |

**整合步骤**：
1. 识别各 Day Demo 的可复用模块，提取到对应目录
2. 统一配置入口（所有 `os.getenv` 改为从 `config.py` 读取）
3. 统一日志初始化（主入口 `logging.basicConfig` 一次，子模块 `logging.getLogger(__name__)`）
4. 确认模块独立可测（`python core/workflow.py` 无需启动 UI 即可运行）

---

### Q138

**.env.example 和 .gitignore 应该怎么写？为什么不能提交 .env 文件？**

**.env 不能提交的原因**：`.env` 里包含真实的 API Key，一旦推送到 GitHub（即使删掉也可能在历史记录里），Key 可能被爬虫扫描到并滥用，导致账单被盗刷或账号被封禁。

**`.env.example`（只含模板，不含真实值）**：
```bash
# .env.example — 复制为 .env 并填入真实值
DEEPSEEK_API_KEY=your_api_key_here
# MODEL=deepseek-chat     # 可选，默认使用 deepseek-chat
```

**`.gitignore`（必须包含的条目）**：
```
# 环境变量（绝对不能提交）
.env

# Python 缓存
__pycache__/
*.pyc
*.pyo

# 编辑器文件
.DS_Store
.idea/
.vscode/

# 依赖目录（应通过 requirements.txt 安装）
venv/
.venv/
```

**操作流程**：
1. 先提交 `.env.example` 到 git（让别人知道要配置什么）
2. 创建 `.gitignore` 排除 `.env`
3. 用户克隆后：`cp .env.example .env`，填入真实 key

---

### Q139

**一个可展示的 LLM 项目 README 需要包含哪些章节？快速启动指南有哪些常见疏漏？**

**必要章节**：
```
# 项目名称
一句话描述

## 功能
- ✅ 支持天气查询
- ✅ 支持货币汇率换算

## 快速开始（最重要的章节）
...

## 技术栈
- Python 3.10+、DeepSeek API、Gradio

## 项目结构
（目录树 + 一句话说明）

## 示例截图
（至少一张，面试时的关键）
```

**快速启动指南的 5 个常见疏漏**：

| 疏漏 | 后果 | 修复方法 |
|------|------|---------|
| 没有 `.env.example` | 别人不知道要设置哪些环境变量 | 提交模板文件 |
| `requirements.txt` 没锁版本 | 安装到不兼容版本 | 加 `>=x.y` 约束 |
| 没说明 Python 版本 | 3.8 不支持 f-string 某些语法 | 在 README 开头注明 "Python 3.10+" |
| 没有示例输入 | 别人不知道输入什么才能看到效果 | 列 3 个典型测试问题 |
| 没有截图 | 面试展示时需要临时运行，有风险 | 截图存 `screenshots/`，引入 README |

---

### Q140

**如何从零搭建一个"可调用工具的个人助理"完整项目？描述模块划分和关键实现。**

核心设计：**tools 独立 + core 封装工作流 + app.py 纯 UI + 统一配置**。

**模块划分**：
```
tools/          → 工具实现（可独立运行，不依赖 LLM）
core/config.py  → 统一配置（CFG dataclass，frozen=True 防止误修改）
core/client.py  → LLM 客户端单例（避免每次 import 都创建新客户端）
core/workflow.py → 多步 for 循环 + WorkflowResult 记录
app.py          → Gradio Blocks（左侧聊天 + 右侧工具链 + 两个 State）
```

**关键实现要点**：

```python
# 1. 配置单例（frozen dataclass 防止意外修改）
CFG = Config(api_key=os.getenv("DEEPSEEK_API_KEY"), ...)

# 2. 客户端单例（避免重复创建）
_client = None
def get_client():
    global _client
    if _client is None:
        _client = OpenAI(api_key=CFG.api_key, base_url=CFG.base_url)
    return _client

# 3. 工作流：for-else 区分正常退出和超限退出
for _ in range(CFG.max_iterations):
    msg = get_client().chat.completions.create(...).choices[0].message
    if not msg.tool_calls:
        break
    # 执行工具...
else:
    # 超出上限，强制生成

# 4. UI：两个 State，职责分离
messages_state = gr.State(value=None)   # 工作流历史（给模型用）
chain_state = gr.State(value="")        # 调用链文本（给展示用）
```

**三个关键验证点**：
1. `python tools/weather.py` 直接运行 → 工具可独立测试
2. `python core/workflow.py` 直接运行 → 工作流无需 UI 即可验证
3. `python app.py` + 浏览器测试 3 种问题（0/1/2 步工具）→ 全流程正确
5. **`temperature=0`**：两次调用都设 0，保证触发行为和最终回答的一致性，方便测试

---

## 补工程细节

### Q141

**LLM 项目中异常处理的"分层设计"是什么？各层应该如何处理各自的异常？**

分层设计指不同代码层只处理自己能解决的异常，不能处理的向上抛或降级。

| 层 | 位置 | 处理策略 | 错误表现 |
|---|---|---|---|
| 工具层 | `tools/*.py` | `return {"error": True, "message": "..."}` | 工具返回错误 dict，模型生成友好回复 |
| 工作流层 | `core/workflow.py` | 捕获 API 异常（超时/限流/认证），可重试则重试 | 返回错误文案，terminated_by="error" |
| UI 层 | `app.py` | `try/except Exception` 兜底 | 用户看到友好文案，不显示 traceback |

**工具层不抛异常的原因**：工作流用 for 循环驱动，工具抛异常会中断整个循环；返回 error dict 让模型能基于错误信息生成自然语言提示（"这个城市暂不支持，您是想查哪里？"）。

**工作流层重试策略**：
```python
except RateLimitError:
    time.sleep(5 * (attempt + 1))   # 指数退避
    continue
except AuthenticationError:
    return WorkflowResult(final_answer="API Key 验证失败", terminated_by="auth_error")
    # 认证错误无需重试，直接返回
```

### Q142

**给 LLM 项目加日志时，应该记录哪三类关键信息？各自的内容是什么？**

| 日志类型 | 时机 | 记录内容 | 用途 |
|---------|------|---------|------|
| **启动日志** | 程序启动时 | 模型名称、API Base URL、Key 前几位（脱敏）、最大迭代次数 | 确认配置正确、定位环境问题 |
| **请求日志** | 每次 API 调用后 | 耗时（秒）、input_tokens、output_tokens、total_tokens | 性能分析、成本监控、发现异常高消耗 |
| **错误日志** | 异常发生时 | 工具名称、参数、异常类型 + `logger.exception()` 自动附加完整 traceback | 无需重现问题即可排查原因 |

**关键细节**：
- API Key **绝对不能完整打印**到日志，只打印前 8 位作为验证标识
- 使用 `logger.exception()` 而不是 `logger.error()`，前者自动附加完整异常链（traceback）
- `logging.basicConfig` 只在主入口调用**一次**；子模块用 `logging.getLogger(__name__)` 继承

### Q143

**什么是"fail-fast"？配置验证为什么要在启动时而不是请求时做？**

**Fail-fast**：系统在检测到错误配置时立即停止执行，而不是等到运行时才暴露错误。

| 对比 | Fail-slow（不做配置验证） | Fail-fast（启动时验证） |
|-----|------------------------|----------------------|
| 错误发现时机 | 用户发第一条消息时崩溃 | 程序启动 5 秒内报错退出 |
| 错误定位 | 深层异常，难以识别是配置问题 | 明确的"DEEPSEEK_API_KEY 未设置"提示 |
| 成本风险 | 若多次请求后才报错，可能已消耗 Token | 0 成本发现配置错误 |
| 用户体验 | 看到 Python traceback | 看到"请检查 .env 文件"的友好提示 |

**实现模式**：
```python
def _load() -> Config:
    errors = []
    key = os.getenv("DEEPSEEK_API_KEY", "").strip()
    if not key:
        errors.append("DEEPSEEK_API_KEY 未设置")
    if errors:
        print("❌ 配置错误：" + "；".join(errors))
        sys.exit(1)   # fail-fast：立即退出
    return Config(api_key=key, ...)

CFG = _load()   # 模块 import 时就执行，最早被触发
```

### Q144

**工具调用的输出校验（output validation）应该验证什么？如何实现？**

输出校验防止"脏数据"（格式错误的数据）传入下一层。应在三处做校验：

**1. 工具返回值校验**（工作流层接收工具结果时）：
```python
def validate_tool_result(name: str, result: dict) -> dict:
    if not isinstance(result, dict):
        return {"error": True, "message": "工具返回格式错误"}
    if result.get("error"):
        return result  # 已经是合法错误 dict，直接透传
    missing = [f for f in REQUIRED_FIELDS.get(name, []) if f not in result]
    if missing:
        logger.warning("工具 '%s' 返回缺少字段：%s", name, missing)
    return result
```

**2. LLM 结构化输出校验**（需要模型返回 JSON 时）：
- 使用 `response_format={"type": "json_object"}` 约束格式
- 用 Pydantic `model_validate_json()` 验证字段
- 校验失败时降级返回默认值，不崩溃

**3. 工作流整体输出校验**：
```python
if not wf.final_answer.strip():
    wf.final_answer = "未能生成回答，请重新提问。"
```

### Q145

**如何从零给一个 LLM 项目补全"异常处理 + 日志 + 配置验证 + 输出校验"四件套？**

按以下顺序改造，优先做影响最大的：

**Step 1：配置验证（改 `core/config.py`）**
- 把 `Config` 加载逻辑包在 `_load()` 函数里
- 收集所有配置错误（不要发现一个就退出，收集全部后一次性报错）
- `if errors: sys.exit(1)`，模块级调用 `CFG = _load()`

**Step 2：日志初始化（改 `app.py` 主入口）**
- 在 `if __name__ == "__main__":` 前调用 `setup_logging()`
- `httpx`/`httpcore` 降级为 WARNING 避免刷屏
- 各子模块添加 `logger = logging.getLogger(__name__)`

**Step 3：API 异常处理（改 `core/workflow.py`）**
- 把 API 调用包在 `_call_api()` 函数里
- `RateLimitError` → 重试 3 次（指数退避）
- `AuthenticationError` → 立即返回错误，不重试
- `APITimeoutError/APIConnectionError` → 抛 `ConnectionError`，上层捕获

**Step 4：工具层（改 `tools/*.py`）**
- 所有工具用 `try/except` 包裹，返回 `{"error": True, "message": "..."}` 而非抛异常
- 在 `dispatch()` 里加 `TypeError` 捕获（参数不匹配）和兜底 `Exception`

**Step 5：UI 兜底（改 `app.py` 的 respond 函数）**
- `try/except Exception` 包裹整个业务调用
- 把技术错误转成用户友好文案

**验证顺序**：先删 API Key 测 fail-fast → 再 mock RateLimitError 测重试 → 再传错工具参数测工具兜底 → 最后整体跑 5 次对话验日志输出。

---

## 测试和优化

### Q146

**LLM 应用测试和普通软件测试有哪些核心区别？**

| 维度 | 普通软件测试 | LLM 项目测试 |
|-----|------------|------------|
| 正确性判断 | `assertEqual`（精确匹配） | 语义/结构/功能匹配（无法精确断言） |
| 稳定性前提 | 默认稳定（相同输入=相同输出） | 需要多次运行验证（概率性输出） |
| 评估方式 | 全自动化断言 | 规则检查 + 人工抽样 |
| 失败模式 | 崩溃或错误返回（可检测） | 生成了错误内容（幻觉、漏召回、误触发，难以自动检测） |
| 边界 | 已知边界（数据类型、长度） | 语义边界难以穷举 |

**核心结论**：LLM 测试的目标不是"100% 通过"，而是**量化"在预期范围内失败的概率"**并把它降到可接受水平。

**四个测试维度**：输出稳定性（一致率）、工具触发准确率（Precision/Recall）、检索准确率（Hit Rate/MRR）、成本合理性（tokens/轮）。

### Q147

**如何测试工具调用的"触发准确率"？精确率和召回率分别测什么？**

**精确率（Precision）**：触发的工具是不是都对的？防止**误触发**。

```
Precision = 正确触发次数 / 总触发次数
（"触发了的里面有多少是应该触发的"）
```

**召回率（Recall）**：应该触发的工具是不是都触发了？防止**漏触发**。

```
Recall = 正确触发次数 / 应该触发的总次数
（"应该触发的里面有多少被触发了"）
```

**F1 综合指标**：`2×P×R / (P+R)`，平衡精确率和召回率。

**测试实现思路**：
1. 准备测试用例，每条标注 `expected_tools`（应触发）和 `should_not_call`（不应触发）
2. 实际运行，收集 `called` 工具集合
3. `missed = expected - called`（漏触发），`wrong = called & forbidden`（误触发）
4. 统计 tp / fp / fn，计算 Precision / Recall / F1

**典型问题与修复**：
- Precision 低（误触发多）→ 收紧 description，加"何时不用"的限定
- Recall 低（漏触发多）→ 扩展 description，补别名/同义词

### Q148

**什么是输出稳定性测试？temperature=0 时输出一定稳定吗？**

**稳定性**：相同问题多次运行时，**核心行为是否一致**——重点是工具触发行为和回答功能，而非措辞完全相同。

**temperature=0 的实际稳定性**：

`temperature=0` 使用贪心解码（每步选概率最高的 token），理论上是确定性的。但实践中仍有不稳定来源：

| 不稳定来源 | 说明 |
|---------|-----|
| 工具参数差异 | 模型有时传 `{"city": "北京"}`，有时传 `{"city": "Beijing"}`，导致工具结果不同 |
| 商业 API 差异 | DeepSeek 等服务内部可能有负载均衡或模型版本更新 |
| 工具本身随机性 | 真实 API（天气/股票）每次返回数据不同，影响后续生成 |

**测试方法**：相同问题跑 3–5 次，对比每次的工具触发序列（`[s.name for s in wf.steps]`），如果结果集合大小 > 1，则判定为不稳定。

### Q149

**如何分析一次完整对话的 Token 成本？有哪些典型的成本增长陷阱？**

**Token 消耗结构**：
```
input_tokens = System Prompt + 对话历史（所有轮次）
             + 工具 Schema（ALL_TOOLS 的 JSON） + 当前消息

output_tokens = 模型回复文字 + tool_calls JSON
```

**工具 Schema 隐性成本**：3 个工具的 Schema 通常占 300–600 tokens，每次请求都要发送。

**两大成本陷阱**：

1. **历史未截断**：多轮对话中历史消息线性增长，第 10 轮时 input_tokens 可能是第 1 轮的 10 倍。修复：用 `trim_history(messages, max_turns=5)` 保留最近 N 轮。

2. **工作流循环失控**：多步工作流每次循环都重新发送完整历史，10 次循环的 Token 消耗是 3 次的 3 倍以上。修复：`max_iterations` 限制 + 超时退出。

**成本计算**：
```python
cost_usd = (input_tokens * 0.14 + output_tokens * 0.28) / 1_000_000
# DeepSeek chat 定价（非缓存，截至 2025）
```

### Q150

**如何从零设计一份 LLM 项目的测试记录表？包含哪些维度和字段？**

测试记录表应包含**元数据、用例详情、汇总指标、改进计划**四部分：

**元数据**：测试时间、项目版本、模型名称、temperature 值。

**用例详情表**（每条一行）：

| 字段 | 说明 |
|-----|------|
| 编号 | 唯一序号（便于引用） |
| 测试问题 | 完整原文 |
| 预期行为 | 应调工具 + 回答方向 |
| 实际行为 | 实际调用工具 + 回答摘要 |
| 结果 | ✅/❌ |
| 问题/备注 | 失败原因或特殊观察 |

**汇总指标**：通过率、工具触发精确率/召回率/F1、平均 Token 消耗、平均耗时。

**改进计划**（P0–P3 优先级）：

```
P0（立即修）：影响核心功能 + 每次都复现
P1（本次修）：影响核心功能 + 多数复现，或影响体验 + 每次都复现
P2（下次修）：偶发或影响体验
P3（记录）：已知问题，可接受
```

一份好的测试记录表的价值在于：让"发现问题 → 修复 → 验证改善"形成闭环，而不是测一次就结束。

---

## 整理作品与总结

### Q151

**一个"别人可以直接运行"的项目 README 需要包含哪些核心章节？**

必须包含的四个部分：

| 章节 | 作用 | 失败表现 |
|-----|-----|---------|
| **项目介绍** | 30 秒内让读者知道这是什么 | "智能助手"等无信息量描述 |
| **功能列表** | 具体说能做什么 | "支持多种功能"等抽象描述 |
| **快速开始** | 安装→配置→运行，3 步内 | 步骤超 5 步 / 没有预期输出 |
| **示例** | 真实输入/输出 | 用文字描述代替真实对话 |

可选加分项：技术架构图、FAQ、.env.example 说明、技术栈表格。

**黄金标准**：陌生开发者按 README 操作，**5 分钟内**能发出第一条消息。验证方法：自己清空配置重跑一遍，或让朋友操作并计时。

### Q152

**如何写"快速开始"章节，让陌生开发者 5 分钟内跑起来？**

**三步原则**：快速开始必须是三步以内的可执行命令：

```bash
# Step 1：安装
git clone <url> && cd <repo> && pip install -r requirements.txt

# Step 2：配置
cp .env.example .env   # 填入 API Key

# Step 3：运行
python app.py          # 访问 http://127.0.0.1:7860
```

**关键：每步都要写预期结果**，让用户能自检：
- Step 1 ✅ 预期：`Successfully installed` 若干包，无红色报错
- Step 2 ✅ 预期：`.env` 中 `DEEPSEEK_API_KEY=sk-xxxxx`（非空）
- Step 3 ✅ 预期：终端输出 `Running on local URL: http://127.0.0.1:7860`

**五种常见疏漏**：
1. 未写 Python 版本要求
2. 没有 `.env.example`（用户不知道需要配什么变量）
3. `requirements.txt` 未锁定版本（安装了不兼容版本）
4. 未说明运行入口文件
5. 示例不真实（截图与实际不符）

### Q153

**示例输入输出为什么比功能描述更有说服力？如何选择展示哪些示例？**

**为什么示例更有力**：功能描述是抽象的，示例是具体的。

```
功能描述（无信息量）：
  "支持实时天气查询和货币换算，可处理复合问题"

示例（直接展示能力）：
  用户：上海明天适合出门吗？顺便帮我算 500 欧元换多少人民币
  助手：上海明天多云，气温 22–28°C，适合出门。
       500 欧元约等于 3,920 人民币（1 EUR = 7.84 CNY）。
```

示例让读者立刻知道：系统能处理复合问题、回答是自然语言、工具调用是自动推断的。

**三类必选示例**：

| 类型 | 示例内容 | 展示的能力 |
|-----|---------|----------|
| 核心功能 | 单工具调用（天气 or 汇率） | 最基本功能正常工作 |
| 差异化亮点 | 多工具串行 + 工具链可视化截图 | 项目最有特色的能力 |
| 边界处理 | 不触发工具的普通聊天 | 系统足够智能，不滥用工具 |

### Q154

**30 天 LLM 应用开发学习中，最重要的五个认知转变是什么？**

| 认知转变 | 起点认知 | 终点认知 |
|---------|---------|---------|
| **API调用→工程系统** | 填 Key 发消息就行 | LLM 是系统组件，需要配置管理、异常处理、日志监控 |
| **Prompt=咒语→规格** | Prompt 越华丽越好 | Prompt 是规格说明，需精简、明确边界、可迭代 |
| **模型万能→RAG补充** | 直接问模型即可 | 私有/实时知识需 RAG：先检索段落，再让模型基于段落回答 |
| **工具=插件→能力边界** | 工具是附加功能 | 工具 description 的精确程度决定模型能否在正确时机使用 |
| **能跑→可维护** | Demo 能跑就算完成 | fail-fast / 分层异常 / 日志 / 测试——这些决定项目能否被他人维护 |

### Q155

**一个 LLM 项目从 Demo 到生产级别还需要补充哪些关键能力？**

按三个方向分类：

**路线 A：深化 Agent 能力**
- LangGraph：图结构定义复杂 Agent 流程（分支、循环、多 Agent 协作）
- MCP（Model Context Protocol）：标准化工具接口协议
- 长期记忆：跨对话记忆（向量存储 + 摘要压缩）

**路线 B：提升 RAG 系统质量**
- 多路召回：向量检索 + BM25 关键词检索，覆盖更多场景
- Reranker：Cross-Encoder 重排序，精度提升 10–20%
- RAGAS：自动化 RAG 质量评估框架

**路线 C：生产部署**

| 能力 | 工具/方案 | 解决的问题 |
|-----|---------|---------|
| 可观测性 | LangFuse / LangSmith | 追踪每次 LLM 调用，定位慢查询 |
| 成本控制 | Prompt 缓存、模型分级路由 | 降低 API 费用 40–80% |
| 安全 | Prompt 注入防护、输出护栏 | 防止越权使用和有害输出 |
| 部署 | Docker + Hugging Face Spaces | 让他人无需配置环境即可使用 |

**判断优先级**：做完 30 天后，看哪个部分让你最不满意——那就是下一阶段的重点方向。

---

## Day 1–7 补充考点（Q156–Q174）

### Q156

**Top-P（核采样）和 Top-K 采样是什么？和 Temperature 有什么关系？**

三个参数都影响模型从概率分布中采样的方式，作用层不同：

| 参数 | 作用 | 典型值 |
|-----|------|------|
| Temperature | 在采样前对 logits 做缩放，拉平或拉尖整体分布 | 0（确定性）~ 1.5（创意） |
| Top-K | 只从概率最高的 K 个 token 中采样，其余直接截断 | 40~100 |
| Top-P | 累积概率达到 P 之前的 token 集合中采样（动态 K） | 0.9~0.95 |

**执行顺序**：Temperature 缩放 → Top-K 截断 → Top-P 截断 → 按剩余概率分布采样。

**核心区别**：
- Top-K 是固定数量截断：词表丰富时保留太多，词表集中时保留太少。
- Top-P 是动态截断：概率集中时集合小（接近贪心），概率分散时集合大，天然适应上下文。

**实践结论**：DeepSeek/OpenAI API 默认 Top-P=0.95，生产代码中通常只调 Temperature，不需要同时修改 Top-P 和 Top-K——同时修改三个参数的组合效果很难预测，易导致输出不稳定。

---

### Q157

**Zero-shot、One-shot、Few-shot 分别是什么？In-context Learning 的本质是什么？**

| 方式 | 定义 | 示例 |
|-----|------|------|
| Zero-shot | Prompt 里只有任务描述，没有示例 | "请将以下句子翻译成英文：xxx" |
| One-shot | 给一个示例 | "中文：你好 → 英文：Hello\n中文：xxx → 英文：" |
| Few-shot | 给多个示例（通常 3–8 个） | 多组"输入 → 输出"对 |

**In-context Learning（上下文学习）的本质**：模型并没有真正"学习"（权重没有更新），而是在推理时从 Prompt 中的示例里识别出任务的模式，并将其应用到新输入。本质是**模式识别和类比推理**，而非梯度更新。

**工程启发**：
- 结构化任务（JSON 输出、固定格式）用 Few-shot 效果远好于 Zero-shot。
- 示例选择比数量更重要：示例应覆盖典型情况和边界情况，3 个精准示例 > 10 个随机示例。
- Few-shot 示例消耗 Token，要纳入成本核算。

---

### Q158

**Base Model 和 Chat Model（Instruct Model）有什么区别？应用开发中应该用哪种？**

| 维度 | Base Model | Chat / Instruct Model |
|-----|-----------|----------------------|
| 训练目标 | 预测下一个 token（自回归语言模型） | 在 Base Model 上做 SFT + RLHF，学会遵循指令 |
| 输入格式 | 原始文本续写 | messages 格式（system/user/assistant） |
| 对话能力 | 不理解"角色"，不会主动停止生成 | 理解对话结构，能回答问题、拒绝请求、保持角色 |
| 典型用途 | 研究、Fine-tune 起点 | 95% 的应用开发场景 |

**应用开发中应该用 Chat Model**，原因：
1. Chat Model 内置了"按指令行事"的能力，Prompt Engineering 效果可预期。
2. Base Model 的输出是"续写"语义，对话格式 Prompt 可能被当作续写材料，行为不可控。
3. API 调用（OpenAI / DeepSeek）的 `/chat/completions` 端点本身就是 Chat Model 专用接口。

---

### Q159

**Token 优化有哪些常见策略？从 Prompt 设计、历史管理、模型选择三个角度分析。**

**Prompt 设计层（压缩输入 Token）**：

| 策略 | 效果 | 注意点 |
|-----|------|------|
| 删除冗余描述 | 输入减少 20–40% | 不能删除示例和约束，否则质量下降 |
| 用表格替代长段落描述 | Token 减少 30% | 表格适合结构化信息，叙事场景不适用 |
| System Prompt 缓存（API 支持） | 重复调用成本降至 10% | 仅适用于不变的 System Prompt |

**历史管理层（控制多轮增长）**：

```python
# 滑动窗口：只保留最近 N 轮
history = history[-N*2:]  # 每轮 = user + assistant 各 1 条

# 摘要压缩：超限时用 LLM 压缩旧历史（更贵但保留语义）
if count_tokens(history) > THRESHOLD:
    summary = summarize(history[:-4])  # 保留最近 2 轮原文
    history = [{"role": "system", "content": summary}] + history[-4:]
```

**模型选择层（降低单次调用成本）**：
- 简单任务（分类、提取短字段）用轻量模型（如 DeepSeek-R1-Lite）。
- 复杂推理、长文本生成才用旗舰模型。
- 成本差距通常在 5–20 倍之间，合理分级可节省 60% 以上费用。

**优化优先级**：历史管理 > Prompt 精简 > 模型分级。历史管理是多轮对话成本最大的变量，应最先解决。

---

### Q160

**requirements.txt 的作用是什么？如何生成和使用它？**

`requirements.txt` 记录项目依赖的 Python 包及其版本，是**环境可复现**的基础——保证任何人在任意机器上能用 `pip install -r requirements.txt` 还原完全一致的运行环境。

**生成方式**：

```bash
# 方式 1：导出当前虚拟环境所有已安装包（含间接依赖）
pip freeze > requirements.txt

# 方式 2：手动维护（只写直接依赖，更干净）
# requirements.txt 内容示例：
openai>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
```

**两种方式的取舍**：

| 方式 | 优点 | 缺点 |
|-----|------|------|
| `pip freeze` | 完全精确，100% 可复现 | 包含大量间接依赖，难以阅读和维护 |
| 手动维护 | 干净，只列直接依赖 | 间接依赖版本不受控，可能在不同环境产生差异 |

**工程建议**：手动维护 `requirements.txt`（只写直接依赖），同时用 `pip freeze > requirements.lock` 锁定精确版本（类似 `package-lock.json`）。提交时提交 `requirements.txt`，锁文件可选。

**使用**：

```bash
pip install -r requirements.txt   # 安装所有依赖
pip install -r requirements.txt --upgrade  # 升级到最新兼容版本
```

---

### Q161

**pathlib.Path 和 os.path 有什么区别？为什么推荐在新项目中使用 pathlib？**

`pathlib`（Python 3.4+）是面向对象的路径库，`os.path` 是基于字符串的函数库。

**核心区别**：

```python
import os
from pathlib import Path

# os.path：函数式，多步操作需嵌套
config_path = os.path.join(os.path.dirname(os.path.abspath(__file__)), "config.json")
exists = os.path.exists(config_path)

# pathlib：链式操作，可读性强
config_path = Path(__file__).parent / "config.json"
exists = config_path.exists()
```

**pathlib 的主要优势**：

| 场景 | os.path | pathlib.Path |
|-----|---------|-------------|
| 路径拼接 | `os.path.join(a, b, c)` | `a / b / c` |
| 读取文件 | `open(str(path))` | `path.read_text(encoding="utf-8")` |
| 获取父目录 | `os.path.dirname(p)` | `p.parent` |
| 获取文件名 | `os.path.basename(p)` | `p.name` |
| 获取扩展名 | `os.path.splitext(p)[1]` | `p.suffix` |
| 跨平台兼容 | 需手动处理分隔符 | 自动处理 |

**在 LLM 项目中的典型用法**：

```python
from pathlib import Path

BASE_DIR = Path(__file__).parent
DATA_DIR = BASE_DIR / "data"
LOG_FILE = BASE_DIR / "logs" / "app.log"

# 遍历所有 txt 文件
for doc in DATA_DIR.glob("*.txt"):
    content = doc.read_text(encoding="utf-8")
```

**推荐 pathlib** 的根本原因：代码更短、意图更清晰，且 `Path` 对象可直接传给大多数 Python 标准库函数（`open()`、`json.load()` 等），不需要 `str()` 转换。

---

### Q162

**Python with 语句（上下文管理器）的原理是什么？在 LLM 项目中有哪些典型应用？**

`with` 语句是**上下文管理器**协议的语法糖。进入 `with` 块时调用 `__enter__`，退出时（无论正常还是异常）调用 `__exit__`。核心价值：**资源获取和释放必然配对，不会因异常而遗漏清理**。

**原理**：

```python
# with obj as x: 等价于：
x = obj.__enter__()
try:
    # with 块内容
finally:
    obj.__exit__(exc_type, exc_val, exc_tb)
```

**LLM 项目中的典型应用**：

| 场景 | 示例 |
|-----|------|
| 文件操作 | `with open("data.txt", encoding="utf-8") as f:` — 文件必然关闭 |
| 数据库连接 | `with sqlite3.connect("db.sqlite") as conn:` — 自动提交/回滚 |
| 计时器（性能测量） | 自定义 `with Timer() as t:` 测量 API 调用耗时 |
| 临时修改环境 | `with patch.dict(os.environ, {"API_KEY": "test"}):` 用于测试 |
| 线程锁 | `with lock:` — 临界区自动加锁/解锁 |

**LLM 项目中最常用的是文件读写**：

```python
# 推荐写法
with open(Path("data") / "input.txt", encoding="utf-8") as f:
    text = f.read()

# 等价但不推荐（可能忘记 close，异常时文件描述符泄漏）
f = open("input.txt")
text = f.read()
f.close()
```

---

### Q163

***args 和 \*\*kwargs 分别是什么？在封装 LLM 客户端时有哪些典型用途？**

- `*args`：收集所有位置参数，组成一个 **tuple**。
- `**kwargs`：收集所有关键字参数，组成一个 **dict**。

**透传模式（最常见用法）**：封装 LLM 客户端时，用 `**kwargs` 把调用方传入的任意参数透传给底层 SDK，避免每次 SDK 新增参数就要改封装层：

```python
class LLMClient:
    def chat(self, messages: list, **kwargs) -> str:
        """**kwargs 透传 temperature、max_tokens、top_p 等参数"""
        response = self.client.chat.completions.create(
            model=self.model,
            messages=messages,
            **kwargs  # 调用方可以传任意 SDK 支持的参数
        )
        return response.choices[0].message.content
```

**调用时**：

```python
client.chat(messages, temperature=0.3, max_tokens=500)  # temperature、max_tokens 进入 kwargs
```

**与固定参数组合使用**：

```python
def retry_call(func, *args, max_retries=3, **kwargs):
    """max_retries 是有默认值的关键字参数，*args/**kwargs 透传给 func"""
    for i in range(max_retries):
        try:
            return func(*args, **kwargs)
        except Exception:
            if i == max_retries - 1:
                raise
```

**注意**：`*args` 和 `**kwargs` 是约定命名，可以用任意名字（如 `*extra`、`**options`），但遵守惯例可读性更好。

---

### Q164

**Python 类型注解（Type Hints）的作用是什么？在 LLM 应用代码中为什么值得加？**

类型注解（PEP 484，Python 3.5+）是给变量、函数参数和返回值标注期望类型的**文档化约定**——Python 运行时不检查，但工具链（IDE、mypy、Pydantic）可以利用它进行静态分析和自动补全。

**类型注解的主要价值**：

| 价值 | 说明 |
|-----|------|
| IDE 自动补全 | 标注了 `messages: list[dict]`，IDE 知道元素是 dict，能提示 `.get()` 等方法 |
| 静态类型检查 | `mypy` 可在运行前发现类型错误（如把 int 传给期望 str 的函数） |
| 代码即文档 | 函数签名直接说明输入输出类型，不需要另写注释 |
| Pydantic 集成 | Pydantic Model 用类型注解自动生成验证逻辑和 JSON Schema |

**在 LLM 项目中的典型写法**：

```python
from typing import Optional
from openai import OpenAI

def call_llm(
    messages: list[dict],
    model: str = "deepseek-chat",
    temperature: float = 0.7,
    max_tokens: Optional[int] = None,
) -> str:
    """Optional[int] 表示可以是 int 或 None"""
    client = OpenAI(base_url="https://api.deepseek.com/v1", api_key=...)
    resp = client.chat.completions.create(
        model=model, messages=messages,
        temperature=temperature, max_tokens=max_tokens,
    )
    return resp.choices[0].message.content
```

**为什么在 LLM 代码中值得加**：LLM 项目中 `messages` 格式（`[{"role": str, "content": str}]`）是最容易传错的地方。加了类型注解后，IDE 会在传入 `str` 而不是 `list[dict]` 时立即报警，避免运行时 API 报 400 错误。

---

### Q165

**HTTP 请求的基本结构是什么？请求头中哪些字段在 LLM API 调用中最重要？**

HTTP 请求由四部分组成：

```
请求行：POST /v1/chat/completions HTTP/1.1
请求头：Host: api.deepseek.com
        Authorization: Bearer sk-xxx
        Content-Type: application/json
        (空行)
请求体：{"model": "deepseek-chat", "messages": [...]}
```

**LLM API 调用中最重要的三个请求头**：

| 请求头 | 作用 | 错误后果 |
|-------|------|---------|
| `Authorization: Bearer <API_KEY>` | 身份认证 | 401 Unauthorized |
| `Content-Type: application/json` | 告知服务器请求体格式 | 400 Bad Request（服务器无法解析） |
| `Accept: text/event-stream` | 声明接受 SSE 格式（流式输出时） | 流式输出失败，返回非流式响应 |

**OpenAI SDK 自动处理这些头**，不需要手动设置。但在用 `requests` 或 `httpx` 直接调用 API 时，必须手动添加：

```python
import requests

response = requests.post(
    "https://api.deepseek.com/v1/chat/completions",
    headers={
        "Authorization": f"Bearer {api_key}",
        "Content-Type": "application/json",
    },
    json={"model": "deepseek-chat", "messages": [...]},
)
```

---

### Q166

**什么是指数退避重试？为什么比固定间隔重试更好？如何实现？**

**指数退避（Exponential Backoff）**：每次重试前等待时间按指数增长（如 1s → 2s → 4s），而不是固定等待（每次都等 1s）。通常加上随机抖动（jitter）避免多个客户端同时重试造成"惊群效应"。

**为什么比固定间隔更好**：

| 场景 | 固定间隔 1s 重试 | 指数退避 |
|-----|--------------|---------|
| 服务器瞬时过载 | 100 个客户端每秒同时重试，加剧过载 | 等待时间随机分散，错峰重试 |
| 网络中断 10s | 重试 10 次全部失败，浪费 10 次请求 | 总等待时间 1+2+4=7s 后放弃，减少无效请求 |
| 429 Rate Limit | 可能继续触发限流 | 给服务器恢复窗口 |

**实现**：

```python
import time
import random
from openai import RateLimitError, APIError

def call_with_retry(client, messages: list, max_retries: int = 3) -> str:
    for attempt in range(max_retries):
        try:
            resp = client.chat.completions.create(
                model="deepseek-chat", messages=messages
            )
            return resp.choices[0].message.content
        except RateLimitError:
            if attempt == max_retries - 1:
                raise
            wait = (2 ** attempt) + random.uniform(0, 1)  # 1~2s, 2~3s, 4~5s
            time.sleep(wait)
        except APIError as e:
            if e.status_code in (400, 401):  # 不可重试
                raise
            if attempt == max_retries - 1:
                raise
            time.sleep(2 ** attempt)
```

**关键设计**：401（认证失败）和 400（请求格式错误）是客户端错误，重试没有意义，应立即失败；429 和 5xx 才是应该重试的服务端问题。

---

### Q167

**API 返回的 JSON 如何安全解析？json.loads() 和 response.json() 有什么区别？**

**两者的区别**：

| 方式 | 适用场景 | 内部行为 |
|-----|---------|---------|
| `json.loads(text)` | 已经拿到字符串（如 LLM 返回的 content） | 直接解析字符串 |
| `response.json()` | requests/httpx 的 Response 对象 | 先读取响应体字节流，再解析 JSON |

**为什么需要"安全"解析 LLM 输出的 JSON**：LLM 不总是输出纯 JSON，常见的"污染"：

```
```json
{"name": "张三"}
```
```

或者多余的解释文字："根据你的要求，以下是结果：{"name": "张三"}"

**健壮解析函数**：

```python
import json
import re
from typing import Optional

def parse_json_safe(text: str) -> Optional[dict]:
    """处理 LLM 输出中的常见 JSON 格式问题"""
    text = text.strip()
    # 去掉 ```json ... ``` 包装
    match = re.search(r"```(?:json)?\s*([\s\S]*?)\s*```", text)
    if match:
        text = match.group(1)
    # 尝试直接解析
    try:
        return json.loads(text)
    except json.JSONDecodeError:
        pass
    # 尝试提取第一个 { } 块
    match = re.search(r"\{[\s\S]*\}", text)
    if match:
        try:
            return json.loads(match.group())
        except json.JSONDecodeError:
            pass
    return None
```

**注意**：`response.json()` 在 requests 里解析失败会抛 `json.JSONDecodeError`，调用前应确认 `response.status_code == 200` 且 `Content-Type: application/json`。

---

### Q168

**流式输出时为什么必须等所有 chunk 接收完才能把 assistant 消息加入历史？**

流式输出（Streaming）时，模型输出分多个 chunk 逐段返回，每个 chunk 只包含部分文本。如果在收到第一个 chunk 后就把不完整的消息加入历史，会导致两个问题：

**问题 1：消息内容不完整**

```
chunk1: "这道题的答案"
chunk2: "是 42，原因是"
chunk3: "宇宙的答案。"

如果只加了 chunk1，历史里是 "这道题的答案"
→ 下一轮对话的上下文是错误的
```

**问题 2：引用未完成的 assistant 消息**

若第 2 轮 user 问"你刚才说的原因是什么？"，模型只能看到截断的历史，会产生幻觉或混乱回答。

**正确实现**：

```python
def stream_chat(client, messages: list) -> str:
    full_content = ""
    stream = client.chat.completions.create(
        model="deepseek-chat",
        messages=messages,
        stream=True,
    )
    for chunk in stream:
        delta = chunk.choices[0].delta.content or ""
        full_content += delta
        print(delta, end="", flush=True)  # 实时显示

    # 所有 chunk 收完后，才把完整消息加入历史
    messages.append({"role": "assistant", "content": full_content})
    return full_content
```

**结论**：流式输出的"实时显示"和"历史更新"是两个独立操作——实时显示随 chunk 进行，历史更新必须等全部完成。

---

### Q169

**多轮对话的消息历史如何持久化保存？序列化到 JSON 再恢复时需要注意什么？**

消息历史本质上是 `list[dict]`，天然适合 JSON 序列化：

```python
import json
from pathlib import Path

def save_history(history: list[dict], path: str) -> None:
    Path(path).write_text(
        json.dumps(history, ensure_ascii=False, indent=2),
        encoding="utf-8"
    )

def load_history(path: str) -> list[dict]:
    p = Path(path)
    if not p.exists():
        return []
    return json.loads(p.read_text(encoding="utf-8"))
```

**恢复时需要注意的四个问题**：

| 问题 | 说明 | 解决方案 |
|-----|------|---------|
| role 值合法性 | 恢复的 role 只能是 system/user/assistant | 加载时过滤非法 role |
| content 类型 | OpenAI API 要求 content 是 str | 检查 content 不为 None 或非字符串 |
| 上下文窗口超限 | 历史很长时直接加载会超 token 限制 | 加载后立即做截断（取最后 N 轮） |
| 首条 system 消息 | 历史里可能没有 system 消息 | 加载后检查，如无则在头部插入默认 system |

**结构验证**：

```python
def validate_history(history: list) -> list[dict]:
    valid = []
    for msg in history:
        if isinstance(msg, dict) and msg.get("role") in ("system", "user", "assistant"):
            if isinstance(msg.get("content"), str):
                valid.append(msg)
    return valid
```

---

### Q170

**Few-shot 示例在 Prompt 中的作用是什么？如何在摘要任务中设计有效的 Few-shot 示例？**

**Few-shot 的作用**：通过具体的"输入→输出"示例，让模型从中推断任务的**格式、风格、长度、边界处理方式**，而不仅仅靠文字描述。对于有明确格式要求的任务（如固定长度摘要、特定领域术语），Few-shot 比 Zero-shot 稳定性提升显著。

**摘要任务的 Few-shot 设计原则**：

| 原则 | 说明 |
|-----|------|
| 示例代表典型情况 | 不同来源、不同长度的文章各选一个 |
| 覆盖边界情况 | 文章极短时如何处理、文章无实质内容时的摘要 |
| 摘要长度和格式与期望一致 | 期望 3 句话就在示例里也给 3 句话 |
| 示例质量高 | 示例的摘要必须真的好，否则模型会模仿坏示例 |

**结构示例**：

```python
SYSTEM_PROMPT = """你是一个文章摘要助手，生成3句话的摘要。

示例1：
文章：[科技新闻文章，300字]
摘要：[3句高质量摘要]

示例2：
文章：[财经新闻文章，500字]
摘要：[3句高质量摘要]

现在请对以下文章生成摘要："""
```

**注意**：示例消耗固定 Token（每次调用都发送），适合输入变化但任务固定的场景。如果每次任务不同，Few-shot 示例的通用性下降，收益减少。

---

### Q171

**Prompt A/B 对比实验应该如何设计？开发集和留出集各起什么作用？**

**核心问题**：不能用同一批数据既"调优"Prompt 又"评估"Prompt，否则 Prompt 会在这批数据上过拟合，导致实际效果虚高。

**正确的数据分层**：

| 数据集 | 大小 | 用途 |
|-------|------|------|
| 开发集（Dev Set） | 20–30 条 | 迭代调优 Prompt，可以反复看、反复改 |
| 留出集（Hold-out Set） | 20–30 条 | 最终一次性评估，Prompt 定了才能用 |

**实验设计步骤**：

```
1. 准备数据：从真实数据中随机采样，人工标注期望输出
   dev_set: 20条（迭代用）
   holdout_set: 20条（最终评估用，调优期间锁箱）

2. 在 dev_set 上运行 Prompt A 和 Prompt B，计算评估指标
   （如摘要任务：ROUGE 分数 / 人工评分 / 长度合规率）

3. 选择 dev_set 表现更好的 Prompt，仅对它做一次 holdout 评估
   → holdout 评估结果才是真实泛化能力

4. 不允许"看了 holdout 结果再改 Prompt 再测"
   → 一旦看了 holdout，这批数据就"污染"了，必须换新数据
```

**常见错误**：用同一批 50 条数据既调了 5 版 Prompt，又做了最终评估——5 次迭代后选出的是"在这 50 条数据上最优"的 Prompt，不代表泛化能力。

---

### Q172

**如何用 Few-shot 示例提升信息抽取的稳定性？示例应该覆盖哪些情况？**

信息抽取的不稳定来源：字段缺失时模型填 null 还是空字符串还是 "未提及"？枚举字段模型会乱造不在列表里的值？这些格式细节用文字约束效果有限，Few-shot 示例能直接"示范"正确行为。

**示例应该覆盖的四类情况**：

| 情况 | 示例目的 |
|-----|---------|
| 所有字段都有值的完整案例 | 告知正常格式 |
| 某字段在原文中缺失 | 示范 null 而不是填"未知" |
| 枚举字段的合法值 | 防止模型造新值（如 "sentiment" 只能是 positive/negative/neutral） |
| 原文噪音多（如口语化、错字） | 示范如何提取关键信息而非逐字复制 |

**实现**：

```python
FEW_SHOT = """
示例1（所有字段完整）：
输入：华为今天发布了 Mate70 系列，售价 5999 元起，采用麒麟 9010 芯片。
输出：{"product": "Mate70", "price": 5999, "chip": "麒麟 9010", "sentiment": "neutral"}

示例2（price 字段缺失）：
输入：苹果发布了新款 iPad Pro，搭载 M4 芯片，非常强大。
输出：{"product": "iPad Pro", "price": null, "chip": "M4", "sentiment": "positive"}

现在请抽取以下文本：
{input_text}
"""
```

**原则**：示例的答案必须是真正正确的——错误的示例会直接被模型模仿，引入系统性错误。

---

### Q173

**LLM 返回的 JSON 字符串有哪些常见格式问题？如何实现健壮的 parse_json_safe 函数？**

LLM 输出 JSON 时常见的"污染"模式：

| 问题 | 示例 |
|-----|------|
| Markdown 代码块包裹 | ` ```json\n{...}\n``` ` |
| 多余前缀文字 | "以下是结果：\n{...}" |
| 尾部注释 | `{"a": 1} // 这里 a 是名字` |
| 单引号替代双引号 | `{'name': '张三'}` |
| 字段值未加引号 | `{"status": ok}` |
| 末尾多余逗号 | `{"a": 1,}` |

**健壮的 parse_json_safe**：

```python
import json
import re
from typing import Optional

def parse_json_safe(text: str) -> Optional[dict]:
    text = text.strip()

    # 去掉 ```json...``` 或 ```...``` 包装
    md_match = re.search(r"```(?:json)?\s*([\s\S]*?)\s*```", text)
    if md_match:
        text = md_match.group(1).strip()

    # 尝试直接解析
    try:
        return json.loads(text)
    except json.JSONDecodeError:
        pass

    # 提取第一个完整的 {...} 块（处理前后有多余文字的情况）
    brace_match = re.search(r"\{[\s\S]*\}", text)
    if brace_match:
        try:
            return json.loads(brace_match.group())
        except json.JSONDecodeError:
            pass

    return None  # 所有尝试失败，由调用方处理 None
```

**使用方式**：

```python
raw = llm_call(prompt)
result = parse_json_safe(raw)
if result is None:
    # 记录日志 + 返回默认值，不要直接崩溃
    logger.warning(f"JSON 解析失败: {raw[:200]}")
    return default_result
```

**注意**：不要在 `parse_json_safe` 里 `raise`——它的职责是"尽力解析"，失败时返回 `None`，由调用方决定后续策略（重试、降级、报错）。

---

### Q174

**不看账单，如何快速估算一次 LLM 调用的成本？估算公式和关键参数是什么？**

**估算公式**：

```
成本 = (输入 token 数 × 输入单价) + (输出 token 数 × 输出单价)
```

**关键参数（以 DeepSeek 为例）**：

| 参数 | DeepSeek-V3（截至 2025 年） |
|-----|--------------------------|
| 输入单价 | ¥0.27 / 百万 token（非高峰） |
| 输出单价 | ¥1.10 / 百万 token |
| 中文 token 换算 | 约 1.5–2 个汉字 = 1 token |
| 英文 token 换算 | 约 4 个字符 = 1 token |

**快速估算步骤**：

```python
def estimate_cost(
    input_text: str,
    output_text: str,
    system_prompt: str = "",
    input_price_per_m: float = 0.27,  # 元/百万token
    output_price_per_m: float = 1.10,
) -> dict:
    # 粗估 token 数：中文每字约 0.6 token，英文每字符约 0.25 token
    def rough_tokens(text: str) -> int:
        return int(len(text) * 0.6)  # 偏保守估算

    input_tokens = rough_tokens(system_prompt + input_text)
    output_tokens = rough_tokens(output_text)

    cost = (input_tokens * input_price_per_m + output_tokens * output_price_per_m) / 1_000_000
    return {
        "input_tokens": input_tokens,
        "output_tokens": output_tokens,
        "cost_cny": round(cost, 6),
    }
```

**典型场景的量级感知**：

| 场景 | 估算成本 |
|-----|---------|
| 单条摘要（500 字输入 + 100 字输出） | ~¥0.0004 |
| 多轮对话（10 轮，每轮 200 字） | ~¥0.003 |
| 批量抽取（1000 条，每条 300 字） | ~¥0.5 |

**记住两个关键差异**：输出 token 比输入贵 4 倍（DeepSeek），要优先压缩输出（设 max_tokens）而不只压缩输入。精确计数用 `tiktoken`（OpenAI）或调用完成后看 `usage.prompt_tokens`。