# Day 1：理解大模型应用开发的全景

> 学习目标：建立对大模型应用开发的整体认知，理解 6 个核心概念

---

## 一、什么是大模型应用开发

### 1.1 定义

大模型应用开发是指：**将预训练好的大语言模型（LLM）通过 API 接入实际业务场景，构建能够理解自然语言、生成内容、执行任务的智能应用**。

### 1.2 核心特点

| 特点 | 说明 |
|------|------|
| **不训练模型** | 直接使用云端的预训练模型，通过 API 调用 |
| **以 Prompt 为核心** | 通过设计提示词来控制模型行为 |
| **快速迭代** | 无需深度学习背景，传统开发者即可上手 |
| **按量付费** | 根据使用量（Token 数）计费，降低试错成本 |

### 1.3 与传统开发的关系

```
传统开发：用户输入 → 规则/逻辑处理 → 固定输出
大模型应用：用户输入 → Prompt + 模型推理 → 灵活的自然语言输出
```

大模型应用不是替代传统开发，而是在需要理解语义、生成内容、处理非结构化数据的场景中提供新的解决方案。

### 1.4 典型应用场景

| 场景 | 示例 |
|------|------|
| **智能客服** | 理解用户问题，自动回答或转人工 |
| **文档问答** | 基于企业知识库回答员工问题 |
| **内容生成** | 自动写邮件、生成报告、创作文案 |
| **信息抽取** | 从简历、合同中提取关键信息 |
| **代码辅助** | 代码补全、代码解释、Bug 修复 |
| **数据分析** | 自然语言查询数据库，生成图表 |

---

## 二、核心概念详解

### 2.1 Prompt（提示词）

#### 2.1.1 什么是 Prompt

Prompt 是用户发送给模型的文本指令，是人与大模型交互的核心方式。模型根据 Prompt 理解任务意图，并生成相应的输出。

#### 2.1.2 Prompt 的组成要素

一个完整的 Prompt 通常包含以下要素：

```
┌─────────────────────────────────────────────────────┐
│                     Prompt 结构                      │
├─────────────────────────────────────────────────────┤
│  1. 角色设定（Role）                                  │
│     "你是一个专业的翻译助手"                          │
│                                                      │
│  2. 任务描述（Task）                                  │
│     "将下面的中文翻译成英文"                          │
│                                                      │
│  3. 上下文信息（Context）                             │
│     "这是一篇科技新闻，涉及人工智能领域"               │
│                                                      │
│  4. 输入数据（Input）                                 │
│     "原文：{待翻译文本}"                              │
│                                                      │
│  5. 输出约束（Constraints）                          │
│     "只输出翻译结果，不要添加任何解释"                 │
│                                                      │
│  6. 示例（Few-shot Examples）                        │
│     "示例：你好 → Hello"                             │
└─────────────────────────────────────────────────────┘
```

#### 2.1.3 Prompt 示例对比

**差的 Prompt：**
```
帮我总结这篇文章
```

**好的 Prompt：**
```
你是一个专业的科技媒体编辑。请对下面的文章进行摘要，要求：
1. 摘要长度控制在 100-150 字
2. 突出核心观点和关键数据
3. 使用客观、简洁的语言
4. 保持原文的逻辑顺序

文章内容：
{文章全文}
```

#### 2.1.4 Prompt 设计原则

| 原则 | 说明 | 示例 |
|------|------|------|
| **明确具体** | 避免模糊表达 | "用 3 句话总结" 而非 "简要总结" |
| **角色设定** | 赋予模型特定身份 | "你是一位资深律师" |
| **提供示例** | 用例子说明期望格式 | Few-shot learning |
| **约束输出** | 指定格式、长度、风格 | "以 JSON 格式输出" |
| **分解任务** | 复杂任务拆成多步 | "第一步...第二步..." |

---

### 2.2 Token（词元）

#### 2.2.1 什么是 Token

Token 是大模型处理文本的最小单位。模型不直接理解字符，而是将文本切分成 Token 序列进行处理。

#### 2.2.2 Token 与文本的换算

| 语言 | 换算规则（约） |
|------|---------------|
| 英文 | 1 Token ≈ 0.75 个单词 |
| 中文 | 1 Token ≈ 1-2 个汉字 |

**示例：**

```
英文："Hello, how are you?" → 约 6 个 Token
中文："你好，最近怎么样？" → 约 8-10 个 Token
```

#### 2.2.3 为什么 Token 很重要

**1. 成本计算**

| 模型 | 输入价格（每 1M Token） | 输出价格（每 1M Token） |
|------|------------------------|------------------------|
| GPT-4o | $2.50 | $10.00 |
| GPT-4o-mini | $0.15 | $0.60 |
| Claude 3.5 Sonnet | $3.00 | $15.00 |
| Qwen-Turbo | ¥0.3 | ¥0.6 |

> 价格仅供参考，以官方最新价格为准

**成本估算示例：**
```
假设：
- 每次请求平均输入 1000 Token
- 每次请求平均输出 500 Token
- 每天 1000 次调用

使用 GPT-4o-mini 的日成本：
输入：1000 × 1000 × 0.15 / 1,000,000 = $0.15
输出：1000 × 500 × 0.60 / 1,000,000 = $0.30
日总成本：$0.45
月成本（30天）：$13.5
```

**2. 上下文限制**

| 模型 | 上下文窗口 |
|------|-----------|
| GPT-4o | 128K Token |
| GPT-4o-mini | 128K Token |
| Claude 3.5 Sonnet | 200K Token |
| Qwen-Max | 32K Token |

超过限制会导致：
- 早期对话内容被截断
- 长文档无法完整处理
- RAG 检索结果无法全部放入

#### 2.2.4 Token 优化策略

| 策略 | 说明 |
|------|------|
| 精简 Prompt | 去除不必要的描述 |
| 压缩历史对话 | 只保留关键信息 |
| 分批处理长文本 | 避免一次性输入过长 |
| 选择合适的模型 | 简单任务用小模型 |

---

### 2.3 上下文窗口（Context Window）

#### 2.3.1 什么是上下文窗口

上下文窗口是模型一次能处理的最大 Token 数量，包括：
- 系统提示词（System Prompt）
- 历史对话记录
- 用户当前输入
- 模型已生成的输出

#### 2.3.2 上下文窗口的结构

```
┌────────────────────────────────────────────────────────┐
│                    上下文窗口                           │
├────────────────────────────────────────────────────────┤
│  System Prompt（系统提示）                              │
│  "你是一个有帮助的助手..."                              │
│  ────────────────────────────────────────              │
│  历史消息 1                                             │
│  User: "今天天气怎么样？"                               │
│  Assistant: "请问您在哪个城市？"                        │
│  ────────────────────────────────────────              │
│  历史消息 2                                             │
│  User: "北京"                                          │
│  Assistant: "北京今天晴，气温 25°C..."                  │
│  ────────────────────────────────────────              │
│  当前输入                                               │
│  User: "明天呢？"                                       │
│  ────────────────────────────────────────              │
│  待生成                                                 │
│  Assistant: [模型正在生成...]                           │
└────────────────────────────────────────────────────────┘
```

#### 2.3.3 上下文溢出的处理

当对话超过上下文窗口限制时，常见处理方式：

| 方式 | 说明 | 优缺点 |
|------|------|--------|
| **截断** | 丢弃最早的对话 | 简单，但丢失信息 |
| **滑动窗口** | 只保留最近 N 轮 | 平衡效果和成本 |
| **摘要压缩** | 对历史对话生成摘要 | 保留关键信息，增加处理成本 |
| **向量检索** | 将历史存入向量库，按需检索 | 复杂但效果好 |

---

### 2.4 Embedding（嵌入/向量化）

#### 2.4.1 什么是 Embedding

Embedding 是将文本转换为高维向量的过程。向量是一个数字数组，例如：

```
"人工智能" → [0.23, -0.45, 0.78, 0.12, ..., -0.34]  (1536维)
```

核心思想：**语义相似的文本，其向量在空间中的距离更近**。

#### 2.4.2 Embedding 的作用

```
传统关键词搜索：
  用户搜索："苹果手机"
  只能匹配包含"苹果"或"手机"的文档
  无法匹配："iPhone 15 Pro Max"

语义搜索（Embedding）：
  用户搜索："苹果手机"
  可以匹配：
    - "iPhone 15 Pro Max"
    - "Apple 最新款智能手机"
    - "iOS 设备推荐"
  因为这些文本的语义相近，向量距离近
```

#### 2.4.3 相似度计算

最常用的是**余弦相似度**：

```
相似度 = cos(θ) = (A · B) / (|A| × |B|)

结果范围：[-1, 1]
- 1: 完全相同方向
- 0: 无相关性
- -1: 完全相反
```

#### 2.4.4 Embedding 的应用场景

| 场景 | 说明 |
|------|------|
| **语义搜索** | 根据含义而非关键词检索 |
| **推荐系统** | 找相似内容进行推荐 |
| **聚类分析** | 将相似文本分组 |
| **RAG 检索** | 从知识库中检索相关文档 |

#### 2.4.5 常见 Embedding 模型

| 模型 | 维度 | 特点 |
|------|------|------|
| text-embedding-3-small | 1536 | OpenAI，性价比高 |
| text-embedding-3-large | 3072 | OpenAI，效果更好 |
| bge-large-zh | 1024 | 中文效果好，可本地部署 |
| m3e-base | 768 | 中文开源，轻量级 |

---

### 2.5 RAG（检索增强生成）

#### 2.5.1 什么是 RAG

RAG = Retrieval-Augmented Generation（检索增强生成）

核心思想：**先从知识库检索相关信息，再将检索结果作为上下文提供给模型，让模型基于这些信息生成回答**。

#### 2.5.2 为什么需要 RAG

| 问题 | RAG 的解决方案 |
|------|---------------|
| 模型知识有截止日期 | 可以检索最新信息 |
| 模型不知道企业内部知识 | 可以检索私有文档 |
| 模型可能"幻觉"编造 | 基于真实文档回答 |
| 上下文窗口有限 | 只检索相关片段 |

#### 2.5.3 RAG 工作流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        RAG 完整流程                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  【离线阶段：构建知识库】                                         │
│                                                                  │
│   原始文档 ──→ 文档切分 ──→ Embedding 向量化 ──→ 存入向量库        │
│     │            │              │                 │              │
│   PDF/Word    Chunk 1        [0.1,0.2,...]     向量数据库          │
│     │            │              │                 │              │
│   TXT/MD      Chunk 2        [0.3,0.4,...]      (Chroma/         │
│     │            │              │              FAISS/Milvus)     │
│              Chunk 3        [0.5,0.6,...]                        │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  【在线阶段：问答流程】                                           │
│                                                                  │
│   用户问题                                                       │
│      │                                                           │
│      ▼                                                           │
│   问题 Embedding ──→ 向量相似度检索 ──→ 返回 Top-K 相关片段        │
│      │                    │                    │                 │
│   [0.2,0.3,...]      计算 cos 相似度         Chunk 1, Chunk 3    │
│                                                (最相关的 K 个)    │
│      │                                           │               │
│      ▼                                           ▼               │
│   ┌─────────────────────────────────────────────────────┐       │
│   │                     构建 Prompt                      │       │
│   │                                                      │       │
│   │   System: 你是一个助手，请基于以下参考资料回答问题    │       │
│   │                                                      │       │
│   │   参考资料：                                         │       │
│   │   {检索到的文档片段}                                 │       │
│   │                                                      │       │
│   │   用户问题：{用户问题}                               │       │
│   └─────────────────────────────────────────────────────┘       │
│      │                                                           │
│      ▼                                                           │
│   大模型生成回答                                                  │
│      │                                                           │
│      ▼                                                           │
│   输出答案 + 可选的引用来源                                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

#### 2.5.4 RAG 的关键环节

| 环节 | 说明 | 影响因素 |
|------|------|---------|
| **文档切分** | 将长文档切成小片段 | Chunk 大小、重叠长度 |
| **向量化** | 文本转向量 | Embedding 模型选择 |
| **检索** | 找相似片段 | Top-K 数量、相似度阈值 |
| **上下文构建** | 组装 Prompt | 模板设计、信息排序 |
| **生成** | 模型回答 | 模型能力、约束设置 |

#### 2.5.5 文档切分详解

**为什么要切分？**
- 整篇文档太长，超过上下文窗口
- 检索精度：小片段更容易精确匹配

**切分参数：**

```
Chunk Size（块大小）：每个片段的 Token 数
  - 太小：语义不完整
  - 太大：检索精度下降
  - 推荐：200-500 Token

Chunk Overlap（块重叠）：相邻片段重叠的 Token 数
  - 避免关键信息被截断
  - 推荐：Chunk Size 的 10-20%
```

**切分示意：**

```
原文：这是一段很长的文档内容...

┌──────────────┐
│  Chunk 1     │
│  (300 Token) │
└──────┬───────┘
       │ ← 重叠部分
       ▼
┌──────────────┐
│  Chunk 2     │
│  (300 Token) │
└──────┬───────┘
       │
       ▼
      ...
```

---

### 2.6 Tool Calling（工具调用）

#### 2.6.1 什么是 Tool Calling

Tool Calling 让大模型能够调用外部工具或 API 来执行具体任务，从"只会说话"变成"能做事"。

#### 2.6.2 Tool Calling vs 普通对话

```
普通对话：
  用户："北京今天天气怎么样？"
  模型："抱歉，我无法获取实时天气信息..."

Tool Calling：
  用户："北京今天天气怎么样？"
  模型：[判断需要调用天气工具] → 调用 get_weather("北京")
  工具返回：{"temp": 25, "weather": "晴"}
  模型："北京今天天气晴朗，气温 25°C，适合外出活动。"
```

#### 2.6.3 Tool Calling 工作流程

```
┌───────────────────────────────────────────────────────────────┐
│                    Tool Calling 流程                           │
├───────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 用户发送问题                                                │
│     │                                                          │
│     ▼                                                          │
│  2. 模型分析问题，判断是否需要调用工具                            │
│     │                                                          │
│     ├── 不需要 → 直接生成回答                                   │
│     │                                                          │
│     └── 需要 → 决定调用哪个工具，提取参数                        │
│                │                                               │
│                ▼                                               │
│  3. 返回工具调用请求                                            │
│     {                                                          │
│       "tool_name": "get_weather",                              │
│       "arguments": {"city": "北京"}                            │
│     }                                                          │
│                │                                               │
│                ▼                                               │
│  4. 应用层执行工具调用                                          │
│     result = get_weather("北京")                               │
│                │                                               │
│                ▼                                               │
│  5. 将工具结果返回给模型                                        │
│     {                                                          │
│       "role": "tool",                                          │
│       "content": "{\"temp\": 25, \"weather\": \"晴\"}"         │
│     }                                                          │
│                │                                               │
│                ▼                                               │
│  6. 模型基于工具结果生成自然语言回答                              │
│     "北京今天天气晴朗，气温 25°C..."                             │
│                                                                │
└───────────────────────────────────────────────────────────────┘
```

#### 2.6.4 工具定义示例

```python
tools = [
    {
        "type": "function",
        "function": {
            "name": "get_weather",
            "description": "获取指定城市的天气信息",
            "parameters": {
                "type": "object",
                "properties": {
                    "city": {
                        "type": "string",
                        "description": "城市名称，如：北京、上海"
                    }
                },
                "required": ["city"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "search_web",
            "description": "搜索互联网获取信息",
            "parameters": {
                "type": "object",
                "properties": {
                    "query": {
                        "type": "string",
                        "description": "搜索关键词"
                    }
                },
                "required": ["query"]
            }
        }
    }
]
```

#### 2.6.5 常见工具类型

| 工具类型 | 示例 |
|---------|------|
| **信息查询** | 天气查询、股票行情、汇率转换 |
| **知识库检索** | 企业文档搜索、产品信息查询 |
| **数据库操作** | SQL 查询、数据统计 |
| **API 调用** | 发送邮件、创建工单、下单 |
| **代码执行** | 运行 Python 代码、数学计算 |

#### 2.6.6 Tool Calling 与 Agent 的关系

```
Tool Calling：单次工具调用能力

Agent：具备 Tool Calling 能力 + 规划能力 + 多步推理能力

示例：用户 "帮我查一下北京明天的天气，如果下雨就推荐室内活动"

Agent 工作流程：
  1. 规划：需要查天气，然后根据结果决定下一步
  2. 调用 get_weather("北京", "明天")
  3. 分析结果：下雨
  4. 调用 search_web("北京室内活动推荐")
  5. 整合信息，生成回答
```

---

## 三、学习要点总结

### 3.1 概念关系图

```
┌─────────────────────────────────────────────────────────────┐
│                    大模型应用开发核心概念                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│                      ┌─────────┐                            │
│                      │  Prompt │ ← 人机交互入口              │
│                      └────┬────┘                            │
│                           │                                  │
│                           ▼                                  │
│                    ┌─────────────┐                          │
│                    │    Token    │ ← 计费单位、处理单元        │
│                    └──────┬──────┘                          │
│                           │                                  │
│                           ▼                                  │
│               ┌───────────────────────┐                     │
│               │    上下文窗口           │ ← 容量限制          │
│               └───────────┬───────────┘                     │
│                           │                                  │
│           ┌───────────────┼───────────────┐                 │
│           ▼               ▼               ▼                 │
│    ┌───────────┐   ┌───────────┐   ┌───────────────┐        │
│    │ Embedding │   │    RAG    │   │ Tool Calling  │        │
│    │  向量化    │   │ 检索增强  │   │   工具调用    │        │
│    └─────┬─────┘   └─────┬─────┘   └───────┬───────┘        │
│          │               │                 │                 │
│          │      向量检索提供知识库      外部能力扩展           │
│          │               │                 │                 │
│          └───────────────┴─────────────────┘                 │
│                          │                                   │
│                          ▼                                   │
│                   ┌─────────────┐                           │
│                   │   大模型     │                           │
│                   │  生成回答    │                           │
│                   └─────────────┘                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 核心概念速查表

| 概念 | 一句话定义 | 关键要点 |
|------|-----------|---------|
| **Prompt** | 发给模型的指令 | 明确、具体、有约束 |
| **Token** | 模型处理的最小单位 | 影响成本和上下文限制 |
| **上下文窗口** | 一次能处理的最大 Token 数 | 超限需截断或压缩 |
| **Embedding** | 文本转向量 | 语义相似的文本向量距离近 |
| **RAG** | 检索+生成的问答模式 | 解决知识库问答问题 |
| **Tool Calling** | 让模型调用外部工具 | 从"说话"到"做事" |

---

## 四、Day 1 实践任务

### 任务 1：阅读总览文章

推荐阅读：
- OpenAI 官方文档：Platform Overview
- 各大模型平台的快速入门指南

### 任务 2：用自己的话定义核心概念

请尝试不看资料，用自己的语言写出以下概念的定义：

```
1. Prompt：___________________________________________

2. Token：___________________________________________

3. 上下文窗口：_______________________________________

4. Embedding：_______________________________________

5. RAG：_____________________________________________

6. Tool Calling：______________________________________
```

### 任务 3：输出学习笔记

撰写一份 300 字以内的 Day 1 学习笔记，包含：
- 今天学到了什么
- 哪些概念还比较模糊
- 接下来想深入了解什么

---

## 五、延伸阅读

### 5.1 推荐资源

| 资源类型 | 推荐 |
|---------|------|
| **官方文档** | OpenAI API Docs、Anthropic Docs |
| **教程** | DeepLearning.AI 短课程 |
| **社区** | Hugging Face、GitHub 优秀项目 |
| **文章** | 大模型应用开发相关技术博客 |

### 5.2 下一步预告

Day 2 将学习：
- Python 最小基础
- 虚拟环境配置
- 第一个脚本实践

---

## 六、常见问题

### Q1：大模型应用开发和传统软件开发有什么区别？

| 维度 | 传统开发 | 大模型应用开发 |
|------|---------|---------------|
| 输入处理 | 结构化输入 | 自然语言输入 |
| 输出确定性 | 输出固定 | 输出有随机性 |
| 核心能力 | 算法和数据结构 | Prompt 设计和流程编排 |
| 调试方式 | 断点、日志 | 观察输出、调整 Prompt |

### Q2：为什么强调先学原生 API 而不是直接用 LangChain？

理解原生 API 后，你能：
- 清楚消息结构和流转过程
- 准确排查问题所在
- 理解框架封装的原理
- 不被框架限制思路

### Q3：Token 计费会很贵吗？

对于学习和 Demo 阶段：
- 使用小模型（如 GPT-4o-mini），成本很低
- 精简 Prompt，减少不必要的 Token
- 控制输出长度

实际开发时：
- 做好成本预估
- 加入缓存机制
- 监控 Token 消耗

---

> 完成以上内容后，你已经建立了对大模型应用开发的整体认知。
> 
> 明天我们将进入实践环节，搭建开发环境并写出第一个脚本。
